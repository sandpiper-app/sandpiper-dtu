---
phase: 10-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/conformance.yml
  - tsconfig.base.json
  - twins/shopify/tsconfig.json
  - twins/slack/tsconfig.json
  - Dockerfile
  - docker-compose.twin.yml
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - INFRA-06

must_haves:
  truths:
    - "Slack live conformance runs on CI schedule alongside Shopify live conformance"
    - "@dtu/ui has path alias in tsconfig.base.json and twin tsconfig references for tsc --build incremental support"
    - "Dockerfile comment no longer references deleted @dtu/core package"
    - "Dockerfile uses ARG for EXPOSE port so Slack twin build exposes correct port 3001"
    - "ROADMAP.md Phase 7 shows correct completion status (2/2 plans, Complete)"
  artifacts:
    - path: ".github/workflows/conformance.yml"
      provides: "conformance-live-slack CI job"
      contains: "conformance-live-slack"
    - path: "tsconfig.base.json"
      provides: "@dtu/ui path alias"
      contains: "@dtu/ui"
    - path: "twins/shopify/tsconfig.json"
      provides: "reference to @dtu/ui package"
      contains: "packages/ui"
    - path: "twins/slack/tsconfig.json"
      provides: "reference to @dtu/ui package"
      contains: "packages/ui"
    - path: "Dockerfile"
      provides: "Correct EXPOSE via ARG, no stale @dtu/core comment"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 correct completion status"
      contains: "2/2"
  key_links:
    - from: ".github/workflows/conformance.yml"
      to: "twins/slack/package.json"
      via: "pnpm --filter @dtu/twin-slack run conformance:live script"
      pattern: "conformance:live"
    - from: "tsconfig.base.json"
      to: "packages/ui/src"
      via: "paths alias @dtu/ui -> ./packages/ui/src"
      pattern: "@dtu/ui"
    - from: "docker-compose.twin.yml"
      to: "Dockerfile"
      via: "build-arg TWIN_PORT for EXPOSE"
      pattern: "TWIN_PORT"
---

<objective>
Fix remaining tech debt: add Slack live conformance CI job, add @dtu/ui to TypeScript project references, fix Dockerfile EXPOSE and stale comment, and correct ROADMAP.md Phase 7 staleness.

Purpose: These are 4 independent configuration/documentation fixes identified in the v1.0 re-audit. None involve code logic — they are CI, build config, Docker, and documentation corrections that close the final tech debt items.

Output: Updated CI workflow, tsconfig files, Dockerfile, docker-compose, and ROADMAP.md.
</objective>

<execution_context>
@/Users/futur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/futur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tech-debt-cleanup/10-RESEARCH.md

Key source files:
@.github/workflows/conformance.yml
@tsconfig.base.json
@twins/shopify/tsconfig.json
@twins/slack/tsconfig.json
@Dockerfile
@docker-compose.twin.yml

<interfaces>
<!-- Current state of files that need fixing -->

From .github/workflows/conformance.yml — existing live job pattern:
```yaml
conformance-live:
  name: Live Conformance (scheduled only)
  if: github.event_name == 'schedule'
  runs-on: ubuntu-latest
  steps:
    # ... checkout, pnpm, node, install, build ...
    - name: Run live conformance suite against Shopify dev store
      run: pnpm --filter @dtu/twin-shopify run conformance:live
      env:
        SHOPIFY_STORE_URL: ${{ secrets.SHOPIFY_STORE_URL }}
        SHOPIFY_ACCESS_TOKEN: ${{ secrets.SHOPIFY_ACCESS_TOKEN }}
```

From tsconfig.base.json — current paths (missing @dtu/ui):
```json
"paths": {
  "@dtu/types": ["./packages/types/src"],
  "@dtu/state": ["./packages/state/src"],
  "@dtu/webhooks": ["./packages/webhooks/src"],
  "@dtu/conformance": ["./packages/conformance/src"]
}
```

From Dockerfile — issues on lines 30 and 82:
- Line 30: comment says "types → state → core → webhooks → conformance → ui" — @dtu/core was deleted
- Line 82: EXPOSE 3000 — wrong for Slack twin (port 3001)

From ROADMAP.md — Phase 7 staleness:
- Plan checkboxes show `- [ ] 07-01-PLAN.md` and `- [ ] 07-02-PLAN.md` (should be [x])
- Progress table: `| 7. Integration & E2E Testing | 0/2 | Not started | - |` (should be 2/2 | Complete | 2026-03-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Slack live conformance CI job and fix tsconfig path aliases</name>
  <files>
    .github/workflows/conformance.yml
    tsconfig.base.json
    twins/shopify/tsconfig.json
    twins/slack/tsconfig.json
  </files>
  <action>
**CI Workflow (.github/workflows/conformance.yml):**

Add a `conformance-live-slack` job after the existing `conformance-live` job. It should follow the exact same structure but target Slack:

```yaml
conformance-live-slack:
  name: Slack Live Conformance (scheduled only)
  if: github.event_name == 'schedule'
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4

    - uses: pnpm/action-setup@v4
      with:
        version: 9

    - uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: pnpm

    - name: Install dependencies
      run: pnpm install

    - name: Build packages
      run: pnpm build

    - name: Run live conformance suite against Slack workspace
      run: pnpm --filter @dtu/twin-slack run conformance:live
      env:
        SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
```

Also rename the existing `conformance-live` job's `name` from "Live Conformance (scheduled only)" to "Shopify Live Conformance (scheduled only)" for clarity now that there are two live jobs.

**tsconfig.base.json:**

Add `@dtu/ui` to the `paths` object:
```json
"@dtu/ui": ["./packages/ui/src"]
```

**twins/shopify/tsconfig.json:**

Add `@dtu/ui` to the `references` array:
```json
{ "path": "../../packages/ui" }
```

**twins/slack/tsconfig.json:**

Add `@dtu/ui` to the `references` array:
```json
{ "path": "../../packages/ui" }
```
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm build</automated>
  </verify>
  <done>
    - conformance.yml has conformance-live-slack job with SLACK_BOT_TOKEN secret, schedule-only condition
    - Existing Shopify live job renamed to "Shopify Live Conformance (scheduled only)"
    - tsconfig.base.json paths includes @dtu/ui
    - Both twin tsconfig.json files reference ../../packages/ui
    - pnpm build succeeds across entire monorepo
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Dockerfile EXPOSE/comment and correct ROADMAP Phase 7 staleness</name>
  <files>
    Dockerfile
    docker-compose.twin.yml
    .planning/ROADMAP.md
  </files>
  <action>
**Dockerfile:**

1. Fix the stale comment on line 30. Change:
   ```
   # Build shared packages first (order matters: types → state → core → webhooks → conformance → ui)
   ```
   to:
   ```
   # Build shared packages first (order matters: types → state → webhooks → conformance → ui)
   ```
   (Remove "core →" since @dtu/core was deleted in Phase 8.)

2. Add an `ARG TWIN_PORT=3000` line before `EXPOSE 3000`, then change `EXPOSE 3000` to `EXPOSE $TWIN_PORT`. Insert after the `ENV NODE_ENV=production` line in the runtime stage:
   ```dockerfile
   ARG TWIN_PORT=3000
   ```
   Then change line 82 from:
   ```dockerfile
   EXPOSE 3000
   ```
   to:
   ```dockerfile
   EXPOSE $TWIN_PORT
   ```

**docker-compose.twin.yml:**

Add `TWIN_PORT: "3001"` as a build arg for the slack-twin service so the Dockerfile EXPOSE is correct:

```yaml
slack-twin:
  build:
    context: .
    dockerfile: Dockerfile
    args:
      TWIN_NAME: slack
      TWIN_PORT: "3001"
```

The shopify-twin already has the default (3000) so no change needed there, but for explicitness add `TWIN_PORT: "3000"` to its build args as well:

```yaml
shopify-twin:
  build:
    context: .
    dockerfile: Dockerfile
    args:
      TWIN_NAME: shopify
      TWIN_PORT: "3000"
```

**ROADMAP.md (.planning/ROADMAP.md):**

1. In Phase 7 plan list, change the unchecked boxes to checked:
   - `- [ ] 07-01-PLAN.md` → `- [x] 07-01-PLAN.md`
   - `- [ ] 07-02-PLAN.md` → `- [x] 07-02-PLAN.md`

2. In the Progress table, fix the Phase 7 row:
   - Change `| 7. Integration & E2E Testing | 0/2 | Not started | - |`
   - To: `| 7. Integration & E2E Testing | 2/2 | Complete | 2026-03-01 |`
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && docker build --build-arg TWIN_NAME=shopify --build-arg TWIN_PORT=3000 -t dtu/shopify-twin-test . 2>&1 | tail -5 && docker build --build-arg TWIN_NAME=slack --build-arg TWIN_PORT=3001 -t dtu/slack-twin-test . 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Dockerfile comment references only existing packages (no @dtu/core)
    - Dockerfile uses ARG TWIN_PORT with EXPOSE $TWIN_PORT
    - docker-compose.twin.yml passes TWIN_PORT build arg for both twins
    - ROADMAP.md Phase 7 plan checkboxes are [x] (checked)
    - ROADMAP.md Progress table shows Phase 7 as 2/2 | Complete | 2026-03-01
    - Docker builds succeed for both twins
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` — Full monorepo build passes with updated tsconfig references
2. Docker build for both twins succeeds with correct EXPOSE values
3. `conformance.yml` has both `conformance-live` (Shopify) and `conformance-live-slack` jobs
4. `tsconfig.base.json` paths includes all 5 packages: types, state, webhooks, conformance, ui
5. Both twin tsconfig.json files reference @dtu/ui package
6. ROADMAP.md Phase 7 shows correct 2/2 Complete status
7. Dockerfile has no stale @dtu/core reference
</verification>

<success_criteria>
- Slack live conformance CI job exists and mirrors Shopify live conformance structure
- @dtu/ui has tsconfig path alias and both twins reference it
- Dockerfile EXPOSE is parameterized via ARG, stale comment fixed
- ROADMAP.md accurately reflects Phase 7 completion
- No build regressions across the monorepo
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-debt-cleanup/10-02-SUMMARY.md`
</output>
