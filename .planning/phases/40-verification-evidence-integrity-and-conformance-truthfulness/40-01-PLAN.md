---
phase: 40-verification-evidence-integrity-and-conformance-truthfulness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/sdk-verification/coverage/truthfulness-contract.test.ts
autonomous: true
requirements:
  - INFRA-23
  - INFRA-24
  - INFRA-25
must_haves:
  truths:
    - "Phase 40 requirement IDs exist in REQUIREMENTS.md and are mapped to Phase 40 before implementation work begins"
    - "There is an automated red contract proving the current branch still fails the Phase 40 truthfulness bar"
    - "Executors have a single source of truth for what Phase 40 must make true"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Definitions for INFRA-23, INFRA-24, and INFRA-25 plus Phase 40 traceability rows"
    - path: "tests/sdk-verification/coverage/truthfulness-contract.test.ts"
      provides: "Automated truthfulness contract covering coverage provenance and conformance wording"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 40 requirement IDs and traceability rows agree"
      pattern: "INFRA-23|INFRA-24|INFRA-25"
    - from: "tests/sdk-verification/coverage/truthfulness-contract.test.ts"
      to: "tests/sdk-verification/coverage/coverage-report.json"
      via: "JSON assertions for phase/provenance fields"
      pattern: "phase.*40|runtime-symbol-execution"
    - from: "tests/sdk-verification/coverage/truthfulness-contract.test.ts"
      to: "twins/slack/conformance/index.ts"
      via: "string assertion rejecting overstated suite wording"
      pattern: "Complete Slack Web API conformance suite"
---

<objective>
Repair Phase 40 requirement traceability before implementation starts and lock in an automated red contract for the truthfulness gaps called out by the research and validation files.

Purpose: Prevent the rest of the phase from building on undefined requirement IDs or vague "make it truthful" language.
Output: Updated requirement definitions/traceability and a failing contract test that subsequent plans must turn green.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-RESEARCH.md
@.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-VALIDATION.md
@.planning/v1.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define INFRA-23..25 and repair Phase 40 traceability</name>
  <files>
.planning/REQUIREMENTS.md
  </files>
  <read_first>
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-RESEARCH.md
.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-VALIDATION.md
  </read_first>
  <action>Add three new v1.2 Conformance Infrastructure requirements directly below INFRA-22 with these concrete meanings: INFRA-23 = SDK coverage status is derived from per-symbol execution evidence emitted during the SDK test run, not a hand-authored symbol map; INFRA-24 = conformance suites and reporters only claim parity where deterministic value checks or exact comparisons actually prove it; INFRA-25 = generated coverage reports, drift gates, and checked-in verification docs carry provenance metadata and reject stale or misleading "live" or "complete" claims. Then update the v1.2 traceability table so `INFRA-23`, `INFRA-24`, and `INFRA-25` each map to `Phase 40` with `Pending` status, and update the v1.2 coverage summary to the exact text `v1.2 requirements: 26 total (23 complete, 3 pending)`, `Mapped to phases: 26`, and `Unmapped: 0`. Do not invent any additional requirement IDs.</action>
  <verify>
    <automated>rg -n "INFRA-23|INFRA-24|INFRA-25" .planning/REQUIREMENTS.md && rg -n "\| INFRA-23 \| Phase 40 \| Pending \||\| INFRA-24 \| Phase 40 \| Pending \||\| INFRA-25 \| Phase 40 \| Pending \|" .planning/REQUIREMENTS.md && rg -n "v1.2 requirements: 26 total \(23 complete, 3 pending\)|Mapped to phases: 26|Unmapped: 0" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
- `.planning/REQUIREMENTS.md` contains literal requirement headings `**INFRA-23**`, `**INFRA-24**`, and `**INFRA-25**`.
- The v1.2 traceability table contains rows `| INFRA-23 | Phase 40 | Pending |`, `| INFRA-24 | Phase 40 | Pending |`, and `| INFRA-25 | Phase 40 | Pending |`.
- The coverage summary block says `v1.2 requirements: 26 total (23 complete, 3 pending)`, `Mapped to phases: 26`, and `Unmapped: 0`.
  </acceptance_criteria>
  <done>Phase 40 no longer depends on undefined requirement IDs; the repo has explicit requirement text and traceability for all three Phase 40 IDs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the red truthfulness contract test for coverage provenance and conformance wording</name>
  <files>
tests/sdk-verification/coverage/truthfulness-contract.test.ts
  </files>
  <read_first>
tests/sdk-verification/coverage/coverage-report.json
tests/sdk-verification/coverage/generate-report-evidence.ts
twins/slack/conformance/index.ts
.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-RESEARCH.md
.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: coverage report requires `phase === '40'` plus top-level provenance fields `evidenceSource`, `executionArtifact`, and `vitestArtifact`.
    - Test 2: no symbol entry may expose a bare `testFile` field as the only provenance mechanism.
    - Test 3: generator source must not declare `const EVIDENCE_MAP`.
    - Test 4: `twins/slack/conformance/index.ts` must not contain the phrase `Complete Slack Web API conformance suite`.
  </behavior>
  <action>Create `tests/sdk-verification/coverage/truthfulness-contract.test.ts` as a red contract that reads the checked-in `coverage-report.json`, `generate-report-evidence.ts`, and `twins/slack/conformance/index.ts` from disk. Make the assertions target the exact future state from Phase 40: report phase `40`, provenance source `runtime-symbol-execution`, execution artifact path `tests/sdk-verification/coverage/symbol-execution.json`, Vitest artifact path `tests/sdk-verification/coverage/vitest-evidence.json`, no top-level `testFile` keys in symbol entries, no `const EVIDENCE_MAP` declaration in the generator, and no `Complete Slack Web API conformance suite` wording in the Slack suite index. Keep the test failing on the current branch; do not patch implementation files in this plan.</action>
  <verify>
    <automated>OUTPUT=$(pnpm vitest run tests/sdk-verification/coverage/truthfulness-contract.test.ts 2>&1); STATUS=$?; printf '%s\n' "$OUTPUT"; test $STATUS -ne 0 && printf '%s' "$OUTPUT" | rg -q "Expected|Assertion" && ! printf '%s' "$OUTPUT" | rg -q "SyntaxError|Cannot find module|Failed to load"</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/coverage/truthfulness-contract.test.ts` contains literal assertions for `phase === '40'`, `runtime-symbol-execution`, `tests/sdk-verification/coverage/symbol-execution.json`, and `Complete Slack Web API conformance suite`.
- The automated verify command exits non-zero against the current branch because Phase 40 implementation has not landed yet.
- The captured test output shows assertion-style failure text rather than syntax, import, or module-load errors.
- No implementation file outside the new test file changes in this task.
  </acceptance_criteria>
  <done>The repo has an executable red contract for the exact truthfulness gaps that the later Phase 40 plans must close.</done>
</task>

</tasks>

<verification>
Run the requirement grep checks, then run the red contract test and confirm it fails for the expected Phase 40 reasons rather than for syntax or import errors.
</verification>

<success_criteria>
Phase 40 has explicit requirement definitions, traceability rows, and a failing automated contract that names the exact provenance and wording changes the later plans must deliver.
</success_criteria>

<output>
After completion, create `.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-01-SUMMARY.md`
</output>
