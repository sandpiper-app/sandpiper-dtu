---
phase: 40-verification-evidence-integrity-and-conformance-truthfulness
plan: "02"
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - tests/sdk-verification/vitest.config.ts
  - tests/sdk-verification/setup/global-setup.ts
  - tests/sdk-verification/setup/register-execution-evidence.ts
  - tests/sdk-verification/setup/execution-evidence-runtime.ts
  - tests/sdk-verification/helpers/slack-client.ts
  - tests/sdk-verification/helpers/shopify-client.ts
  - tests/sdk-verification/helpers/shopify-rest-client.ts
  - tests/sdk-verification/helpers/shopify-api-client.ts
  - tests/sdk-verification/coverage/generate-report-evidence.ts
  - tests/sdk-verification/coverage/coverage-report.json
autonomous: true
requirements:
  - INFRA-23
must_haves:
  truths:
    - "SDK symbol coverage is derived from runtime symbol hits captured during the test run"
    - "coverage-report.json records Phase 40 provenance metadata naming the execution and Vitest artifacts"
    - "A hand-authored symbol-to-test map is no longer the mechanism that makes a symbol live"
    - "The regenerated Phase 40 coverage report preserves `summary.live >= 222` after the EVIDENCE_MAP removal"
  artifacts:
    - path: "tests/sdk-verification/setup/register-execution-evidence.ts"
      provides: "Narrow setup hook for direct-constructor SDK call sites that bypass shared helpers"
    - path: "tests/sdk-verification/setup/execution-evidence-runtime.ts"
      provides: "Runtime evidence recorder and JSON flush for symbol execution"
    - path: "tests/sdk-verification/helpers/slack-client.ts"
      provides: "Primary helper-seam capture for shared Slack SDK calls"
    - path: "tests/sdk-verification/helpers/shopify-api-client.ts"
      provides: "Primary helper-seam capture for shared Shopify SDK calls"
    - path: "tests/sdk-verification/coverage/generate-report-evidence.ts"
      provides: "Generator that derives live/deferred status from runtime symbol evidence"
    - path: "tests/sdk-verification/coverage/coverage-report.json"
      provides: "Phase 40 provenance-rich coverage report"
  key_links:
    - from: "tests/sdk-verification/vitest.config.ts"
      to: "tests/sdk-verification/setup/register-execution-evidence.ts"
      via: "Vitest setupFiles registration"
      pattern: "setupFiles"
    - from: "tests/sdk-verification/setup/execution-evidence-runtime.ts"
      to: "tests/sdk-verification/coverage/symbol-execution.json"
      via: "JSON artifact written after the test run"
      pattern: "symbol-execution.json"
    - from: "tests/sdk-verification/helpers/slack-client.ts"
      to: "tests/sdk-verification/setup/execution-evidence-runtime.ts"
      via: "helper seam emits runtime symbol hits instead of hand-authored attribution"
      pattern: "recordSymbolHit"
    - from: "tests/sdk-verification/coverage/generate-report-evidence.ts"
      to: "tests/sdk-verification/coverage/vitest-evidence.json"
      via: "intersects symbol hits with passing tests"
      pattern: "vitest-evidence.json"
---

<objective>
Replace hand-authored symbol attribution with actual runtime symbol execution evidence and regenerate the SDK coverage report with Phase 40 provenance.

Purpose: INFRA-23 is the core technical repair for the phase; without it the rest of the truthfulness story is still synthetic.
Output: Runtime symbol evidence capture, a new execution artifact, and a provenance-rich Phase 40 coverage report.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-RESEARCH.md
@.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-VALIDATION.md
@tests/sdk-verification/vitest.config.ts
@tests/sdk-verification/setup/global-setup.ts
@tests/sdk-verification/helpers/slack-client.ts
@tests/sdk-verification/helpers/shopify-client.ts
@tests/sdk-verification/helpers/shopify-rest-client.ts
@tests/sdk-verification/helpers/shopify-api-client.ts
@tests/sdk-verification/coverage/generate-report-evidence.ts
@tests/sdk-verification/coverage/coverage-report.json
@tests/sdk-verification/sdk/slack-method-coverage.test.ts
@tests/sdk-verification/sdk/shopify-api-rest-client.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture per-symbol runtime execution evidence at helper seams and explicit exception hooks</name>
  <files>
tests/sdk-verification/vitest.config.ts
tests/sdk-verification/setup/global-setup.ts
tests/sdk-verification/setup/register-execution-evidence.ts
tests/sdk-verification/setup/execution-evidence-runtime.ts
tests/sdk-verification/helpers/slack-client.ts
tests/sdk-verification/helpers/shopify-client.ts
tests/sdk-verification/helpers/shopify-rest-client.ts
tests/sdk-verification/helpers/shopify-api-client.ts
  </files>
  <read_first>
tests/sdk-verification/vitest.config.ts
tests/sdk-verification/setup/global-setup.ts
tests/sdk-verification/helpers/slack-client.ts
tests/sdk-verification/helpers/shopify-client.ts
tests/sdk-verification/helpers/shopify-rest-client.ts
tests/sdk-verification/helpers/shopify-api-client.ts
tests/sdk-verification/sdk/slack-method-coverage.test.ts
tests/sdk-verification/sdk/shopify-api-rest-client.test.ts
.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-RESEARCH.md
  </read_first>
  <action>Create a new Vitest setup file `tests/sdk-verification/setup/register-execution-evidence.ts` plus a shared runtime helper `tests/sdk-verification/setup/execution-evidence-runtime.ts`. Use the checked-in manifest files under `tools/sdk-surface/manifests/` as the only symbol catalog. Instrument the four existing helper seams `tests/sdk-verification/helpers/slack-client.ts`, `tests/sdk-verification/helpers/shopify-client.ts`, `tests/sdk-verification/helpers/shopify-rest-client.ts`, and `tests/sdk-verification/helpers/shopify-api-client.ts` so they call a shared `recordSymbolHit(...)` API keyed by manifest symbol IDs. Use `register-execution-evidence.ts` only for the explicit direct-constructor exception set that bypasses helpers today (Bolt receiver tests, `InstallProvider`, and the OIDC `new WebClient(...)` flow), and keep that exception list documented in code comments instead of trying to patch every SDK export in the manifest. Add `setupFiles` alongside the existing `globalSetup` entry in `tests/sdk-verification/vitest.config.ts` — do not replace `globalSetup`. The setup file manages per-test-file context plus recorder flush lifecycle; `globalSetup` continues to boot and reset twins. Persist `tests/sdk-verification/coverage/symbol-execution.json` with a top-level `generatedAt` timestamp plus hit records shaped around `{ symbol, packageName, version, testFile, testName, hitCount }`. Update `tests/sdk-verification/setup/global-setup.ts` to remove any stale `symbol-execution.json` before each full project run. Do not add, preserve, or rename an `EVIDENCE_MAP` fallback.</action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/sdk/slack-method-coverage.test.ts tests/sdk-verification/sdk/shopify-api-rest-client.test.ts --reporter=verbose --reporter=json --outputFile.json=tests/sdk-verification/coverage/vitest-evidence.json && test -f tests/sdk-verification/coverage/symbol-execution.json && node -e "const fs=require('fs'); const raw=JSON.parse(fs.readFileSync('tests/sdk-verification/coverage/symbol-execution.json','utf8')); const text=JSON.stringify(raw); if(!raw.generatedAt) process.exit(1); if(!text.includes('@slack/web-api@7.14.1/WebClient.admin.users.list')) process.exit(1); if(!text.includes('@shopify/shopify-api@12.3.0/RestClient.get')) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/vitest.config.ts` contains a `setupFiles` entry for `setup/register-execution-evidence.ts`.
- `tests/sdk-verification/helpers/slack-client.ts` and `tests/sdk-verification/helpers/shopify-api-client.ts` contain `recordSymbolHit(` calls for shared helper-seam capture.
- Running the targeted SDK tests creates `tests/sdk-verification/coverage/symbol-execution.json`.
- `tests/sdk-verification/coverage/symbol-execution.json` includes a top-level `generatedAt` field.
- The execution artifact contains literal symbol keys `@slack/web-api@7.14.1/WebClient.admin.users.list` and `@shopify/shopify-api@12.3.0/RestClient.get` after the targeted run.
- No file in this task declares `const EVIDENCE_MAP`.
  </acceptance_criteria>
  <done>Runtime symbol hits are captured during SDK verification runs and persisted to a machine-readable execution artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the coverage generator to consume runtime symbol evidence and emit a Phase 40 report</name>
  <files>
tests/sdk-verification/coverage/generate-report-evidence.ts
tests/sdk-verification/coverage/coverage-report.json
  </files>
  <read_first>
tests/sdk-verification/coverage/generate-report-evidence.ts
tests/sdk-verification/coverage/coverage-report.json
tests/sdk-verification/coverage/truthfulness-contract.test.ts
tests/sdk-verification/coverage/symbol-execution.json
tests/sdk-verification/coverage/vitest-evidence.json
  </read_first>
  <action>Rewrite `tests/sdk-verification/coverage/generate-report-evidence.ts` so live/deferred status comes from the runtime artifact produced in Task 1 intersected with passing test files from `vitest-evidence.json`. Emit a top-level `provenance` object with exact keys `evidenceSource: 'runtime-symbol-execution'`, `executionArtifact: 'tests/sdk-verification/coverage/symbol-execution.json'`, and `vitestArtifact: 'tests/sdk-verification/coverage/vitest-evidence.json'`; set `phase: '40'`; and store per-symbol evidence inside an `evidence` object containing at least `hitCount` and `testFiles`. Remove the legacy symbol-to-test-file map and any top-level per-symbol `testFile` field. After the targeted smoke run proves capture wiring, rerun the full SDK verification suite with `pnpm test:sdk --reporter=verbose --reporter=json --outputFile.json=tests/sdk-verification/coverage/vitest-evidence.json`, then run `pnpm coverage:generate` so the checked-in `tests/sdk-verification/coverage/coverage-report.json` is regenerated from full-suite evidence rather than the two-file smoke sample. Keep live-count continuity by ensuring the regenerated report still has `summary.live >= 222`.</action>
  <verify>
    <automated>pnpm test:sdk --reporter=verbose --reporter=json --outputFile.json=tests/sdk-verification/coverage/vitest-evidence.json && pnpm coverage:generate && node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('tests/sdk-verification/coverage/coverage-report.json','utf8')); if(r.phase!=='40') process.exit(1); if(r.provenance?.evidenceSource!=='runtime-symbol-execution') process.exit(1); if(r.provenance?.executionArtifact!=='tests/sdk-verification/coverage/symbol-execution.json') process.exit(1); if(r.provenance?.vitestArtifact!=='tests/sdk-verification/coverage/vitest-evidence.json') process.exit(1); const text=JSON.stringify(r); if(text.includes('\"testFile\"')) process.exit(1); if((r.summary?.live ?? 0) < 222) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/coverage/coverage-report.json` contains `"phase": "40"`.
- The report contains `provenance.evidenceSource = "runtime-symbol-execution"`, `provenance.executionArtifact = "tests/sdk-verification/coverage/symbol-execution.json"`, and `provenance.vitestArtifact = "tests/sdk-verification/coverage/vitest-evidence.json"`.
- Symbol entries no longer expose a top-level `testFile` key; they expose an `evidence` object with `hitCount` and `testFiles`.
- The regenerated report preserves `summary.live >= 222`.
- `tests/sdk-verification/coverage/generate-report-evidence.ts` no longer declares `const EVIDENCE_MAP`.
  </acceptance_criteria>
  <done>The checked-in coverage report is Phase 40 truthfulness-aware and derived from actual symbol execution rather than a hand-maintained attribution map.</done>
</task>

</tasks>

<verification>
Run the targeted SDK smoke tests first to prove helper-seam and exception-hook capture, then rerun the full SDK suite with the JSON reporter, regenerate the report, and confirm the evidence transition keeps `summary.live >= 222` before Plan 40-03 hardens the gate.
</verification>

<success_criteria>
INFRA-23 is satisfied: the repo generates a Phase 40 coverage report from runtime symbol execution evidence, not from a hand-authored symbol-to-test mapping, and it preserves the existing 222+ live-symbol floor through the transition.
</success_criteria>

<output>
After completion, create `.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-02-SUMMARY.md`
</output>
