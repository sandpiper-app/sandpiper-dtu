---
phase: 40-verification-evidence-integrity-and-conformance-truthfulness
plan: "03"
type: execute
wave: 3
depends_on:
  - 40-02
files_modified:
  - tests/sdk-verification/drift/check-drift.ts
  - tests/sdk-verification/drift/report-provenance.ts
  - tests/sdk-verification/drift/report-provenance.test.ts
autonomous: true
requirements:
  - INFRA-25
must_haves:
  truths:
    - "drift:check fails when coverage provenance is missing, stale, or legacy-shaped"
    - "A passing drift gate proves both freshness and provenance, not just a live-count floor"
    - "Phase 40 truthfulness gates are codified in unit-tested helpers rather than inline ad hoc checks"
  artifacts:
    - path: "tests/sdk-verification/drift/report-provenance.ts"
      provides: "Pure validation helpers for report provenance and freshness"
    - path: "tests/sdk-verification/drift/report-provenance.test.ts"
      provides: "Regression tests covering missing provenance, stale reports, and legacy fields"
    - path: "tests/sdk-verification/drift/check-drift.ts"
      provides: "Hard-fail gate for provenance-aware coverage truthfulness"
  key_links:
    - from: "tests/sdk-verification/drift/check-drift.ts"
      to: "tests/sdk-verification/drift/report-provenance.ts"
      via: "CLI gate delegates report truthfulness checks to shared helpers"
      pattern: "validateCoverageReportTruthfulness"
    - from: "tests/sdk-verification/drift/check-drift.ts"
      to: "tests/sdk-verification/coverage/coverage-report.json"
      via: "freshness and provenance validation before live-count enforcement"
      pattern: "runtime-symbol-execution"
---

<objective>
Turn provenance truthfulness into a hard gate so stale or misleading coverage reports cannot pass CI just because the live count is still above the floor.

Purpose: INFRA-25 is the enforcement layer for the Phase 40 evidence model.
Output: Unit-tested provenance validators and a hardened `pnpm drift:check`.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-RESEARCH.md
@.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-VALIDATION.md
@tests/sdk-verification/drift/check-drift.ts
@tests/sdk-verification/coverage/coverage-report.json
@tests/sdk-verification/coverage/truthfulness-contract.test.ts
@tests/sdk-verification/coverage/symbol-execution.json
@tests/sdk-verification/coverage/vitest-evidence.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit-tested provenance and freshness validators for the coverage report</name>
  <files>
tests/sdk-verification/drift/report-provenance.ts
tests/sdk-verification/drift/report-provenance.test.ts
  </files>
  <read_first>
tests/sdk-verification/drift/check-drift.ts
tests/sdk-verification/coverage/coverage-report.json
tests/sdk-verification/coverage/truthfulness-contract.test.ts
.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: report with no `provenance.evidenceSource` fails.
    - Test 2: report whose `generatedAt` is older than `symbol-execution.json.generatedAt` or `vitest-evidence.json.startTime` fails.
    - Test 3: any symbol entry containing a legacy top-level `testFile` fails.
    - Test 4: report with `runtime-symbol-execution` provenance and fresh artifacts passes.
  </behavior>
  <action>Create `tests/sdk-verification/drift/report-provenance.ts` as a pure helper module exporting a validator function for coverage report truthfulness, and back it with `tests/sdk-verification/drift/report-provenance.test.ts`. Make the helper enforce these exact rules: top-level `provenance.evidenceSource` must equal `runtime-symbol-execution`; `provenance.executionArtifact` and `provenance.vitestArtifact` must be present; the referenced artifact files must exist; `tests/sdk-verification/coverage/symbol-execution.json` must expose a top-level `generatedAt`; `vitest-evidence.json` must expose a top-level `startTime`; and `coverage-report.json.generatedAt` must be newer than or equal to both of those embedded timestamps. Fail if any required timestamp is missing or unparsable. Also fail when any symbol entry contains a legacy top-level `testFile` field. Keep the function data-driven so `check-drift.ts` can call it directly without duplicating logic.</action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/drift/report-provenance.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/drift/report-provenance.ts` exports a provenance validator used by later CLI code.
- `tests/sdk-verification/drift/report-provenance.test.ts` contains cases for missing provenance, stale embedded timestamps, legacy `testFile`, and a passing Phase 40-shaped report.
- The automated verify command exits 0.
  </acceptance_criteria>
  <done>Coverage truthfulness rules are captured in unit-tested code instead of being implicit in comments or ad hoc CLI branches.</done>
</task>

<task type="auto">
  <name>Task 2: Harden `pnpm drift:check` to fail stale or misleading coverage reports</name>
  <files>
tests/sdk-verification/drift/check-drift.ts
  </files>
  <read_first>
tests/sdk-verification/drift/check-drift.ts
tests/sdk-verification/drift/report-provenance.ts
tests/sdk-verification/coverage/coverage-report.json
tests/sdk-verification/coverage/symbol-execution.json
tests/sdk-verification/coverage/vitest-evidence.json
  </read_first>
  <action>Update `tests/sdk-verification/drift/check-drift.ts` so the coverage section runs the shared provenance validator before applying the live-count floor. Keep the existing `summary.live >= 222` regression floor, but add hard failures when the report lacks `runtime-symbol-execution` provenance, when the execution or Vitest artifact paths are missing, when `coverage-report.json.generatedAt` is older than `symbol-execution.json.generatedAt` or `new Date(vitest-evidence.json.startTime).toISOString()`, or when any symbol entry still uses the legacy top-level `testFile` shape. Task 1's unit tests own the negative-path coverage; this task wires the validator into the CLI and proves the happy-path gate stays green with fresh artifacts. Make the CLI output name the failing provenance rule explicitly so Phase 40 regressions are obvious in CI logs.</action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/drift/report-provenance.test.ts && pnpm drift:check</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/drift/check-drift.ts` contains the literal string `runtime-symbol-execution`.
- `pnpm drift:check` exits 0 with the fresh Phase 40 artifacts.
- The CLI no longer treats live count alone as sufficient evidence of truthfulness.
  </acceptance_criteria>
  <done>`pnpm drift:check` now enforces Phase 40 provenance and freshness rules instead of only enforcing a coverage count floor.</done>
</task>

</tasks>

<verification>
Run the provenance-helper unit tests first to prove the negative cases, then run `pnpm drift:check` and confirm the coverage section validates provenance before reporting the live-count result.
</verification>

<success_criteria>
INFRA-25 is satisfied for the gating layer: misleading or stale coverage reports fail fast, and a passing drift gate proves provenance freshness plus live-count continuity.
</success_criteria>

<output>
After completion, create `.planning/phases/40-verification-evidence-integrity-and-conformance-truthfulness/40-03-SUMMARY.md`
</output>
