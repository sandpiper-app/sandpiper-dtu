---
phase: 23-shopify-oauth-storefront
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/state/src/state-manager.ts
  - twins/shopify/src/plugins/admin.ts
  - twins/shopify/src/plugins/oauth.ts
  - twins/shopify/src/services/token-validator.ts
  - tests/sdk-verification/sdk/shopify-api-auth.test.ts
autonomous: true
requirements:
  - SHOP-18

must_haves:
  truths:
    - "GET /admin/oauth/authorize redirects to redirect_uri with HMAC-signed query string carrying code, shop, state, timestamp"
    - "POST /admin/oauth/access_token returns access_token+scope when client_id, client_secret, and a valid code are provided"
    - "POST /admin/oauth/access_token returns 400 error when body is empty, missing required fields, code is unknown, or code is expired (older than 60 seconds)"
    - "POST /admin/oauth/access_token preserves client_credentials, token-exchange, and refresh_token grant types (no regression)"
    - "shopify.auth.callback() completes the full begin->authorize->callback flow against the live twin and returns a session"
  artifacts:
    - path: "packages/state/src/state-manager.ts"
      provides: "oauth_codes table + token_type column on tokens table"
      contains: "oauth_codes"
    - path: "twins/shopify/src/plugins/oauth.ts"
      provides: "GET /admin/oauth/authorize + tightened POST /admin/oauth/access_token"
      exports: ["oauthPlugin"]
    - path: "twins/shopify/src/services/token-validator.ts"
      provides: "tokenType field in TokenValidationResult"
      exports: ["validateAccessToken"]
  key_links:
    - from: "twins/shopify/src/plugins/oauth.ts (GET /admin/oauth/authorize)"
      to: "packages/state/src/state-manager.ts (oauth_codes table)"
      via: "storeOAuthCode() / consumeOAuthCode()"
      pattern: "storeOAuthCode|consumeOAuthCode"
    - from: "tests/sdk-verification/sdk/shopify-api-auth.test.ts (callback test)"
      to: "twins/shopify/src/plugins/oauth.ts (GET /admin/oauth/authorize)"
      via: "fetch to twin authorize URL to obtain a real code"
      pattern: "admin/oauth/authorize"
---

<objective>
Implement a real OAuth authorize/callback flow on the Shopify twin and harden the
access_token endpoint so it validates credentials and codes.

Purpose: The SDK's auth.begin() + auth.callback() flow requires a real authorize
redirect with HMAC-signed callback params and code validation — the current twin
short-circuits both.

Output:
- GET /admin/oauth/authorize endpoint (new)
- POST /admin/oauth/access_token tightened (validates client_id, client_secret, code)
- oauth_codes SQLite table + token_type column on tokens table
- validateAccessToken updated to return tokenType
- Auth test updated so callback test uses the twin's authorize flow for a real code
</objective>

<execution_context>
@/Users/futur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/futur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-shopify-oauth-storefront/23-RESEARCH.md

@packages/state/src/state-manager.ts
@twins/shopify/src/plugins/oauth.ts
@twins/shopify/src/plugins/admin.ts
@twins/shopify/src/services/token-validator.ts
@tests/sdk-verification/sdk/shopify-api-auth.test.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From packages/state/src/state-manager.ts — current tokens table (to extend):
```sql
CREATE TABLE IF NOT EXISTS tokens (
  token TEXT PRIMARY KEY,
  shop_domain TEXT NOT NULL,
  scopes TEXT NOT NULL,
  created_at INTEGER NOT NULL
);
```

createToken signature:
```typescript
createToken(token: string, shopDomain: string, scopes: string): void
```
getToken returns:
```typescript
{ token: string; shop_domain: string; scopes: string; created_at: number } | undefined
```

From twins/shopify/src/services/token-validator.ts — current return shape (to extend):
```typescript
export interface TokenValidationResult {
  valid: boolean;
  shopDomain?: string;
}
```

From twins/shopify/src/plugins/admin.ts — POST /admin/tokens body (to extend):
```typescript
Body: { token: string; shopDomain?: string; scopes?: string }
// calls: fastify.stateManager.createToken(token, shopDomain, scopes)
```

From tests/sdk-verification/sdk/shopify-api-auth.test.ts — callback test currently
builds params manually with code: 'test-code-123' (line ~234); must be replaced with
a real code obtained from GET /admin/oauth/authorize on the twin.
The test already has computeCallbackHmac() helper (hex SHA-256 HMAC).

HMAC algorithm for OAuth callback (from research, confirmed from SDK source):
```typescript
import { createHmac } from 'node:crypto';
// Exclude hmac/signature keys, sort remaining with localeCompare, URLSearchParams encode
const { hmac: _h, signature: _s, ...rest } = params;
const sortedKeys = Object.keys(rest).sort((a, b) => a.localeCompare(b));
const qs = new URLSearchParams(sortedKeys.map(k => [k, rest[k]])).toString();
return createHmac('sha256', secret).update(qs).digest('hex');
```

SDK sends to POST /admin/oauth/access_token:
```json
{ "client_id": "test-api-key", "client_secret": "test-api-secret", "code": "<code>", "expiring": "0" }
```
Content-Type: application/json (handled by Fastify default JSON parser).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StateManager oauth_codes table + token_type column, admin.ts and token-validator updates, test scaffold</name>
  <files>
    packages/state/src/state-manager.ts,
    twins/shopify/src/plugins/admin.ts,
    twins/shopify/src/services/token-validator.ts,
    tests/sdk-verification/sdk/shopify-api-auth.test.ts
  </files>
  <behavior>
    - tokens table gains `token_type TEXT NOT NULL DEFAULT 'admin'` column via ALTER TABLE in runMigrations() — use `ALTER TABLE tokens ADD COLUMN token_type TEXT NOT NULL DEFAULT 'admin'` wrapped in a try/catch so it is idempotent (SQLite throws if column already exists)
    - New oauth_codes table created in runMigrations():
      `CREATE TABLE IF NOT EXISTS oauth_codes (code TEXT PRIMARY KEY, created_at INTEGER NOT NULL)`
    - createToken gains optional 4th param: `createToken(token, shopDomain, scopes, tokenType?: string = 'admin')` — INSERT includes token_type value
    - getToken return type gains `token_type: string`
    - New StateManager methods: `storeOAuthCode(code: string): void` and `consumeOAuthCode(code: string): boolean` (returns true if code existed, was not expired, and was deleted; false if not found or expired). Expired means `created_at + 60 < Math.floor(Date.now() / 1000)` — codes older than 60 seconds are deleted and treated as not found
    - validateAccessToken return type adds `tokenType?: string`; implementation reads `tokenRecord.token_type` and includes it in the result
    - admin.ts POST /admin/tokens body extended: `{ token, shopDomain?, scopes?, tokenType? }`; passes tokenType to createToken (default 'admin')
    - NOTE: Validation tests for the access_token endpoint (empty body, missing fields, unknown code, expired code) are added in Task 2 after the endpoint is tightened — they would pass vacuously against the permissive endpoint in Task 1
    - All new prepared statements for oauth_codes and token_type must be added to reset() and close() null-out lists
    - For `ALTER TABLE tokens ADD COLUMN token_type`: use `try { db.exec("ALTER TABLE tokens ADD COLUMN token_type TEXT NOT NULL DEFAULT 'admin'") } catch {}` inside runMigrations to handle idempotency
  </behavior>
  <action>
    In packages/state/src/state-manager.ts:
    1. Add private statement fields: `private storeOAuthCodeStmt`, `private consumeOAuthCodeStmt`, `private createTokenV2Stmt` (new insert with token_type)
    2. runMigrations: add `ALTER TABLE tokens ADD COLUMN token_type TEXT NOT NULL DEFAULT 'admin'` (try/catch idempotent); add `CREATE TABLE IF NOT EXISTS oauth_codes (code TEXT PRIMARY KEY, created_at INTEGER NOT NULL)`
    3. prepareStatements: replace createTokenStmt with new INSERT including token_type; update getTokenStmt (no change needed — SELECT * picks up new column); add storeOAuthCodeStmt (`INSERT INTO oauth_codes (code, created_at) VALUES (?, ?)`); add consumeOAuthCodeStmt (`DELETE FROM oauth_codes WHERE code = ? RETURNING code`)
    4. Update createToken(token, shopDomain, scopes, tokenType = 'admin') signature and implementation
    5. Update getToken return type to include token_type: string
    6. Add storeOAuthCode(code: string): void
    7. Add consumeOAuthCode(code: string): boolean — SELECT the code row; if not found return false; if `created_at + 60 < Math.floor(Date.now() / 1000)` delete the row and return false (expired); otherwise delete and return true
    8. Add statement nulling in reset() and close() for the two new statements
    9. Update validateAccessToken in token-validator.ts: TokenValidationResult gains `tokenType?: string`; return `{ valid: true, shopDomain: ..., tokenType: tokenRecord.token_type }`
    10. Update admin.ts POST /admin/tokens: destructure tokenType from body, pass to createToken
    11. No test changes in Task 1 — validation tests are added in Task 2 after the endpoint is tightened.

    CRITICAL: Do NOT use `RETURNING` clause if it causes SQLite version issues — use `db.prepare('DELETE FROM oauth_codes WHERE code = ?').run(code).changes > 0` instead (simpler and always works with better-sqlite3).
  </action>
  <verify>
    <automated>pnpm test:sdk --reporter=verbose --run tests/sdk-verification/sdk/shopify-api-auth.test.ts</automated>
  </verify>
  <done>
    - tokens table has token_type column (default 'admin')
    - oauth_codes table exists in schema
    - createToken accepts optional tokenType param
    - consumeOAuthCode deletes and returns true if found and not expired; returns false for expired codes (older than 60s)
    - validateAccessToken returns tokenType in result
    - POST /admin/tokens accepts tokenType in body
    - Existing auth tests still pass (no regressions from schema/token changes)
    - Validation tests are deferred to Task 2 where the endpoint is tightened
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /admin/oauth/authorize + POST /admin/oauth/access_token tightening + callback test update</name>
  <files>
    twins/shopify/src/plugins/oauth.ts,
    tests/sdk-verification/sdk/shopify-api-auth.test.ts
  </files>
  <behavior>
    - GET /admin/oauth/authorize: extract redirect_uri, state, client_id from query; generate code = randomUUID(); store code via storeOAuthCode(); build callback params {code, shop: 'dev.myshopify.com', state, timestamp: Math.floor(Date.now()/1000)}; compute HMAC using SHA-256 Hex over sorted URLSearchParams (excluding hmac/signature); redirect to redirect_uri with all params + hmac appended
    - POST /admin/oauth/access_token: add grant-type branching to preserve existing flows. If body contains `grant_type: 'client_credentials'`, `grant_type: 'urn:ietf:params:oauth:grant-type:token-exchange'`, or `grant_type: 'refresh_token'`, fall through to existing permissive behavior (issue token immediately). Only apply code validation when body contains `code` field (authorization_code flow): validate body has client_id, client_secret, code (all non-empty strings) — return 400 {error: 'invalid_request'} if any missing; validate code via consumeOAuthCode() — return 400 {error: 'invalid_grant'} if code not found; on success: issue token via randomUUID(), createToken(token, 'dev.myshopify.com', scopes, 'admin'), return {access_token, scope}. If body is completely empty or null, return 400 {error: 'invalid_request'}
    - shopify-api-auth.test.ts callback test: replace hardcoded `code: 'test-code-123'` and manual HMAC computation with a real fetch to the twin's GET /admin/oauth/authorize endpoint to obtain the redirect, parse the code and hmac from the redirect URL, use those in the callback request (so the SDK's validateHmac passes naturally)
    - shopify-api-auth.test.ts: add new `describe` block "POST /admin/oauth/access_token — SHOP-18 validation" containing:
      - Test: empty body returns 400 error (fetch raw POST with empty body)
      - Test: missing client_secret returns 400 error (body: {client_id: 'x', code: 'y'})
      - Test: unknown code returns 400 error (body: {client_id: 'x', client_secret: 'y', code: 'unknown-code'})
      - Test: replayed code returns 400 error (obtain a real code from GET /admin/oauth/authorize, consume it via a successful POST, then POST again with same code)
      - Test: client_credentials grant_type still works (body: {grant_type: 'client_credentials', client_id: 'x', client_secret: 'y'} — expect 200 with access_token, proving grant-type branching preserves existing flows)
  </behavior>
  <action>
    In twins/shopify/src/plugins/oauth.ts:

    1. Add `GET /admin/oauth/authorize` route:
       - Extract query params: redirect_uri, state, client_id (all strings)
       - If redirect_uri missing: reply 400 {error: 'invalid_request'}
       - code = randomUUID()
       - fastify.stateManager.storeOAuthCode(code)
       - Build params object: { code, shop: 'dev.myshopify.com', state: state ?? '', timestamp: String(Math.floor(Date.now() / 1000)) }
       - Compute HMAC: exclude hmac/signature, sort with localeCompare, URLSearchParams encode, createHmac('sha256', API_SECRET).update(qs).digest('hex')
       - The API_SECRET must come from the fastify instance or a constant — use 'test-api-secret' as the default (matches SDK test config). Expose via fastify plugin options or declare a module augmentation. Use a module-level constant `const TWIN_API_SECRET = process.env.SHOPIFY_API_SECRET ?? 'test-api-secret'`
       - Redirect 302 to `redirect_uri?code=...&hmac=...&shop=...&state=...&timestamp=...`
       - Build the redirect URL via `new URL(redirect_uri)` and append searchParams, then reply.redirect(302, url.toString())

    2. Tighten `POST /admin/oauth/access_token` with grant-type branching:
       - Body type: `{ client_id?: string; client_secret?: string; code?: string; grant_type?: string; expiring?: string; subject_token?: string; refresh_token?: string }`
       - If !body or body is empty: return reply.status(400).send({error: 'invalid_request', error_description: 'Request body is required'})
       - GRANT-TYPE BRANCHING (preserves existing client_credentials, token-exchange, refresh_token flows):
         - If body.grant_type === 'client_credentials' OR body.grant_type === 'urn:ietf:params:oauth:grant-type:token-exchange' OR body.grant_type === 'refresh_token': fall through to existing permissive behavior (issue token immediately, no code validation)
         - Otherwise (authorization_code flow — body has `code` field):
           - If !body.client_id || !body.client_secret || !body.code: return reply.status(400).send({error: 'invalid_request', error_description: 'client_id, client_secret, and code are required'})
           - const consumed = fastify.stateManager.consumeOAuthCode(body.code)
           - If !consumed: return reply.status(400).send({error: 'invalid_grant', error_description: 'authorization code not found or already used'})
       - Issue token: randomUUID(), createToken(token, 'dev.myshopify.com', scopes, 'admin')
       - Return {access_token: token, scope: scopes}
       IMPORTANT: The grant-type branching is critical — without it, existing smoke tests, storefront client_credentials tests, and token-exchange flows will break.

    In tests/sdk-verification/sdk/shopify-api-auth.test.ts, update the callback test:
    - The test already has the begin() flow that captures the SDK's redirect Location (redirectUrl).
    - After extracting state from the redirect URL, add: fetch the twin's authorize URL using the redirect_uri from the SDK's location. Specifically: extract the full authorize URL from location (it IS the Location header set by auth.begin() pointing to the twin).
    - CRITICAL: The SDK's begin() emits `https://dev.myshopify.com/admin/oauth/authorize?...`. Rewrite the host to the twin base URL before fetching. E.g., `const twinAuthorizeUrl = authorizeUrl.replace('https://dev.myshopify.com', SHOPIFY_TWIN_URL)`
    - Fetch that rewritten URL as a GET with redirect: 'manual', capture the 302 Location header, parse the code from it.
    - Replace the manually-computed callbackParams (code: 'test-code-123' and manual HMAC) with: parse the twin's redirect location, extract all params from it (code, hmac, shop, state, timestamp), use them directly as callbackParams. No need to recompute HMAC — the twin's authorize endpoint set the correct HMAC.
    - The SDK's auth.callback() will then call POST /admin/oauth/access_token with the real code, which the twin will accept and consume.

    Add validation tests (new describe block "POST /admin/oauth/access_token — SHOP-18 validation"):
    - Empty body test: raw POST with empty body → expect 400
    - Missing fields test: body with only client_id and code → expect 400
    - Unknown code test: body with all fields but code='unknown' → expect 400
    - Replayed code test: obtain code from GET authorize, consume via successful POST, then POST again → expect 400
    - Grant-type preservation test: POST with grant_type='client_credentials' + client_id + client_secret → expect 200 (proves existing flows not broken)

    IMPORTANT: The SDK's auth.begin() emits a Location header pointing to `https://{shop}/admin/oauth/authorize?...` (the real Shopify URL format). The `setAbstractFetchFunc` rewrite only applies to SDK HTTP calls, NOT to raw `fetch()` in tests. You MUST rewrite the host in the authorize URL before fetching it: replace the `https://dev.myshopify.com` origin with the twin's base URL (e.g., `http://127.0.0.1:PORT` from SHOPIFY_TWIN_URL or the test config). Create a helper like `rewriteToTwin(url: string): string` that replaces the origin portion while keeping the path and query string intact. Fetch the rewritten URL with `{ redirect: 'manual' }` to get the twin's 302 response without following it.
  </action>
  <verify>
    <automated>pnpm test:sdk --reporter=verbose --run tests/sdk-verification/sdk/shopify-api-auth.test.ts</automated>
  </verify>
  <done>
    - GET /admin/oauth/authorize returns 302 redirect with code, hmac, shop, state, timestamp params
    - POST /admin/oauth/access_token returns 400 on empty body
    - POST /admin/oauth/access_token returns 400 on unknown code
    - POST /admin/oauth/access_token returns 400 on replayed code (second use)
    - POST /admin/oauth/access_token still accepts client_credentials grant_type (no regression)
    - callback test completes OAuth via begin->authorize->callback flow using twin's real authorize endpoint
    - All auth tests pass including new validation tests (no regressions)
    - pnpm test:sdk exits 0 (full suite green — smoke, storefront, auth all pass)
  </done>
</task>

</tasks>

<verification>
pnpm test:sdk --reporter=verbose --run tests/sdk-verification/sdk/shopify-api-auth.test.ts
All auth tests pass including validation tests.
Then run full suite to verify no regressions from grant-type branching:
pnpm test:sdk
Full suite green — smoke, storefront, auth all pass.
</verification>

<success_criteria>
- GET /admin/oauth/authorize redirects with HMAC-signed callback params using SHA-256 Hex
- POST /admin/oauth/access_token validates client_id, client_secret, code; rejects empty body and unknown codes
- oauth_codes table in SQLite, cleared by reset()
- tokens table has token_type column (default 'admin')
- auth.callback() test uses the twin's real authorize flow end-to-end
- pnpm test:sdk --run shopify-api-auth.test.ts exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/23-shopify-oauth-storefront/23-01-SUMMARY.md`
</output>
