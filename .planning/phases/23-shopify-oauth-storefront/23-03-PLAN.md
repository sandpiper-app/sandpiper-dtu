---
phase: 23-shopify-oauth-storefront
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - twins/shopify/src/plugins/oauth.ts
  - tests/sdk-verification/sdk/shopify-api-auth.test.ts
autonomous: true
requirements:
  - SHOP-18
gap_closure: true

must_haves:
  truths:
    - "POST /admin/oauth/access_token rejects wrong client_id/client_secret pairs instead of issuing an access token"
    - "POST /admin/oauth/access_token still returns access_token plus scope for the pinned SDK credentials when the code is valid"
    - "shopify.auth.callback() and shopify.auth.clientCredentials() still succeed with the pinned SDK credentials after credential validation is tightened"
  artifacts:
    - path: "twins/shopify/src/plugins/oauth.ts"
      provides: "exact client_id/client_secret validation for OAuth token exchange"
      contains: "invalid_client"
    - path: "tests/sdk-verification/sdk/shopify-api-auth.test.ts"
      provides: "SHOP-18 coverage for wrong-credential rejection and preserved happy paths"
      contains: "invalid_client"
  key_links:
    - from: "tests/sdk-verification/helpers/shopify-api-client.ts"
      to: "twins/shopify/src/plugins/oauth.ts"
      via: "test-api-key/test-api-secret must match the twin credential check"
      pattern: "test-api-key|test-api-secret"
    - from: "tests/sdk-verification/sdk/shopify-api-auth.test.ts"
      to: "twins/shopify/src/plugins/oauth.ts"
      via: "raw POST /admin/oauth/access_token assertions for invalid_client and preserved success cases"
      pattern: "admin/oauth/access_token"
---

<objective>
Close the remaining SHOP-18 gap by validating the actual OAuth client credential values
on `/admin/oauth/access_token`, not just checking that the fields are non-empty.

Purpose: Phase 23 already has real authorize/callback flow and one-time code handling, but
verification proved that wrong `client_id` and `client_secret` values still return `200`.
This plan tightens that contract without regressing the pinned SDK happy path.

Output:
- exact client_id/client_secret validation in `oauth.ts`
- focused auth coverage for invalid credentials on both auth-code and passthrough grant paths
- preserved callback and `client_credentials` success cases for the pinned SDK config
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-shopify-oauth-storefront/23-RESEARCH.md
@.planning/phases/23-shopify-oauth-storefront/23-VERIFICATION.md
@.planning/phases/23-shopify-oauth-storefront/23-01-SUMMARY.md

@twins/shopify/src/plugins/oauth.ts
@tests/sdk-verification/helpers/shopify-api-client.ts
@tests/sdk-verification/sdk/shopify-api-auth.test.ts

<interfaces>
From `tests/sdk-verification/helpers/shopify-api-client.ts`:
```typescript
return shopifyApi({
  apiKey: 'test-api-key',
  apiSecretKey: 'test-api-secret',
  // ...
});
```

From `twins/shopify/src/plugins/oauth.ts`:
```typescript
const TWIN_API_SECRET = process.env.SHOPIFY_API_SECRET ?? 'test-api-secret';
const PASSTHROUGH_GRANT_TYPES = new Set([
  'client_credentials',
  'urn:ietf:params:oauth:grant-type:token-exchange',
  'refresh_token',
]);
```

Current authorization-code validation already exists and must stay intact:
```typescript
if (!PASSTHROUGH_GRANT_TYPES.has(body.grant_type ?? '')) {
  if (
    !isNonEmptyString(body.client_id) ||
    !isNonEmptyString(body.client_secret) ||
    !isNonEmptyString(body.code)
  ) {
    return reply.status(400).send({ error: 'invalid_request' });
  }

  const consumed = fastify.stateManager.consumeOAuthCode(body.code);
  if (!consumed) {
    return reply.status(400).send({ error: 'invalid_grant' });
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject wrong OAuth client credentials while preserving the working SDK flows</name>
  <files>twins/shopify/src/plugins/oauth.ts, tests/sdk-verification/sdk/shopify-api-auth.test.ts</files>
  <behavior>
    - Authorization-code requests with a real code but wrong `client_id` or wrong `client_secret` return a non-200 error with `{ error: 'invalid_client' }`
    - `client_credentials` requests with wrong credentials also return `{ error: 'invalid_client' }` instead of minting a token
    - The pinned SDK credentials (`test-api-key` / `test-api-secret`) still allow `shopify.auth.callback()` and `shopify.auth.clientCredentials()` to succeed
    - Existing `invalid_request` and `invalid_grant` behavior for empty body, missing fields, unknown codes, and replayed codes remains unchanged
  </behavior>
  <action>
    Update `tests/sdk-verification/sdk/shopify-api-auth.test.ts` first so the gap is executable:
    - Add a case that obtains a real code via `getAuthorizeRedirectFromTwin()` and then posts that code with the correct secret but wrong `client_id`, asserting a non-200 response with `{ error: 'invalid_client' }`
    - Add the matching wrong-`client_secret` case using a real code
    - Add a `client_credentials` case with the correct `client_id` and wrong `client_secret`, asserting `{ error: 'invalid_client' }`
    - Keep the current success case for `client_credentials` with the pinned SDK credentials so the happy path stays pinned

    Then tighten `twins/shopify/src/plugins/oauth.ts`:
    - Introduce `const TWIN_API_KEY = process.env.SHOPIFY_API_KEY ?? 'test-api-key'` beside the existing secret constant
    - Add a helper that compares `client_id` and `client_secret` to the exact configured twin credentials, not just non-empty strings
    - Run this exact-value credential check before the `PASSTHROUGH_GRANT_TYPES` branching so every `/admin/oauth/access_token` request rejects bad credentials, including `client_credentials`, token-exchange, and refresh-token flows
    - On credential mismatch, return `401` with `{ error: 'invalid_client', error_description: 'client_id or client_secret is invalid' }`
    - Keep the existing `invalid_request` response for empty body / missing required auth-code fields and `invalid_grant` for unknown or replayed authorization codes
    - Do not change the issued token shape, scopes, code TTL, or one-time code consumption logic
  </action>
  <verify>
    <automated>pnpm test:sdk --reporter=verbose --run tests/sdk-verification/sdk/shopify-api-auth.test.ts</automated>
  </verify>
  <done>
    Wrong client credentials never mint a token, the auth test suite covers that behavior explicitly, and the pinned SDK callback plus `client_credentials` flows still pass with the correct credentials.
  </done>
</task>

</tasks>

<verification>
Run the focused auth suite first:
`pnpm test:sdk --reporter=verbose --run tests/sdk-verification/sdk/shopify-api-auth.test.ts`

Then confirm there is no broader regression:
`pnpm test:sdk`
</verification>

<success_criteria>
- Wrong `client_id` or `client_secret` values return `invalid_client` from `/admin/oauth/access_token`
- Correct pinned SDK credentials still pass `shopify.auth.callback()` and `shopify.auth.clientCredentials()`
- Existing empty-body, missing-field, unknown-code, and replay protections remain green
- `pnpm test:sdk --reporter=verbose --run tests/sdk-verification/sdk/shopify-api-auth.test.ts` exits `0`
</success_criteria>

<output>
After completion, create `.planning/phases/23-shopify-oauth-storefront/23-03-SUMMARY.md`
</output>
