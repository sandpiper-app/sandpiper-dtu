---
phase: 17-shopify-rest-resource-client-expansion
plan: "04"
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-02"
  - "17-03"
files_modified:
  - tests/sdk-verification/coverage/generate-report.ts
  - tests/sdk-verification/coverage/coverage-report.json
autonomous: true
requirements:
  - SHOP-14
  - SHOP-15

must_haves:
  truths:
    - "coverage-report.json shows GraphqlClient, RestClient, StorefrontClient, graphqlProxy, Shopify.clients, Shopify.rest as 'live' tier attributed to Phase 17 test files"
    - "All 74+ REST resource classes in @shopify/shopify-api/rest/admin/2024-01/ are attributed: Tier 1/2 as 'live', Tier 3 as 'deferred'"
    - "pnpm drift:check passes — no symbol has null tier"
    - "Every LIVE_SYMBOLS key added for Phase 17 resolves to an actual manifest entry in coverage-report.json (validated after generation)"
    - "Phase 16 existing attributions (including stub tiers) are preserved — no regression in live/stub counts"
    - "coverage-report.json phase field updated to '17' closing the @shopify/shopify-api surface"
  artifacts:
    - path: "tests/sdk-verification/coverage/generate-report.ts"
      provides: "Updated LIVE_SYMBOLS map with Phase 17 client surface + REST resource class attributions"
    - path: "tests/sdk-verification/coverage/coverage-report.json"
      provides: "Regenerated report with Phase 17 symbol tiers"
  key_links:
    - from: "tests/sdk-verification/coverage/generate-report.ts LIVE_SYMBOLS"
      to: "tests/sdk-verification/sdk/shopify-api-graphql-client.test.ts, shopify-api-rest-client.test.ts, shopify-api-storefront-client.test.ts"
      via: "pnpm coverage:generate reads manifests and LIVE_SYMBOLS to emit tier attributions"
      pattern: "LIVE_SYMBOLS"
---

<objective>
Update the coverage ledger to attribute all Phase 17 symbols: the four client surfaces (GraphqlClient, RestClient, StorefrontClient, graphqlProxy) and all 74 REST resource classes with their tier assignments. Regenerate coverage-report.json to close the @shopify/shopify-api surface for SHOP-14 and SHOP-15.

Purpose: This is the Phase 17 bookkeeping step — every @shopify/shopify-api symbol gets a declared tier. Tier 1/2 REST resources get 'live' attribution (they have twin routes and test coverage). Tier 3 REST resources get 'deferred' with the REST deprecation note.
Output: generate-report.ts with full Phase 17 LIVE_SYMBOLS, regenerated coverage-report.json with phase '17'.
</objective>

<execution_context>
@/Users/futur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/futur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/sdk-verification/coverage/generate-report.ts
@.planning/phases/17-shopify-rest-resource-client-expansion/17-01-SUMMARY.md
@.planning/phases/17-shopify-rest-resource-client-expansion/17-02-SUMMARY.md
@.planning/phases/17-shopify-rest-resource-client-expansion/17-03-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From tests/sdk-verification/coverage/generate-report.ts (current state):
```typescript
// LIVE_SYMBOLS keys use format: "{packageName}@{version}/{symbolPath}"
// Existing entries cover Phase 15 (@shopify/admin-api-client) and SLCK-06.5 (@slack/web-api)

// Phase 17 additions needed in LIVE_SYMBOLS:

// SHOP-14: client surfaces
'@shopify/shopify-api@12.3.0/GraphqlClient': 'sdk/shopify-api-graphql-client.test.ts',
'@shopify/shopify-api@12.3.0/GraphqlClient.request': 'sdk/shopify-api-graphql-client.test.ts',
'@shopify/shopify-api@12.3.0/GraphqlClient.query': 'sdk/shopify-api-graphql-client.test.ts',
'@shopify/shopify-api@12.3.0/RestClient': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/RestClient.get': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/RestClient.post': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/RestClient.put': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/RestClient.delete': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/ShopifyClients': 'sdk/shopify-api-graphql-client.test.ts',
'@shopify/shopify-api@12.3.0/ShopifyClients.Graphql': 'sdk/shopify-api-graphql-client.test.ts',
'@shopify/shopify-api@12.3.0/ShopifyClients.Rest': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/ShopifyClients.Storefront': 'sdk/shopify-api-storefront-client.test.ts',
'@shopify/shopify-api@12.3.0/ShopifyClients.graphqlProxy': 'sdk/shopify-api-graphql-client.test.ts',
'@shopify/shopify-api@12.3.0/GraphqlProxy': 'sdk/shopify-api-graphql-client.test.ts',
'@shopify/shopify-api@12.3.0/Shopify.clients': 'sdk/shopify-api-graphql-client.test.ts',
'@shopify/shopify-api@12.3.0/Shopify.rest': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/StorefrontClient': 'sdk/shopify-api-storefront-client.test.ts',
'@shopify/shopify-api@12.3.0/StorefrontClient.request': 'sdk/shopify-api-storefront-client.test.ts',

// SHOP-15: Tier 1 REST resources (live — have twin routes and test coverage)
'@shopify/shopify-api@12.3.0/Product': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/Customer': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/Order': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/Fulfillment': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/InventoryItem': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/InventoryLevel': 'sdk/shopify-api-rest-client.test.ts',

// SHOP-15: Tier 2 REST resources (stub routes exist in twin, Metafield tested)
'@shopify/shopify-api@12.3.0/Metafield': 'sdk/shopify-api-rest-client.test.ts',
'@shopify/shopify-api@12.3.0/Webhook': 'sdk/shopify-api-rest-client.test.ts',
// Note: Collection, Page, Blog, Article have stub routes but no dedicated tests;
// these get 'deferred' attribution since they're not in LIVE_SYMBOLS

// Phase 16 additions (if not already in LIVE_SYMBOLS — add them for completeness)
'@shopify/shopify-api@12.3.0/shopifyApi': 'sdk/shopify-api-billing.test.ts',
'@shopify/shopify-api@12.3.0/Shopify.auth': 'sdk/shopify-api-auth.test.ts',
'@shopify/shopify-api@12.3.0/Shopify.session': 'sdk/shopify-api-session.test.ts',
'@shopify/shopify-api@12.3.0/Shopify.webhooks': 'sdk/shopify-api-webhooks.test.ts',
'@shopify/shopify-api@12.3.0/Shopify.billing': 'sdk/shopify-api-billing.test.ts',
```

Coverage report metadata to update:
```typescript
// In generate-report.ts at the bottom (around line 99):
const report = {
  // ...
  phase: '17',   // UPDATE from '15'
  note: 'Phase 17: @shopify/shopify-api GraphqlClient, RestClient, StorefrontClient, graphqlProxy, and strategic REST resource class attributions. SHOP-14 + SHOP-15 complete.',
  // ...
};
```

Run command to regenerate:
```bash
pnpm coverage:generate
# Check output: reports LIVE count, deferred count, and writes coverage-report.json
pnpm drift:check
# Should exit 0: validates all symbols have live or deferred tiers
```

Note: The actual symbol names in the manifest depend on what ts-morph generated. The executor should read the current @shopify/shopify-api manifest from tools/sdk-surface/manifests/ to confirm exact symbol names before updating LIVE_SYMBOLS. Especially check whether REST resource class names are present (Product, Customer, etc.) vs REST resource subpaths.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update LIVE_SYMBOLS map + regenerate coverage report</name>
  <files>tests/sdk-verification/coverage/generate-report.ts, tests/sdk-verification/coverage/coverage-report.json</files>
  <action>
    **Step 1 — Check the @shopify/shopify-api manifest for exact symbol names:**
    ```bash
    cat tools/sdk-surface/manifests/shopify-api.json | python3 -m json.tool | grep -E '"(Graphql|Rest|Storefront|graphqlProxy|Product|Customer|Order|Fulfillment|Inventory|Metafield|Webhook|Shopify\.|ShopifyClients)' | head -60
    ```
    Use these exact names in LIVE_SYMBOLS. If symbol names differ from the interface block above, use the manifest names.
    **CRITICAL: Only add LIVE_SYMBOLS entries for keys that actually exist in the manifest. Unknown keys are silently ignored by the generator, creating false attribution.**

    **Step 2 — Snapshot current coverage state before modifications:**
    Read the current coverage-report.json to identify all existing attributions (especially Phase 16 entries with `stub` tier like `Shopify.billing`, and existing `live` entries like `Shopify.flow`, `Shopify.fulfillmentService`). Preserve ALL existing attributions when adding Phase 17 entries. Do NOT remove or overwrite Phase 16 entries.
    ```bash
    cat tests/sdk-verification/coverage/coverage-report.json | python3 -m json.tool | grep -E '"tier"|"phase"' | sort | uniq -c | sort -rn | head -20
    ```

    **Step 3 — Check what Phase 16 already added to LIVE_SYMBOLS:**
    Read current generate-report.ts LIVE_SYMBOLS. It currently has Phase 14-15 entries. Check whether Phase 16 entries (Shopify.auth, Shopify.session, etc.) were already added — if not, add them now. If the generator uses only `live|deferred` tiers but the report has `stub` tier entries, ensure the generator preserves existing `stub` attributions (do not regress them to `deferred`).

    **Step 4 — Add Phase 17 LIVE_SYMBOLS entries to generate-report.ts:**
    Add the client surface entries and Tier 1/2 REST resource entries from the interface block above.
    If a symbol name from the interface block does not appear in the manifest, skip it — only attribute symbols that exist in the manifest.

    **Step 5 — Update phase and note metadata** in the report object at the bottom.

    **Step 6 — Regenerate and validate:**
    ```bash
    cd /Users/futur/projects/sandpiper-dtu
    pnpm coverage:generate
    ```

    **Step 7 — Verify LIVE_SYMBOLS keys resolve to manifest:**
    After regeneration, verify that every LIVE_SYMBOLS key appears in the output report:
    ```bash
    # Extract LIVE_SYMBOLS keys and check each exists in coverage-report.json
    node -e "
      const report = require('./tests/sdk-verification/coverage/coverage-report.json');
      const symbols = report.symbols || report.entries || [];
      const symNames = new Set(Array.isArray(symbols) ? symbols.map(s => s.name || s.symbol) : Object.keys(symbols));
      const liveKeys = Object.keys(require('./tests/sdk-verification/coverage/generate-report.ts').LIVE_SYMBOLS || {});
      const missing = liveKeys.filter(k => !symNames.has(k));
      if (missing.length) { console.error('MISSING from manifest:', missing); process.exit(1); }
      else { console.log('All LIVE_SYMBOLS keys resolve to manifest entries'); }
    "
    ```
    If this validation approach doesn't work with the actual report structure, adapt to the actual JSON shape — the goal is to confirm every LIVE_SYMBOLS key lands in the final report.

    **Step 8 — Verify no Phase 16 regression:**
    ```bash
    # Compare live+stub counts before and after
    # The live count should increase, stub count should stay the same
    cat tests/sdk-verification/coverage/coverage-report.json | python3 -c "
    import json, sys
    data = json.load(sys.stdin)
    tiers = {}
    for entry in (data.get('symbols') or data.get('entries') or []):
      t = entry.get('tier', 'unknown')
      tiers[t] = tiers.get(t, 0) + 1
    for t, c in sorted(tiers.items()):
      print(f'{t}: {c}')
    "
    ```

    **Step 9 — Verify drift check:**
    ```bash
    pnpm drift:check
    ```
    Should exit 0. If it fails due to coverage-report.json location or git add -f requirement (coverage/ is gitignored), force-add:
    ```bash
    git add -f tests/sdk-verification/coverage/coverage-report.json
    ```
    This is the same pattern used in Phase 16 (Plan 16-04 key decision: "force-add gitignored coverage directory").
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm coverage:generate && pnpm drift:check</automated>
  </verify>
  <done>generate-report.ts has Phase 17 LIVE_SYMBOLS entries, pnpm coverage:generate writes coverage-report.json with phase '17', pnpm drift:check exits 0 (no null tiers)</done>
</task>

</tasks>

<verification>
pnpm coverage:generate succeeds and writes coverage-report.json.
pnpm drift:check exits 0.
coverage-report.json summary.live count is higher than Phase 16 (reflects new Phase 17 attributions).
pnpm test:sdk still passes — all Phase 16 + Phase 17 tests green.
</verification>

<success_criteria>
- generate-report.ts LIVE_SYMBOLS includes Phase 17 client surface and Tier 1/2 REST resource entries
- coverage-report.json regenerated with phase '17', all @shopify/shopify-api symbols attributed as live or deferred
- pnpm drift:check exits 0
- SHOP-14 and SHOP-15 fully satisfied: client surfaces live, REST resources tiered
- Full test suite (pnpm test:sdk) still green
</success_criteria>

<output>
After completion, create .planning/phases/17-shopify-rest-resource-client-expansion/17-04-SUMMARY.md
</output>
