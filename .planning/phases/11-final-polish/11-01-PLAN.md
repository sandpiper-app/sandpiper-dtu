---
phase: 11-final-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - twins/shopify/src/plugins/errors.ts
  - twins/shopify/tsconfig.conformance.json
  - twins/slack/tsconfig.json
  - .planning/phases/01-foundation-monorepo-setup/01-01-SUMMARY.md
  - .planning/phases/01-foundation-monorepo-setup/01-02-SUMMARY.md
  - .planning/phases/02-shopify-twin-core-operations/02-01-SUMMARY.md
  - .planning/phases/02-shopify-twin-core-operations/02-03-SUMMARY.md
  - .planning/phases/02-shopify-twin-core-operations/02-04-SUMMARY.md
  - .planning/phases/02-shopify-twin-core-operations/02-05-SUMMARY.md
  - .planning/phases/03-webhook-system-conformance-framework/03-01-SUMMARY.md
  - .planning/phases/03-webhook-system-conformance-framework/03-02-SUMMARY.md
  - .planning/phases/03-webhook-system-conformance-framework/03-03-SUMMARY.md
  - .planning/phases/04-shopify-twin-advanced-features/04-01-SUMMARY.md
  - .planning/phases/04-shopify-twin-advanced-features/04-02-SUMMARY.md
  - .planning/phases/04-shopify-twin-advanced-features/04-03-SUMMARY.md
  - .planning/phases/06-twin-uis/06-01-SUMMARY.md
  - .planning/phases/06-twin-uis/06-02-SUMMARY.md
  - .planning/phases/06-twin-uis/06-03-SUMMARY.md
  - .planning/phases/06-twin-uis/06-04-SUMMARY.md
  - .planning/phases/06-twin-uis/06-05-SUMMARY.md
  - .planning/phases/06-twin-uis/06-06-SUMMARY.md
  - .planning/phases/07-integration-e2e-testing/07-01-SUMMARY.md
  - .planning/phases/07-integration-e2e-testing/07-02-SUMMARY.md
  - .planning/phases/09-code-quality-cleanup/09-01-SUMMARY.md
  - .planning/phases/10-tech-debt-cleanup/10-01-SUMMARY.md
  - .planning/phases/10-tech-debt-cleanup/10-02-SUMMARY.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "GET /admin/errors on Shopify twin returns list of all error configs"
    - "GET /admin/errors/:operation on Shopify twin returns config for a specific operation (or null)"
    - "twins/shopify/tsconfig.conformance.json has no reference to deleted @dtu/core package"
    - "twins/slack/tsconfig.json includes @dtu/conformance project reference"
    - "All 28 SUMMARY.md files have requirements_completed field in frontmatter"
  artifacts:
    - path: "twins/shopify/src/plugins/errors.ts"
      provides: "GET /admin/errors and GET /admin/errors/:operation endpoints"
      contains: "fastify.get.*admin/errors"
    - path: "twins/shopify/tsconfig.conformance.json"
      provides: "Clean TypeScript conformance config without stale @dtu/core reference"
    - path: "twins/slack/tsconfig.json"
      provides: "Complete project references including @dtu/conformance"
      contains: "packages/conformance"
  key_links:
    - from: "twins/shopify/src/plugins/errors.ts"
      to: "packages/state/src/state-manager.ts"
      via: "fastify.stateManager.database accessor and getErrorConfig() method"
      pattern: "stateManager\\.database|stateManager\\.getErrorConfig"
---

<objective>
Close all remaining integration and documentation tech debt from v1.0 audit: add Shopify error inspection endpoints, fix stale/missing tsconfig references, and backfill requirements_completed frontmatter in all SUMMARY.md files.

Purpose: Achieve full parity between Shopify and Slack admin APIs, clean TypeScript build graph, and complete documentation traceability.
Output: Updated errors plugin, two fixed tsconfig files, 23 updated SUMMARY.md files.
</objective>

<execution_context>
@/Users/futur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/futur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-final-polish/11-RESEARCH.md

Source files for Task 1:
@twins/shopify/src/plugins/errors.ts
@twins/slack/src/plugins/errors.ts

Source files for Task 2:
@twins/shopify/tsconfig.conformance.json
@twins/slack/tsconfig.json
@twins/shopify/tsconfig.json (reference for correct Slack references)

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/state/src/state-manager.ts:
```typescript
// Line 174 — public database accessor
get database(): Database.Database { ... }

// Line ~313 — error_configs table columns
// id, operation_name, status_code, error_body, delay_ms, enabled

// Existing method for single-operation lookup:
getErrorConfig(operationName: string): ErrorConfig | undefined;
```

From twins/shopify/src/plugins/errors.ts (current, to be extended):
```typescript
import type { FastifyPluginAsync } from 'fastify';
import type { ErrorSimulator } from '../services/error-simulator.js';

declare module 'fastify' {
  interface FastifyInstance {
    errorSimulator: ErrorSimulator;
    webhookSecret: string;
  }
}

export const errorsPlugin: FastifyPluginAsync = async (fastify) => {
  // POST /admin/errors/configure — EXISTS
  // POST /admin/errors/enable — EXISTS
  // POST /admin/errors/disable — EXISTS
  // GET /admin/errors — MISSING (add)
  // GET /admin/errors/:operation — MISSING (add)
};
```

From twins/slack/src/plugins/errors.ts (reference for GET /admin/errors pattern):
```typescript
fastify.get('/admin/errors', async () => {
  const rows = fastify.slackStateManager.database
    .prepare('SELECT * FROM slack_error_configs')
    .all();
  return { configs: rows };
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /admin/errors and GET /admin/errors/:operation to Shopify twin</name>
  <files>twins/shopify/src/plugins/errors.ts</files>
  <action>
Add two GET endpoints to the existing `errorsPlugin` in `twins/shopify/src/plugins/errors.ts`, after the existing POST routes:

1. **GET /admin/errors** — List all configured error configs.
   Use `fastify.stateManager.database.prepare('SELECT * FROM error_configs').all()` to query all rows.
   Return `{ configs: rows }`.

2. **GET /admin/errors/:operation** — Get config for a specific operation.
   Use `fastify.stateManager.getErrorConfig(request.params.operation)` for the lookup.
   Return `{ config }` if found, `{ config: null }` if not.

Pattern source: `twins/slack/src/plugins/errors.ts` lines 37-42 (GET /admin/errors).

The Shopify StateManager exposes a public `.database` getter (line 174 of `packages/state/src/state-manager.ts`). The `error_configs` table has columns: `id`, `operation_name`, `status_code`, `error_body`, `delay_ms`, `enabled`.

Do NOT modify any existing POST routes. Only ADD the two new GET routes after the existing `POST /admin/errors/disable` handler.
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && npx tsx -e "
const { buildApp } = await import('./twins/shopify/src/index.js');
const app = await buildApp({ logger: false });
await app.ready();

// Test GET /admin/errors (empty list)
let res = await app.inject({ method: 'GET', url: '/admin/errors' });
console.assert(res.statusCode === 200, 'GET /admin/errors should return 200');
const body1 = JSON.parse(res.body);
console.assert(Array.isArray(body1.configs), 'Should return configs array');

// Configure an error, then list
await app.inject({ method: 'POST', url: '/admin/errors/configure', payload: { operationName: 'testOp', statusCode: 500 } });
res = await app.inject({ method: 'GET', url: '/admin/errors' });
const body2 = JSON.parse(res.body);
console.assert(body2.configs.length >= 1, 'Should have at least 1 config');

// Test GET /admin/errors/:operation
res = await app.inject({ method: 'GET', url: '/admin/errors/testOp' });
const body3 = JSON.parse(res.body);
console.assert(body3.config !== null, 'Should find testOp config');
console.assert(body3.config.operation_name === 'testOp', 'Should match operation name');

// Test GET /admin/errors/:operation (not found)
res = await app.inject({ method: 'GET', url: '/admin/errors/nonexistent' });
const body4 = JSON.parse(res.body);
console.assert(body4.config === null, 'Should return null for nonexistent');

await app.close();
console.log('All Shopify error endpoint tests passed');
"</automated>
  </verify>
  <done>GET /admin/errors returns { configs: [...] } with all error configs. GET /admin/errors/:operation returns { config: {...} } or { config: null }. Both endpoints accessible without authentication (admin API).</done>
</task>

<task type="auto">
  <name>Task 2: Fix tsconfig references — remove stale @dtu/core, add missing @dtu/conformance</name>
  <files>twins/shopify/tsconfig.conformance.json, twins/slack/tsconfig.json</files>
  <action>
Two single-line tsconfig fixes:

1. **twins/shopify/tsconfig.conformance.json** — Remove the stale `@dtu/core` reference (line 13).

   Current references array:
   ```json
   "references": [
     { "path": "../../packages/types" },
     { "path": "../../packages/state" },
     { "path": "../../packages/core" },
     { "path": "../../packages/webhooks" },
     { "path": "../../packages/conformance" },
     { "path": "." }
   ]
   ```

   After fix (remove the `@dtu/core` line):
   ```json
   "references": [
     { "path": "../../packages/types" },
     { "path": "../../packages/state" },
     { "path": "../../packages/webhooks" },
     { "path": "../../packages/conformance" },
     { "path": "." }
   ]
   ```

2. **twins/slack/tsconfig.json** — Add `{ "path": "../../packages/conformance" }` to references, positioned after `webhooks` and before `ui` (matching Shopify's `twins/shopify/tsconfig.json` ordering).

   Current references array:
   ```json
   "references": [
     { "path": "../../packages/types" },
     { "path": "../../packages/state" },
     { "path": "../../packages/webhooks" },
     { "path": "../../packages/ui" }
   ]
   ```

   After fix:
   ```json
   "references": [
     { "path": "../../packages/types" },
     { "path": "../../packages/state" },
     { "path": "../../packages/webhooks" },
     { "path": "../../packages/conformance" },
     { "path": "../../packages/ui" }
   ]
   ```

Context: `packages/core/` was deleted in Phase 8. The main `twins/shopify/tsconfig.json` was updated then but `tsconfig.conformance.json` was missed. The Slack twin imports from `@dtu/conformance` (in `twins/slack/conformance/`) but its main tsconfig.json doesn't list the project reference.
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && node -e "
const fs = require('fs');

// Verify tsconfig.conformance.json has no @dtu/core reference
const confTsconfig = JSON.parse(fs.readFileSync('twins/shopify/tsconfig.conformance.json', 'utf8'));
const hasCore = confTsconfig.references.some(r => r.path.includes('core'));
console.assert(!hasCore, 'tsconfig.conformance.json should not reference @dtu/core');
console.log('tsconfig.conformance.json: @dtu/core removed:', !hasCore);

// Verify slack tsconfig.json has @dtu/conformance reference
const slackTsconfig = JSON.parse(fs.readFileSync('twins/slack/tsconfig.json', 'utf8'));
const hasConformance = slackTsconfig.references.some(r => r.path.includes('conformance'));
console.assert(hasConformance, 'slack tsconfig.json should reference @dtu/conformance');
console.log('slack tsconfig.json: @dtu/conformance present:', hasConformance);

// Verify ordering: conformance before ui
const confIdx = slackTsconfig.references.findIndex(r => r.path.includes('conformance'));
const uiIdx = slackTsconfig.references.findIndex(r => r.path.includes('ui'));
console.assert(confIdx < uiIdx, 'conformance should come before ui');
console.log('slack tsconfig.json: conformance before ui:', confIdx < uiIdx);

console.log('All tsconfig checks passed');
"</automated>
  </verify>
  <done>twins/shopify/tsconfig.conformance.json has no @dtu/core reference. twins/slack/tsconfig.json includes @dtu/conformance project reference positioned after webhooks and before ui.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill requirements_completed frontmatter in all SUMMARY.md files</name>
  <files>
    .planning/phases/01-foundation-monorepo-setup/01-01-SUMMARY.md,
    .planning/phases/01-foundation-monorepo-setup/01-02-SUMMARY.md,
    .planning/phases/02-shopify-twin-core-operations/02-01-SUMMARY.md,
    .planning/phases/02-shopify-twin-core-operations/02-03-SUMMARY.md,
    .planning/phases/02-shopify-twin-core-operations/02-04-SUMMARY.md,
    .planning/phases/02-shopify-twin-core-operations/02-05-SUMMARY.md,
    .planning/phases/03-webhook-system-conformance-framework/03-01-SUMMARY.md,
    .planning/phases/03-webhook-system-conformance-framework/03-02-SUMMARY.md,
    .planning/phases/03-webhook-system-conformance-framework/03-03-SUMMARY.md,
    .planning/phases/04-shopify-twin-advanced-features/04-01-SUMMARY.md,
    .planning/phases/04-shopify-twin-advanced-features/04-02-SUMMARY.md,
    .planning/phases/04-shopify-twin-advanced-features/04-03-SUMMARY.md,
    .planning/phases/06-twin-uis/06-01-SUMMARY.md,
    .planning/phases/06-twin-uis/06-02-SUMMARY.md,
    .planning/phases/06-twin-uis/06-03-SUMMARY.md,
    .planning/phases/06-twin-uis/06-04-SUMMARY.md,
    .planning/phases/06-twin-uis/06-05-SUMMARY.md,
    .planning/phases/06-twin-uis/06-06-SUMMARY.md,
    .planning/phases/07-integration-e2e-testing/07-01-SUMMARY.md,
    .planning/phases/07-integration-e2e-testing/07-02-SUMMARY.md,
    .planning/phases/09-code-quality-cleanup/09-01-SUMMARY.md,
    .planning/phases/10-tech-debt-cleanup/10-01-SUMMARY.md,
    .planning/phases/10-tech-debt-cleanup/10-02-SUMMARY.md
  </files>
  <action>
Add `requirements_completed` field to YAML frontmatter in 23 SUMMARY.md files that are missing it. The 5 files that already have the field (02-02, 05-01, 05-02, 05-03, 08-01) must NOT be modified.

**Requirement-to-plan mapping** (derived from each PLAN.md's `requirements:` field):

| File | requirements_completed value |
|------|------------------------------|
| 01-01-SUMMARY.md | `[INFRA-01, INFRA-09]` |
| 01-02-SUMMARY.md | `[INFRA-02, INFRA-07, INFRA-08]` |
| 02-01-SUMMARY.md | `[SHOP-02, SHOP-07, INFRA-03]` |
| 02-03-SUMMARY.md | `[SHOP-03, INFRA-04]` |
| 02-04-SUMMARY.md | `[]` |
| 02-05-SUMMARY.md | `[]` |
| 03-01-SUMMARY.md | `[INFRA-05]` |
| 03-02-SUMMARY.md | `[INFRA-05]` |
| 03-03-SUMMARY.md | `[INFRA-06]` |
| 04-01-SUMMARY.md | `[SHOP-04]` |
| 04-02-SUMMARY.md | `[SHOP-05]` |
| 04-03-SUMMARY.md | `[SHOP-06]` |
| 06-01-SUMMARY.md | `[UI-05]` |
| 06-02-SUMMARY.md | `[UI-01, UI-02]` |
| 06-03-SUMMARY.md | `[UI-03, UI-04]` |
| 06-04-SUMMARY.md | `[]` |
| 06-05-SUMMARY.md | `[]` |
| 06-06-SUMMARY.md | `[]` |
| 07-01-SUMMARY.md | `[INTG-01, INTG-03]` |
| 07-02-SUMMARY.md | `[INTG-02]` |
| 09-01-SUMMARY.md | `[]` |
| 10-01-SUMMARY.md | `[SHOP-01]` |
| 10-02-SUMMARY.md | `[INFRA-06]` |

**Insertion rules:**

1. **Files WITH `---` frontmatter block** (most files): Insert `requirements_completed: [...]` as a new line within the existing `---` delimited block, after the last metadata field before the closing `---`.

2. **Files WITHOUT `---` frontmatter block** (e.g., 02-03-SUMMARY.md starts with `##` heading): Add a minimal frontmatter block at the very top of the file:
   ```
   ---
   requirements_completed: [SHOP-03, INFRA-04]
   ---

   ## Plan 02-03: ...
   ```

3. Use flow sequence format `[REQ-ID, REQ-ID]` consistently (matching existing 05-01, 05-02, 05-03, 08-01 patterns).

4. Do NOT modify any existing frontmatter fields or markdown body content.

5. Do NOT touch the 5 files that already have requirements_completed (02-02, 05-01, 05-02, 05-03, 08-01).
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && node -e "
const fs = require('fs');
const path = require('path');
const glob = require('child_process').execSync('find .planning/phases -name \"*-SUMMARY.md\" | sort', { encoding: 'utf8' }).trim().split('\n');

let total = 0;
let withField = 0;
const missing = [];

for (const file of glob) {
  total++;
  const content = fs.readFileSync(file, 'utf8');
  if (content.includes('requirements_completed')) {
    withField++;
  } else {
    missing.push(file);
  }
}

console.log('Total SUMMARY files:', total);
console.log('With requirements_completed:', withField);
console.log('Missing:', missing.length);
if (missing.length > 0) {
  console.log('Missing files:', missing.join(', '));
}
console.assert(total === 28, 'Should have 28 SUMMARY files');
console.assert(withField === 28, 'All 28 should have requirements_completed');
console.assert(missing.length === 0, 'None should be missing');
console.log(withField === 28 ? 'All SUMMARY files have requirements_completed' : 'FAIL: some files missing field');
"</automated>
  </verify>
  <done>All 28 SUMMARY.md files contain requirements_completed field in frontmatter. The 5 previously-existing fields are unchanged. The 23 newly-added fields match the requirement-to-plan mapping from PLAN.md files.</done>
</task>

</tasks>

<verification>
1. `GET /admin/errors` on Shopify twin returns `{ configs: [...] }` — verified via app.inject()
2. `GET /admin/errors/:operation` on Shopify twin returns `{ config: {...} }` or `{ config: null }` — verified via app.inject()
3. `twins/shopify/tsconfig.conformance.json` has no `@dtu/core` reference — verified by JSON parse
4. `twins/slack/tsconfig.json` includes `@dtu/conformance` reference — verified by JSON parse
5. All 28 SUMMARY.md files have `requirements_completed` in frontmatter — verified by find + grep
6. `pnpm build` passes (no tsconfig breakage) — full build verification
</verification>

<success_criteria>
- Shopify twin GET /admin/errors returns all configured error configs as JSON array
- Shopify twin GET /admin/errors/:operation returns specific config or null
- tsconfig.conformance.json references array does not mention packages/core
- slack tsconfig.json references array includes packages/conformance before packages/ui
- Every SUMMARY.md file (28 total) has requirements_completed field in YAML frontmatter
- pnpm build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-final-polish/11-01-SUMMARY.md`
</output>
