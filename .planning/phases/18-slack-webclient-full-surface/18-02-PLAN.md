---
phase: 18-slack-webclient-full-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - twins/slack/src/plugins/web-api/conversations.ts
  - twins/slack/src/plugins/web-api/users.ts
  - tests/sdk-verification/sdk/slack-conversations.test.ts
  - tests/sdk-verification/sdk/slack-users.test.ts
autonomous: true
requirements:
  - SLCK-08

must_haves:
  truths:
    - "All 28 conversations family methods return ok:true or a structured valid response from the twin"
    - "Core conversation state mutations (create, join, leave, archive, rename, invite, members, replies) work correctly"
    - "All 12 users family methods return ok:true or a structured valid response from the twin"
    - "users.lookupByEmail returns a user matching seeded email; users.profile.get returns a profile object"
  artifacts:
    - path: "twins/slack/src/plugins/web-api/conversations.ts"
      provides: "28 conversations methods: existing list/info/history + 25 new methods"
      contains: "conversations.create"
    - path: "twins/slack/src/plugins/web-api/users.ts"
      provides: "12 users methods: existing list/info + 10 new methods"
      contains: "users.lookupByEmail"
    - path: "tests/sdk-verification/sdk/slack-conversations.test.ts"
      provides: "Conversations family tests covering all 28 methods"
      exports: ["describe"]
    - path: "tests/sdk-verification/sdk/slack-users.test.ts"
      provides: "Users family tests covering all 12 methods"
      exports: ["describe"]
  key_links:
    - from: "tests/sdk-verification/sdk/slack-conversations.test.ts"
      to: "twins/slack/src/plugins/web-api/conversations.ts"
      via: "conversations.create creating a channel then conversations.info retrieving it"
      pattern: "conversations.create"
    - from: "tests/sdk-verification/sdk/slack-users.test.ts"
      to: "twins/slack/src/plugins/web-api/users.ts"
      via: "users.lookupByEmail using seeded user email from SlackStateManager"
      pattern: "users.lookupByEmail"
---

<objective>
Expand the Slack twin's conversations and users plugins from their current 3-method and 2-method baselines to full Tier 1 coverage. Conversations grows from list/info/history to 28 methods including stateful mutations (create, join, leave, archive, rename, invite, kick, open, close, mark, setPurpose, setTopic, members, replies) and stub-shaped Slack Connect methods. Users grows from list/info to 12 methods including lookupByEmail, getPresence, conversations, and profile get/set. Two test files exercise all new methods.

Purpose: SLCK-08 requires every bound WebClient method to have a declared coverage entry. Conversations (28 methods) and Users (12 methods) represent 40 of the ~60 Tier 1 methods. This plan runs parallel to Plan 01 since it shares no files with files.ts, chat.ts, or rate-limiter.ts.

Output: conversations.ts expanded to 28 methods, users.ts expanded to 12 methods, two test files green (no index.ts changes needed — conversations and users are already registered).
</objective>

<execution_context>
@/Users/futur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/futur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-slack-webclient-full-surface/18-RESEARCH.md

@twins/slack/src/plugins/web-api/conversations.ts
@twins/slack/src/plugins/web-api/users.ts
@twins/slack/src/state/slack-state-manager.ts
@tests/sdk-verification/sdk/slack-auth-gateway.test.ts
@tests/sdk-verification/helpers/slack-client.ts
@tests/sdk-verification/setup/seeders.ts

<interfaces>
<!-- Existing state manager methods available for use in plugin expansion. -->

From twins/slack/src/state/slack-state-manager.ts (confirmed public methods):
```typescript
// Channel CRUD
createChannel(data: { id?: string; name: string; is_private?: boolean }): any
getChannel(id: string): any
listChannels(): any[]
updateChannel(id: string, updates: Partial<...>): void

// User CRUD
createUser(data: { id?: string; name: string; email?: string; ... }): any
getUser(id: string): any
listUsers(): any[]

// Message CRUD
createMessage(data: { channel_id: string; user_id: string; text?: string; ts: string; thread_ts?: string }): void
getMessage(ts: string): any
listMessages(channelId: string, opts?: { limit?: number; cursor?: string; oldest?: string; latest?: string }): any[]
```

For users.lookupByEmail — SlackStateManager.listUsers() returns all users; executor must filter by email field in user rows.

For conversations.members — returns { members: string[], response_metadata: { next_cursor: '' } }. Channel members can be tracked in a simple in-memory Set OR by seeding member IDs from state. Simplest approach: return [userId] (the token's owner) as the single member list from state (no members table needed for conformance). channels table has a num_members column.

For conversations.replies — requires thread_ts; return listMessages filtered by thread_ts.

For Slack Connect methods (acceptSharedInvite, approveSharedInvite, declineSharedInvite, inviteShared, listConnectInvites, requestSharedInvite) — return { ok: true } stub shape with no state. Full auth check still required.

getParams helper already in conversations.ts for GET/POST dual support.

From conversations plugin formatChannel():
```typescript
function formatChannel(ch: any): any {
  return { id, name, is_channel, is_private, is_archived, topic, purpose, num_members, created }
}
```

generateChannelId from id-generator.ts:
```typescript
import { generateChannelId } from '../../services/id-generator.js';
// generates C_... prefixed ID
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand conversations.ts to 28 methods</name>
  <files>
    twins/slack/src/plugins/web-api/conversations.ts,
    tests/sdk-verification/sdk/slack-conversations.test.ts
  </files>
  <behavior>
    - Test: conversations.create({ name: 'new-channel' }) returns { ok: true, channel: { id, name } }
    - Test: conversations.join({ channel: 'C_GENERAL' }) returns { ok: true, channel: { id } }
    - Test: conversations.leave({ channel: 'C_GENERAL' }) returns { ok: true }
    - Test: conversations.archive({ channel: createdChannel }) returns { ok: true }
    - Test: conversations.unarchive({ channel: archivedChannel }) returns { ok: true }
    - Test: conversations.rename({ channel, name: 'new-name' }) returns { ok: true, channel: { name: 'new-name' } }
    - Test: conversations.invite({ channel, users: 'U_BOT_TWIN' }) returns { ok: true, channel: { id } }
    - Test: conversations.kick({ channel, user: 'U_BOT_TWIN' }) returns { ok: true }
    - Test: conversations.open({ users: 'U_BOT_TWIN' }) returns { ok: true, channel: { id } }
    - Test: conversations.close({ channel }) returns { ok: true }
    - Test: conversations.mark({ channel, ts: '...' }) returns { ok: true }
    - Test: conversations.setPurpose({ channel, purpose: 'test purpose' }) returns { ok: true, purpose: 'test purpose' }
    - Test: conversations.setTopic({ channel, topic: 'test topic' }) returns { ok: true, topic: 'test topic' }
    - Test: conversations.members({ channel: 'C_GENERAL' }) returns { ok: true, members: array, response_metadata: { next_cursor: '' } }
    - Test: conversations.replies({ channel, ts: msgTs }) returns { ok: true, messages: array, has_more: false }
    - Test: (smoke) conversations.acceptSharedInvite({}) returns { ok: true }
    - Test: existing list/info/history still pass
  </behavior>
  <action>
Add 25 new route handlers to the existing `conversationsPlugin` in `conversations.ts`. Keep all existing handlers intact. Import `generateChannelId` from `../../services/id-generator.js`.

Handlers to add (all POST unless noted GET+POST for read methods):

**State-mutating handlers:**

`POST /api/conversations.create` — body: `{ name, is_private? }`. Create channel via `fastify.slackStateManager.createChannel({ name, is_private: !!is_private })`. The state manager's createChannel should assign an id; if the `createChannel` method doesn't accept `is_private`, pass it in the data object. Return `{ ok: true, channel: formatChannel(newChannel) }`.

`POST /api/conversations.join` — body: `{ channel }`. Check channel exists. Return `{ ok: true, channel: formatChannel(ch) }`.

`POST /api/conversations.leave` — body: `{ channel }`. Return `{ ok: true }`.

`POST /api/conversations.archive` — body: `{ channel }`. Update channel `is_archived: 1`. Return `{ ok: true }`.

`POST /api/conversations.unarchive` — body: `{ channel }`. Update channel `is_archived: 0`. Return `{ ok: true }`.

`POST /api/conversations.rename` — body: `{ channel, name }`. Update channel `name`. Return `{ ok: true, channel: { id: channel, name } }`.

`POST /api/conversations.invite` — body: `{ channel, users }`. Channel must exist. Return `{ ok: true, channel: formatChannel(ch) }`.

`POST /api/conversations.kick` — body: `{ channel, user }`. Channel must exist. Return `{ ok: true }`.

`POST /api/conversations.open` — body: `{ users?, channel? }`. If channel provided, return it. If users provided, create or return an IM channel: return `{ ok: true, already_open: false, channel: { id: 'D_TWIN', is_im: true } }`.

`POST /api/conversations.close` — body: `{ channel }`. Return `{ ok: true, no_op: false, already_closed: false }`.

`POST /api/conversations.mark` — body: `{ channel, ts }`. Return `{ ok: true }`.

`POST /api/conversations.setPurpose` — body: `{ channel, purpose }`. Update channel purpose via `fastify.slackStateManager.updateChannel(channel, { purpose })`. Return `{ ok: true, purpose }`.

`POST /api/conversations.setTopic` — body: `{ channel, topic }`. Update channel topic via `fastify.slackStateManager.updateChannel(channel, { topic })`. Return `{ ok: true, topic }`.

**Read handlers (GET+POST):**

`GET+POST /api/conversations.members` — params: `{ channel }`. If channel missing or not found: `{ ok: false, error: 'channel_not_found' }`. Return `{ ok: true, members: ['U_BOT_TWIN'], response_metadata: { next_cursor: '' } }`.

`GET+POST /api/conversations.replies` — params: `{ channel, ts }`. If missing: error. Query `listMessages` with the channel_id filter and return messages where `thread_ts === ts` OR `ts === ts`. Return `{ ok: true, messages: threadMessages, has_more: false, response_metadata: { next_cursor: '' } }`.

**Stub-shaped Slack Connect handlers (POST only, return { ok: true }):**

```
/api/conversations.acceptSharedInvite
/api/conversations.approveSharedInvite
/api/conversations.declineSharedInvite
/api/conversations.inviteShared
/api/conversations.listConnectInvites
/api/conversations.requestSharedInvite
/api/conversations.externalInvitePermissions.set
/api/conversations.canvases.create
/api/conversations.canvases.delete
/api/conversations.canvases.sections.lookup
```

All stub handlers still require the auth check (extractToken + getToken). Return `{ ok: true }` after auth passes.

---

**slack-conversations.test.ts** — Create test file covering all the above methods. Follow the exact pattern from `slack-auth-gateway.test.ts`: `describe`, `beforeEach` with `resetSlack()` + `seedSlackBotToken()`, individual `it` tests.

Use `channelId = 'C_GENERAL'` as the target for most operations (it's seeded on reset). For operations that need a new channel (archive, rename), call `client.conversations.create({ name: 'test-' + Date.now() })` first.

Key test coverage:
- Core CRUD: create, join, leave, archive, unarchive, rename, invite, kick, open, close, mark, setPurpose, setTopic, members, replies — each gets its own `it` block
- Slack Connect stubs: one `it` block for acceptSharedInvite (smoke test that it returns ok:true)
- Existing methods: one `it` block each for list, info, history to confirm they still work
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- slack-conversations 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `pnpm test:sdk -- slack-conversations` passes all tests
    - conversations.ts has 28 method handlers (3 existing + 25 new)
    - TypeScript compiles clean for slack twin
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand users.ts to 12 methods + write slack-users test file</name>
  <files>
    twins/slack/src/plugins/web-api/users.ts,
    tests/sdk-verification/sdk/slack-users.test.ts
  </files>
  <behavior>
    - Test: users.conversations({ user: 'U_BOT_TWIN' }) returns { ok: true, channels: array, response_metadata: { next_cursor: '' } }
    - Test: users.getPresence({ user: 'U_BOT_TWIN' }) returns { ok: true, presence: 'active' }
    - Test: users.lookupByEmail({ email: 'bot@twin.local' }) returns { ok: true, user: { id: 'U_BOT_TWIN' } }
    - Test: users.profile.get({ user: 'U_BOT_TWIN' }) returns { ok: true, profile: { display_name: string } }
    - Test: users.profile.set({ profile: { display_name: 'Test' } }) returns { ok: true }
    - Test: users.setPresence({ presence: 'auto' }) returns { ok: true }
    - Test: users.identity() returns { ok: true, user: { id: string }, team: { id: string } }
    - Test: users.deletePhoto() returns { ok: true }
    - Test: users.setPhoto() returns { ok: true }
    - Test: existing users.list and users.info still pass
  </behavior>
  <action>
Add 10 new route handlers to the existing `usersPlugin` in `users.ts`. Keep all existing handlers intact.

New handlers to add:

**Read handlers (GET+POST):**

`GET+POST /api/users.conversations` — params: `{ user? }`. Return `{ ok: true, channels: fastify.slackStateManager.listChannels().map(formatChannel — but users.ts doesn't have formatChannel, so return minimal shape: .map(ch => ({ id: ch.id, name: ch.name })), response_metadata: { next_cursor: '' } }`.

`GET+POST /api/users.getPresence` — params: `{ user }`. Return `{ ok: true, presence: 'active', online: true }`.

`GET+POST /api/users.lookupByEmail` — params: `{ email }`. Search `fastify.slackStateManager.listUsers()` for a user with matching email field. If not found: `{ ok: false, error: 'users_not_found' }`. If found: `{ ok: true, user: formatMember(user) }`.

`GET+POST /api/users.profile.get` — params: `{ user? }`. Look up user (or use token's user_id). Return `{ ok: true, profile: { display_name: user.display_name ?? user.name ?? '', email: user.email ?? '', real_name: user.real_name ?? '' } }`.

`GET+POST /api/users.identity` — Return `{ ok: true, user: { id: tokenRecord.user_id ?? 'U_BOT_TWIN', name: 'bot' }, team: { id: 'T_TWIN' } }`.

**Write handlers (POST only):**

`POST /api/users.profile.set` — body: `{ profile }`. No state needed for conformance. Return `{ ok: true }`.

`POST /api/users.setPresence` — body: `{ presence }`. Return `{ ok: true }`.

`POST /api/users.deletePhoto` — Return `{ ok: true }`.

`POST /api/users.setPhoto` — Return `{ ok: true }`.

`POST /api/users.setActive` (bonus, manifested) — Return `{ ok: true }`.

All new handlers need the auth/rate/error-sim preamble. Use `getParams(request)` for GET+POST methods.

Note on `users.lookupByEmail` seeding: The SlackStateManager seeds `U_BOT_TWIN` with `email: 'bot@twin.local'` in `seedDefaults()`. If not, check the state manager's seedDefaults method and note in the test that the email may differ. Check state manager source before writing test — if the bot user has no email, use `users.list()` to get the actual email and use that.

---

**slack-users.test.ts** — Create test file covering all methods. Follow the same pattern as other test files. Use `beforeEach` with `resetSlack()` + `seedSlackBotToken()`. The seeded bot user `U_BOT_TWIN` is the primary test subject. For `lookupByEmail`, first call `users.info({ user: 'U_BOT_TWIN' })` to get the actual email from the seeded user, then use that email in `lookupByEmail`. This avoids hardcoding an email that might differ from what the state manager seeds.
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- slack-users 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `pnpm test:sdk -- slack-users` passes all tests
    - users.ts has 12 method handlers (2 existing + 10 new)
    - TypeScript compiles clean for slack twin
  </done>
</task>

</tasks>

<verification>
Run `pnpm test:sdk -- slack-conversations slack-users` from the repo root. Both suites must be green. Also verify `pnpm --filter @dtu/slack-twin exec tsc --noEmit` produces no errors.
</verification>

<success_criteria>
- conversations.ts has 28 method handlers (list, info, history, create, join, leave, archive, unarchive, rename, invite, kick, open, close, mark, setPurpose, setTopic, members, replies, plus 10 Slack Connect stub-shapes)
- users.ts has 12 method handlers (list, info, conversations, getPresence, lookupByEmail, profile.get, profile.set, setPresence, identity, deletePhoto, setPhoto, setActive)
- slack-conversations.test.ts: all tests green
- slack-users.test.ts: all tests green
- No changes to index.ts required (these plugins already registered)
</success_criteria>

<output>
After completion, create `.planning/phases/18-slack-webclient-full-surface/18-02-SUMMARY.md`
</output>
