---
phase: 41-regression-closure-and-release-gate-recovery
plan: "05"
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - tests/sdk-verification/helpers/shopify-api-client.ts
  - tests/sdk-verification/coverage/generate-report-evidence.ts
  - tests/sdk-verification/sdk/slack-method-call-fixtures.ts
  - tests/sdk-verification/sdk/slack-method-call-matrix.ts
  - tests/sdk-verification/sdk/slack-method-coverage.test.ts
  - tests/sdk-verification/coverage/proof-integrity-regression.test.ts
autonomous: true
requirements:
  - INFRA-23
  - SLCK-14
must_haves:
  truths:
    - "Runtime symbol evidence now records only symbols actually accessed or executed, not helper-construction assumptions"
    - "Slack method coverage is proven against the exact bound-method set derived from the pinned manifest, not by representative family sampling"
    - "A Slack method counts as covered in this plan only when its invocation completes against the twin without transport failure and returns a payload containing a boolean `ok` field"
    - "Admin and other stub families may satisfy this plan with `{ ok: true }` because SLCK-14 here is a callability proof, not a semantic-parity proof"
  artifacts:
    - path: "tests/sdk-verification/sdk/slack-method-call-matrix.ts"
      provides: "Manifest-keyed invocation matrix for every bound Slack WebClient method"
      contains: "Record<string,"
    - path: "tests/sdk-verification/sdk/slack-method-call-fixtures.ts"
      provides: "Shared seeded fixture state for the full Slack bound-surface proof"
      contains: "buildSlackMethodCallFixtures"
    - path: "tests/sdk-verification/sdk/slack-method-coverage.test.ts"
      provides: "Manifest-exact full-surface Slack proof with an explicit coverage contract"
      contains: "manifestMethodKeys"
  key_links:
    - from: "tests/sdk-verification/sdk/slack-method-coverage.test.ts"
      to: "tools/sdk-surface/manifests/slack-web-api@7.14.1.json"
      via: "manifest keys are the exact SLCK-14 proof contract"
      pattern: "WebClient"
    - from: "tests/sdk-verification/helpers/shopify-api-client.ts"
      to: "tests/sdk-verification/coverage/generate-report-evidence.ts"
      via: "runtime hits feed coverage directly with no fallback attribution"
      pattern: "recordSymbolHit|live"
---

<objective>
Close the remaining proof gaps that can be resolved without touching conformance semantics or completion docs.

Purpose: The repo can still overclaim parity if runtime evidence is inflated by helper construction or if Slack surface proof remains representative-only. This plan makes those seams literal and executable before the final conformance/signoff plan.
Output: A green partial proof-integrity suite for build/runtime/surface truth and a full manifest-exact Slack bound-method proof.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-CONTEXT.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-RESEARCH.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-VALIDATION.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-01-PLAN.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-02-PLAN.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-03-PLAN.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove synthetic runtime evidence and replace representative Slack sampling with full manifest-exact proof</name>
  <files>
tests/sdk-verification/helpers/shopify-api-client.ts
tests/sdk-verification/coverage/generate-report-evidence.ts
tests/sdk-verification/sdk/slack-method-call-fixtures.ts
tests/sdk-verification/sdk/slack-method-call-matrix.ts
tests/sdk-verification/sdk/slack-method-coverage.test.ts
tests/sdk-verification/coverage/proof-integrity-regression.test.ts
  </files>
  <read_first>
tests/sdk-verification/helpers/shopify-api-client.ts
tests/sdk-verification/helpers/slack-client.ts
tests/sdk-verification/coverage/generate-report-evidence.ts
tests/sdk-verification/setup/seeders.ts
tests/sdk-verification/sdk/slack-method-coverage.test.ts
tests/sdk-verification/coverage/proof-integrity-regression.test.ts
tools/sdk-surface/manifests/slack-web-api@7.14.1.json
  </read_first>
  <action>
Make runtime evidence and Slack surface proof literal instead of inferred.

1. In `tests/sdk-verification/helpers/shopify-api-client.ts`, remove eager `recordSymbolHit(...)` calls that fire during `createShopifyApiClient()` construction for these exact symbols:
   - `@shopify/shopify-api@12.3.0/Shopify.auth`
   - `@shopify/shopify-api@12.3.0/Shopify.clients`
   - `@shopify/shopify-api@12.3.0/ShopifyClients`
   - `@shopify/shopify-api@12.3.0/ShopifyClients.Rest`
   - `@shopify/shopify-api@12.3.0/Shopify.rest`
   - `@shopify/shopify-api@12.3.0/ShopifyClients.graphqlProxy`
   Record those symbols only when the real access seam occurs:
   - `Shopify.auth` and `Shopify.clients` only inside the relevant proxy `get` trap when the property is actually read
   - `ShopifyClients`, `ShopifyClients.Rest`, `Shopify.rest`, and `ShopifyClients.graphqlProxy` only inside wrapped constructors or wrapped functions when that constructor/function actually executes

2. In `tests/sdk-verification/coverage/generate-report-evidence.ts`, keep the runtime-hit-plus-passed-file join, but remove any fallback attribution path from helper construction, static file ownership, or synthetic top-level ownership. A symbol is `live` only when that exact symbol key appears in runtime hits from a passing test file.

3. Create `tests/sdk-verification/sdk/slack-method-call-fixtures.ts` exporting one helper, `buildSlackMethodCallFixtures()`, that seeds and returns the reusable fixture object for the full Slack proof. It must own the shared setup for IDs such as:
   - `teamId: 'T_TWIN'`
   - `botUserId: 'U_BOT_TWIN'`
   - `authedUserId: 'U_AUTHED'`
   - `channelId: 'C_GENERAL'`
   - one seeded `privateChannelId`
   - one `dmId` created through `conversations.open`
   - one `messageTs` from a posted bot message in `channelId`
   - one `viewId` from `views.open`
   - one `fileId` from a successful `files.getUploadURLExternal` response
   - any remaining IDs required by the manifest methods, created once here rather than inside individual matrix entries

4. Create `tests/sdk-verification/sdk/slack-method-call-matrix.ts` as a manifest-keyed `Record<string, (client, fixtures) => Promise<unknown>>` containing one invocation per bound `WebClient` method in the pinned manifest. Use the shared fixture object from `buildSlackMethodCallFixtures()`; do not let individual matrix entries invent their own seeding story.

5. Rewrite `tests/sdk-verification/sdk/slack-method-coverage.test.ts` so it:
   - loads `tools/sdk-surface/manifests/slack-web-api@7.14.1.json`
   - derives the exact set of bound WebClient method keys from the manifest at test time
   - asserts that manifest-derived method count is at least `275` before continuing, so an accidentally filtered set cannot pass
   - asserts the matrix keys exactly equal that manifest-derived set
   - calls `buildSlackMethodCallFixtures()` once per test context
   - iterates every matrix entry against the twin
   - treats `resolves against twin` as: the call does not throw a transport exception and the returned payload is an object containing a boolean `ok` field
   - removes any `representative method` or family-sampling wording
   Admin-family stubs returning `{ ok: true }` are acceptable in this plan because SLCK-14 here is proving full-surface callability, not semantic parity.

6. Update `tests/sdk-verification/coverage/proof-integrity-regression.test.ts` so the build-script, constructor-hit, and representative-proof assertions go green after these changes. Leave the conformance-header assertion for Plan 41-06.
  </action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/coverage/proof-integrity-regression.test.ts -t "build script includes twin builds|shopify-api-client does not record top-level symbols at construction time|slack method coverage proof is not representative-only" && pnpm vitest run tests/sdk-verification/sdk/slack-method-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/helpers/shopify-api-client.ts` no longer contains eager constructor-time `recordSymbolHit(...)` calls for the six exact symbols listed in the action text.
- `@shopify/shopify-api@12.3.0/Shopify.auth` and `@shopify/shopify-api@12.3.0/Shopify.clients` are only recorded inside proxy getters, and `ShopifyClients.Rest` plus `ShopifyClients.graphqlProxy` are only recorded inside wrapped constructor/function bodies.
- `tests/sdk-verification/sdk/slack-method-call-fixtures.ts` exists and exports the shared seeded fixture helper used by the full-surface proof.
- `tests/sdk-verification/sdk/slack-method-call-matrix.ts` exists and exports a manifest-keyed invocation matrix.
- `tests/sdk-verification/sdk/slack-method-coverage.test.ts` loads `tools/sdk-surface/manifests/slack-web-api@7.14.1.json`, asserts the matrix keys exactly equal the manifest-derived method set, asserts that set contains at least 275 methods, and no longer contains the phrase `representative method`.
- The Slack method proof treats a method as covered only when the invocation returns an object with boolean `ok` and does not fail at the transport layer.
- `pnpm vitest run tests/sdk-verification/coverage/proof-integrity-regression.test.ts -t "build script includes twin builds|shopify-api-client does not record top-level symbols at construction time|slack method coverage proof is not representative-only" && pnpm vitest run tests/sdk-verification/sdk/slack-method-coverage.test.ts` exits 0.
  </acceptance_criteria>
  <done>Runtime evidence is now literal and the Slack method-surface requirement is proven against the full pinned manifest instead of a sampled subset.</done>
</task>

</tasks>

<verification>
Run the filtered proof-integrity checks and the full Slack method-coverage test. Do not advance until the runtime-evidence assertions and full manifest-exact surface proof are green together.
</verification>

<success_criteria>
The repo can no longer claim symbol coverage or Slack method coverage from synthetic attribution or family sampling. Both are now derived from exact execution seams and the full pinned manifest.
</success_criteria>

<output>
After completion, create `.planning/phases/41-regression-closure-and-release-gate-recovery/41-05-SUMMARY.md`
</output>
