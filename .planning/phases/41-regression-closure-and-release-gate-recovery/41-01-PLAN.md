---
phase: 41-regression-closure-and-release-gate-recovery
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/sdk-verification/sdk/shopify-regression-closure.test.ts
  - tests/sdk-verification/sdk/slack-regression-closure.test.ts
  - tests/sdk-verification/coverage/proof-integrity-regression.test.ts
autonomous: true
requirements:
  - INFRA-19
  - INFRA-20
  - INFRA-23
  - INFRA-24
  - INFRA-25
  - SHOP-17
  - SHOP-18
  - SHOP-20
  - SLCK-14
  - SLCK-18
  - SLCK-20
  - SLCK-22
must_haves:
  truths:
    - "Wave 0 turns each currently open build, Shopify, Slack, and proof regression into an explicit executable contract before production files change"
    - "The new contracts fail on the current branch with assertion-style failures, not syntax errors, import errors, or missing-fixture crashes"
    - "No production implementation file changes in this plan; this plan only adds RED contract coverage"
  artifacts:
    - path: "tests/sdk-verification/sdk/shopify-regression-closure.test.ts"
      provides: "RED contracts for Shopify version, OAuth response shape, delete persistence, orderUpdate integrity, and InventoryLevel parity"
      contains: "unsupported API version 2025-02"
    - path: "tests/sdk-verification/sdk/slack-regression-closure.test.ts"
      provides: "RED contracts for Slack scope enforcement gaps, OAuth/OIDC credential validation, and filesUploadV2 behavior"
      contains: "users.setPhoto returns missing_scope"
    - path: "tests/sdk-verification/coverage/proof-integrity-regression.test.ts"
      provides: "RED contracts for root build truth, constructor-time symbol inflation, representative-only Slack surface proof, and conformance header/value under-checking"
      contains: "build script includes twin builds"
  key_links:
    - from: "tests/sdk-verification/sdk/shopify-regression-closure.test.ts"
      to: "twins/shopify/src/services/api-version.ts"
      via: "unsupported-version rejection is verified against the current parser"
      pattern: "2025-02"
    - from: "tests/sdk-verification/sdk/shopify-regression-closure.test.ts"
      to: "twins/shopify/src/plugins/oauth.ts"
      via: "refresh/offline token response shape contracts"
      pattern: "refresh_token_expires_in|requested_token_type"
    - from: "tests/sdk-verification/sdk/slack-regression-closure.test.ts"
      to: "twins/slack/src/services/method-scopes.ts"
      via: "missing_scope assertions for methods currently absent from the scope catalog"
      pattern: "users.setPhoto|files.getUploadURLExternal|search.files"
    - from: "tests/sdk-verification/coverage/proof-integrity-regression.test.ts"
      to: "tests/sdk-verification/helpers/shopify-api-client.ts"
      via: "constructor-time symbol hits are forbidden by explicit contract"
      pattern: "Shopify.auth|Shopify.rest|ShopifyClients.Rest"
---

<objective>
Create the Phase 41 RED contracts before implementation starts.

Purpose: The current branch is close enough to green that a sloppy fix plan could easily hide remaining drift. This plan turns every open regression cluster into an explicit failing contract so later plans have exact targets and cannot claim success by inference.
Output: Three new regression-contract test files that compile and fail for the right reasons on the current branch.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-CONTEXT.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-RESEARCH.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED Shopify regression contracts</name>
  <files>
tests/sdk-verification/sdk/shopify-regression-closure.test.ts
  </files>
  <read_first>
tests/sdk-verification/helpers/shopify-api-client.ts
tests/sdk-verification/setup/seeders.ts
tests/sdk-verification/sdk/shopify-api-auth.test.ts
tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts
twins/shopify/src/services/api-version.ts
twins/shopify/src/plugins/oauth.ts
twins/shopify/src/plugins/rest.ts
twins/shopify/src/schema/resolvers.ts
packages/state/src/state-manager.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/types.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/create-session.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/refresh-token.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/token-exchange.ts
  </read_first>
  <action>
Create `tests/sdk-verification/sdk/shopify-regression-closure.test.ts` and keep it RED on the current branch.

Use `createShopifyApiClient()`, `resetShopify()`, `seedShopifyAccessToken()`, vendored `restResources` from `@shopify/shopify-api/rest/admin/2024-01`, and direct `fetch()` only where the SDK surface is awkward. Add these exact test names and target states:

1. `returns 404 for unsupported API version 2025-02 on Admin REST and GraphQL routes`
   - Probe `GET /admin/api/2025-02/products.json`
   - Probe `POST /admin/api/2025-02/graphql.json`
   - Assert non-200 plus no echoed `X-Shopify-API-Version: 2025-02`

2. `Product.delete() removes the product from subsequent GET /products/:id.json`
   - Create a product through the pinned REST resource
   - Delete it through the pinned REST resource
   - Assert direct GET returns 404 or SDK `find()` rejects/not found rather than returning the deleted record

3. `refreshToken() returns refresh token metadata for offline sessions`
   - Call `shopify.auth.refreshToken(...)`
   - Assert the resulting session has `refreshToken` and `refreshTokenExpires`

4. `offline tokenExchange with expiring=true returns refresh token metadata`
   - Call `shopify.auth.tokenExchange({ requestedTokenType: OfflineAccessToken, expiring: true })`
   - Assert the resulting session has `refreshToken` and `refreshTokenExpires`

5. `orderUpdate preserves lineItems arrays for subsequent order queries`
   - Create an order, run `orderUpdate`, then query the order again
   - Assert the follow-up query returns an array-valued `lineItems.edges[*].node`, not a string-parsing failure

6. `InventoryLevel.adjust() returns inventory_level and Location.inventory_levels sees the same row`
   - Before writing this test, read the current failure path in `tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts` and the REST inventory handlers in `twins/shopify/src/plugins/rest.ts`
   - Seed or create an inventory item using the same code path the existing behavioral parity test exercises
   - Call `InventoryLevel.connect`, `InventoryLevel.adjust`, and `Location.inventory_levels`
   - Assert the same `(inventory_item_id, location_id, available)` tuple is visible through both paths and that the failure, on the current branch, is the actual parity mismatch rather than a fixture/setup miss

Do not change production files in this task. Keep the failures local to assertions about the current broken behavior.
  </action>
  <verify>
    <automated>OUTPUT=$(pnpm vitest run tests/sdk-verification/sdk/shopify-regression-closure.test.ts 2>&1); STATUS=$?; printf '%s\n' "$OUTPUT"; test $STATUS -ne 0 && printf '%s' "$OUTPUT" | rg -q "unsupported API version 2025-02|Product.delete\\(\\)|refreshToken|offline tokenExchange|orderUpdate preserves lineItems|InventoryLevel.adjust\\(\\)" && ! printf '%s' "$OUTPUT" | rg -q "SyntaxError|Cannot find module|Failed to load"</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/sdk/shopify-regression-closure.test.ts` contains the exact test names `returns 404 for unsupported API version 2025-02 on Admin REST and GraphQL routes`, `Product.delete() removes the product from subsequent GET /products/:id.json`, `refreshToken() returns refresh token metadata for offline sessions`, `offline tokenExchange with expiring=true returns refresh token metadata`, `orderUpdate preserves lineItems arrays for subsequent order queries`, and `InventoryLevel.adjust() returns inventory_level and Location.inventory_levels sees the same row`.
- The verify command exits non-zero on the current branch because at least one of those assertions still fails.
- The failure output contains assertion-style text for the named regressions and does not contain syntax or module-load failures.
- No production file outside the new test file changes in this task.
  </acceptance_criteria>
  <done>The repo has an executable RED contract for every currently open Shopify regression cluster.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED Slack regression contracts</name>
  <files>
tests/sdk-verification/sdk/slack-regression-closure.test.ts
  </files>
  <read_first>
tests/sdk-verification/helpers/slack-client.ts
tests/sdk-verification/setup/seeders.ts
tests/sdk-verification/sdk/slack-webclient-base.test.ts
twins/slack/src/services/method-scopes.ts
twins/slack/src/plugins/web-api/files.ts
twins/slack/src/plugins/web-api/new-families.ts
twins/slack/src/plugins/oauth.ts
third_party/upstream/node-slack-sdk/packages/web-api/src/WebClient.ts
third_party/upstream/node-slack-sdk/packages/web-api/src/types/request/files.ts
third_party/upstream/node-slack-sdk/packages/web-api/src/types/request/oauth.ts
third_party/upstream/node-slack-sdk/packages/web-api/src/types/request/openid.ts
  </read_first>
  <action>
Create `tests/sdk-verification/sdk/slack-regression-closure.test.ts` and keep it RED on the current branch.

Use `createSlackClient()`, `resetSlack()`, `seedSlackChannel()`, and direct `POST /admin/tokens` seeding for narrow-scope tokens. Add these exact test names and behaviors:

1. `users.setPhoto returns missing_scope for a token seeded with chat:write only`
2. `files.getUploadURLExternal returns missing_scope for a token seeded with chat:write only`
3. `search.files returns missing_scope for a token seeded with chat:write only`
   - Seed a token directly through `POST /admin/tokens` with `scope: "chat:write"`
   - Assert each method resolves to `{ ok: false, error: "missing_scope" }`

4. `files.getUploadURLExternal returns an absolute upload_url and rejects missing filename or length`
   - Assert a successful response has `upload_url` starting with `http://` or `https://`
   - Assert calls missing `filename` or `length` fail with `{ ok: false, error: "invalid_arguments" }`

5. `files.completeUploadExternal rejects an empty files array`
   - Call the endpoint with `files: []` and assert `{ ok: false, error: "invalid_arguments" }`

6. `oauth.access rejects an invalid client secret`
7. `oauth.v2.exchange rejects an invalid client secret`
8. `openid.connect.token rejects an unknown client_id and wrong secret`
   - Assert each endpoint returns `{ ok: false, error: "invalid_client" }` rather than `ok: true`

Do not patch Slack implementation files in this task.
  </action>
  <verify>
    <automated>OUTPUT=$(pnpm vitest run tests/sdk-verification/sdk/slack-regression-closure.test.ts 2>&1); STATUS=$?; printf '%s\n' "$OUTPUT"; test $STATUS -ne 0 && printf '%s' "$OUTPUT" | rg -q "users.setPhoto returns missing_scope|absolute upload_url|oauth.access rejects an invalid client secret" && ! printf '%s' "$OUTPUT" | rg -q "SyntaxError|Cannot find module|Failed to load"</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/sdk/slack-regression-closure.test.ts` contains the exact test names listed in the action text.
- The file seeds a narrow-scope token through `POST /admin/tokens` rather than editing the global seeder behavior inside the test.
- The verify command exits non-zero on the current branch with assertion-style failures naming the current regressions.
- No production file outside the new test file changes in this task.
  </acceptance_criteria>
  <done>The repo has an executable RED contract for the current Slack scope, OAuth/OIDC, and filesUploadV2 regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add RED proof-integrity regression contracts</name>
  <files>
tests/sdk-verification/coverage/proof-integrity-regression.test.ts
  </files>
  <read_first>
package.json
tests/sdk-verification/helpers/shopify-api-client.ts
tests/sdk-verification/coverage/generate-report-evidence.ts
tests/sdk-verification/setup/execution-evidence-runtime.ts
tests/sdk-verification/sdk/slack-method-coverage.test.ts
packages/conformance/src/comparator.ts
tools/sdk-surface/manifests/slack-web-api@7.14.1.json
  </read_first>
  <action>
Create `tests/sdk-verification/coverage/proof-integrity-regression.test.ts` as a source-reading RED contract for the active proof gaps.

Add these exact tests:

1. `build script includes twin builds`
   - Read `package.json`
   - Assert the `build` script includes both `./packages/*` and `./twins/*`

2. `shopify-api-client does not record top-level symbols at construction time`
   - Read `tests/sdk-verification/helpers/shopify-api-client.ts`
   - Assert the file does not contain eager `recordSymbolHit(...)` calls for `Shopify.auth`, `Shopify.clients`, `ShopifyClients`, `ShopifyClients.Rest`, `Shopify.rest`, or `ShopifyClients.graphqlProxy` outside proxy getters or wrapped constructors

3. `slack method coverage proof is not representative-only`
   - Read `tests/sdk-verification/sdk/slack-method-coverage.test.ts`
   - Assert it does not contain the phrase `representative method`

4. `conformance comparator preserves deterministic proof headers`
   - Read `packages/conformance/src/comparator.ts`
   - Assert there is no exact-mode path that collapses compared headers to `content-type` only; write this against the current broken implementation so the test fails specifically because the exact-mode comparator still strips deterministic proof headers such as `x-shopify-api-version`, `x-oauth-scopes`, and `x-accepted-oauth-scopes`

Keep the test RED on the current branch. Do not patch implementation files in this task.
  </action>
  <verify>
    <automated>OUTPUT=$(pnpm vitest run tests/sdk-verification/coverage/proof-integrity-regression.test.ts 2>&1); STATUS=$?; printf '%s\n' "$OUTPUT"; test $STATUS -ne 0 && printf '%s' "$OUTPUT" | rg -q "build script includes twin builds|shopify-api-client does not record top-level symbols at construction time|representative-only|deterministic proof headers" && ! printf '%s' "$OUTPUT" | rg -q "SyntaxError|Cannot find module|Failed to load"</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/coverage/proof-integrity-regression.test.ts` contains the exact test names listed in the action text.
- The file reads `package.json`, `shopify-api-client.ts`, `slack-method-coverage.test.ts`, and `comparator.ts` directly from disk.
- The verify command exits non-zero on the current branch for assertion reasons tied to the current proof gaps.
- No production file outside the new test file changes in this task.
  </acceptance_criteria>
  <done>The repo has RED proof contracts for build truth, runtime-evidence truth, method-surface proof, and conformance header/value proof.</done>
</task>

</tasks>

<verification>
Run the three new contract files individually and confirm all of them fail for the expected Phase 41 reasons instead of syntax, module, or fixture errors.
</verification>

<success_criteria>
Phase 41 starts from explicit, executable RED contracts for every currently open regression cluster, so later plans have no room to hide behind indirect green paths.
</success_criteria>

<output>
After completion, create `.planning/phases/41-regression-closure-and-release-gate-recovery/41-01-SUMMARY.md`
</output>
