---
phase: 41-regression-closure-and-release-gate-recovery
plan: "04"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - twins/slack/src/services/method-scopes.ts
  - twins/slack/src/services/oauth-secrets.ts
  - twins/slack/src/plugins/web-api/files.ts
  - twins/slack/src/plugins/web-api/new-families.ts
  - twins/slack/src/plugins/oauth.ts
  - docker-compose.twin.yml
  - tests/sdk-verification/sdk/slack-scope-catalog.test.ts
  - tests/sdk-verification/sdk/slack-docker-upload-url-smoke.test.ts
  - tests/sdk-verification/sdk/slack-regression-closure.test.ts
  - tests/sdk-verification/sdk/slack-webclient-base.test.ts
autonomous: true
requirements:
  - INFRA-20
  - SLCK-18
  - SLCK-20
  - SLCK-22
must_haves:
  truths:
    - "Every auth-checked Slack method now has an explicit `METHOD_SCOPES` entry, including no-auth endpoints as `[]`"
    - "Narrow-scope tokens fail audited methods with `missing_scope` instead of passing because the catalog forgot them"
    - "Slack OAuth/OIDC stubs reject invalid credentials instead of minting success responses"
    - "`filesUploadV2` always returns an absolute upload URL and rejects upstream-invalid payloads"
  artifacts:
    - path: "tests/sdk-verification/sdk/slack-scope-catalog.test.ts"
      provides: "Completeness proof that the shared scope catalog covers every checked method"
      contains: "METHOD_SCOPES"
    - path: "twins/slack/src/services/method-scopes.ts"
      provides: "Expanded, explicit scope catalog shared by seeding and enforcement"
      contains: "files.getUploadURLExternal"
    - path: "twins/slack/src/services/oauth-secrets.ts"
      provides: "Shared client-secret source of truth used by every OAuth/OIDC entry point"
      contains: "OAUTH_CLIENT_SECRETS"
    - path: "docker-compose.twin.yml"
      provides: "Slack twin environment with an explicit public API base URL"
      contains: "SLACK_API_URL"
  key_links:
    - from: "tests/sdk-verification/sdk/slack-scope-catalog.test.ts"
      to: "twins/slack/src/services/method-scopes.ts"
      via: "catalog completeness test guards against future omissions"
      pattern: "METHOD_SCOPES"
    - from: "twins/slack/src/plugins/web-api/new-families.ts"
      to: "twins/slack/src/plugins/oauth.ts"
      via: "shared client-secret validation across OAuth entry points"
      pattern: "CLIENT_SECRETS"
---

<objective>
Close the remaining Slack auth/files regressions with one shared source of truth.

Purpose: The current Slack twin still hides missing scope coverage behind catalog omissions, still accepts invalid OAuth/OIDC credentials, and still has an environment-dependent `filesUploadV2` flow. This plan makes those seams explicit, testable, and aligned with the vendored SDK contracts.
Output: A green Slack regression suite, a green scope-catalog completeness test, and an absolute/validated file-upload flow.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-CONTEXT.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-RESEARCH.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-VALIDATION.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-01-PLAN.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the shared Slack scope catalog exhaustive and test-enforced</name>
  <files>
twins/slack/src/services/method-scopes.ts
tests/sdk-verification/sdk/slack-scope-catalog.test.ts
tests/sdk-verification/sdk/slack-regression-closure.test.ts
  </files>
  <read_first>
twins/slack/src/services/method-scopes.ts
twins/slack/src/plugins/web-api/chat.ts
twins/slack/src/plugins/web-api/conversations.ts
twins/slack/src/plugins/web-api/files.ts
twins/slack/src/plugins/web-api/users.ts
twins/slack/src/plugins/web-api/stubs.ts
twins/slack/src/plugins/web-api/admin.ts
twins/slack/src/plugins/web-api/auth.ts
twins/slack/src/plugins/web-api/pins.ts
twins/slack/src/plugins/web-api/reactions.ts
twins/slack/src/plugins/web-api/views.ts
twins/slack/src/plugins/web-api/new-families.ts
tests/sdk-verification/setup/seeders.ts
tests/sdk-verification/sdk/slack-regression-closure.test.ts
  </read_first>
  <action>
Eliminate scope-catalog omissions as a class of bug.

1. Create `tests/sdk-verification/sdk/slack-scope-catalog.test.ts` with these exact test names:
   - `scope catalog is exhaustive for every auth-checked Slack method`
   - `scope catalog rejects unresolved dynamic method names`
   Build the test as a TypeScript-AST source audit over every file under `twins/slack/src/plugins/web-api/` plus `twins/slack/src/plugins/oauth.ts`. Do not rely on regex-only scanning.

2. The catalog audit must collect method names that reach auth/scope enforcement or accepted-scope headers through all current patterns:
   - direct `checkScope('method.name', ...)` calls
   - `checkScope(methodName, ...)` or `METHOD_SCOPES[methodName]` lookups where `methodName` resolves to a `const` string literal in the same scope
   - call sites of local wrapper/helpers that themselves call `checkScope(...)` or read `METHOD_SCOPES[...]`, such as `stub('files.delete')`, `checkAuth(..., 'users.info')`, or `checkAuthRateError(..., 'chat.postMessage')`
   If the audit finds a method argument path it cannot resolve to a concrete string literal, the second test must fail instead of skipping that case.

3. In `twins/slack/src/services/method-scopes.ts`, add explicit entries for every method the new test reports as missing. At minimum, the final file must include these exact keys:
   - `users.setPhoto`
   - `files.getUploadURLExternal`
   - `files.completeUploadExternal`
   - `search.files`
   - `chat.appendStream`
   - `chat.stopStream`
   Add all other reported omissions in the same pass.

4. Keep no-auth methods explicit with `[]` rather than leaving them absent from the catalog.

5. Preserve `allScopesString()` as the union of the catalog so `seedSlackBotToken()` stays aligned with enforcement after the catalog expands.

6. Update the regression file so the `chat:write`-only token assertions for `users.setPhoto`, `files.getUploadURLExternal`, and `search.files` go green.
  </action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/sdk/slack-scope-catalog.test.ts && pnpm vitest run tests/sdk-verification/sdk/slack-regression-closure.test.ts -t "missing_scope"</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/sdk/slack-scope-catalog.test.ts` exists, scans every Slack Web API plugin file plus `twins/slack/src/plugins/oauth.ts`, and uses AST-based identifier resolution rather than regex-only matching.
- The catalog audit resolves direct method literals, identifier-backed method names, and wrapper/helper call sites that feed `checkScope(...)` or `METHOD_SCOPES[...]`.
- `twins/slack/src/services/method-scopes.ts` contains the exact keys `users.setPhoto`, `files.getUploadURLExternal`, `files.completeUploadExternal`, `search.files`, `chat.appendStream`, and `chat.stopStream`.
- The completeness test fails if a checked method is missing from `METHOD_SCOPES`, and it also fails if any method path cannot be resolved to a concrete string literal.
- `pnpm vitest run tests/sdk-verification/sdk/slack-scope-catalog.test.ts && pnpm vitest run tests/sdk-verification/sdk/slack-regression-closure.test.ts -t "missing_scope"` exits 0.
  </acceptance_criteria>
  <done>Slack scope enforcement and Slack seeding now share one exhaustive, test-enforced catalog instead of drifting by omission.</done>
</task>

<task type="auto">
  <name>Task 2: Make Slack OAuth/OIDC and filesUploadV2 strict and environment-safe</name>
  <files>
twins/slack/src/plugins/web-api/new-families.ts
twins/slack/src/plugins/oauth.ts
twins/slack/src/services/oauth-secrets.ts
twins/slack/src/plugins/web-api/files.ts
docker-compose.twin.yml
tests/sdk-verification/sdk/slack-regression-closure.test.ts
tests/sdk-verification/sdk/slack-webclient-base.test.ts
  </files>
  <read_first>
twins/slack/src/plugins/web-api/new-families.ts
twins/slack/src/plugins/oauth.ts
twins/slack/src/plugins/web-api/files.ts
docker-compose.twin.yml
tests/sdk-verification/sdk/slack-regression-closure.test.ts
tests/sdk-verification/sdk/slack-webclient-base.test.ts
tests/sdk-verification/sdk/slack-docker-upload-url-smoke.test.ts
third_party/upstream/node-slack-sdk/packages/web-api/src/types/request/oauth.ts
third_party/upstream/node-slack-sdk/packages/web-api/src/types/request/openid.ts
third_party/upstream/node-slack-sdk/packages/web-api/src/types/request/files.ts
third_party/upstream/node-slack-sdk/packages/web-api/src/WebClient.ts
  </read_first>
  <action>
Make Slack OAuth/OIDC and `filesUploadV2` obey the vendored request contracts.

1. Create `twins/slack/src/services/oauth-secrets.ts` as the single source of truth for known Slack OAuth/OIDC client IDs and client secrets. Import that file into both `twins/slack/src/plugins/oauth.ts` and `twins/slack/src/plugins/web-api/new-families.ts`. Unknown client IDs must return `{ ok: false, error: "invalid_client" }`.

2. In `twins/slack/src/plugins/web-api/new-families.ts`:
   - `oauth.access` must require `client_id`, `client_secret`, and `code`, and must validate `client_secret`
   - `oauth.v2.exchange` must require `client_id`, `client_secret`, and `token`, and must validate `client_secret`
   - `openid.connect.token` must require a known `client_id`, a matching `client_secret`, and either `code` or `refresh_token` in line with the vendored `OAuthGrantRefresh` type; wrong or unknown credentials return `invalid_client`, missing required arguments return `invalid_arguments`

3. In `twins/slack/src/plugins/web-api/files.ts`:
   - reject missing or empty `filename`
   - reject missing, non-numeric, or non-positive `length`
   - keep parsing JSON-string `files` payloads, but reject an empty `files` array with `invalid_arguments`
   - produce an absolute `upload_url` even when `SLACK_API_URL` is unset by falling back to the request origin (`protocol + host`) rather than returning a relative path

4. In `docker-compose.twin.yml`, set `SLACK_API_URL` for the `slack-twin` service to `http://localhost:3001/api` so Docker-based verification also exercises the absolute upload URL path through a stable public base URL.

5. Create `tests/sdk-verification/sdk/slack-docker-upload-url-smoke.test.ts` that runs against `SLACK_API_URL=http://localhost:3001`, seeds a token through `/admin/tokens`, calls `files.getUploadURLExternal`, and asserts the returned `upload_url` starts with `http://localhost:3001/api/`.

6. Update the Slack regression file and `tests/sdk-verification/sdk/slack-webclient-base.test.ts` so the invalid-credential and absolute-upload-url assertions all go green.
  </action>
  <verify>
    <automated>sh -c 'set -e; docker compose -f docker-compose.twin.yml up -d --build slack-twin; trap "docker compose -f docker-compose.twin.yml rm -sf slack-twin >/dev/null 2>&1 || true" EXIT; SLACK_API_URL=http://localhost:3001 pnpm vitest run tests/sdk-verification/sdk/slack-docker-upload-url-smoke.test.ts; pnpm vitest run tests/sdk-verification/sdk/slack-regression-closure.test.ts tests/sdk-verification/sdk/slack-webclient-base.test.ts'</automated>
  </verify>
  <acceptance_criteria>
- `twins/slack/src/services/oauth-secrets.ts` exists and is imported by both `twins/slack/src/plugins/oauth.ts` and `twins/slack/src/plugins/web-api/new-families.ts`.
- `twins/slack/src/plugins/web-api/new-families.ts` rejects invalid credentials for `oauth.access`, `oauth.v2.exchange`, and `openid.connect.token`.
- `twins/slack/src/plugins/web-api/files.ts` contains explicit validation for `filename`, `length`, and empty `files` arrays.
- `docker-compose.twin.yml` contains `SLACK_API_URL` for the `slack-twin` service.
- `tests/sdk-verification/sdk/slack-docker-upload-url-smoke.test.ts` exists and proves the Docker-composed Slack twin returns an absolute `upload_url` rooted at `http://localhost:3001/api/`.
- `sh -c 'set -e; docker compose -f docker-compose.twin.yml up -d --build slack-twin; trap "docker compose -f docker-compose.twin.yml rm -sf slack-twin >/dev/null 2>&1 || true" EXIT; SLACK_API_URL=http://localhost:3001 pnpm vitest run tests/sdk-verification/sdk/slack-docker-upload-url-smoke.test.ts; pnpm vitest run tests/sdk-verification/sdk/slack-regression-closure.test.ts tests/sdk-verification/sdk/slack-webclient-base.test.ts'` exits 0.
  </acceptance_criteria>
  <done>Slack OAuth/OIDC and filesUploadV2 now follow the vendored SDK contracts instead of accepting invalid inputs or depending on hidden environment assumptions.</done>
</task>

</tasks>

<verification>
Run the scope-catalog completeness test, the Slack regression file, and the existing WebClient base test. Do not advance until all three are green together.
</verification>

<success_criteria>
Slack scope enforcement, OAuth/OIDC validation, and filesUploadV2 behavior are now explicit, strict, and environment-safe, eliminating the false-green behavior the audit reproduced.
</success_criteria>

<output>
After completion, create `.planning/phases/41-regression-closure-and-release-gate-recovery/41-04-SUMMARY.md`
</output>
