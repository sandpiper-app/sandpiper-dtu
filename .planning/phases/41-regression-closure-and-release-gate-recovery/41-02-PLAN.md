---
phase: 41-regression-closure-and-release-gate-recovery
plan: "02"
type: execute
wave: 1
depends_on:
  - "01"
files_modified:
  - package.json
  - twins/shopify/src/plugins/oauth.ts
  - tests/sdk-verification/setup/global-setup.ts
  - tests/sdk-verification/setup/execution-evidence-runtime.ts
  - tests/sdk-verification/coverage/runtime-artifact-resilience.test.ts
  - tests/sdk-verification/coverage/fixtures/failing-evidence-fixture.test.ts
autonomous: true
requirements:
  - INFRA-19
  - INFRA-23
  - INFRA-25
must_haves:
  truths:
    - "Root `pnpm build` now fails when a twin fails to compile because the root build actually includes both twins"
    - "The Shopify twin compile error on `subject_token_type` is fixed without loosening OAuth validation behavior"
    - "A failing SDK run leaves `tests/sdk-verification/coverage/symbol-execution.json` present and valid so later gates remain runnable"
  artifacts:
    - path: "package.json"
      provides: "Root build script that includes workspace twins"
      contains: "./twins/*"
    - path: "twins/shopify/src/plugins/oauth.ts"
      provides: "Compiling Shopify OAuth request type including subject_token_type"
      contains: "subject_token_type?: string"
    - path: "tests/sdk-verification/coverage/runtime-artifact-resilience.test.ts"
      provides: "Automated proof that evidence artifacts survive a failing test run"
      contains: "symbol-execution.json"
  key_links:
    - from: "package.json"
      to: "twins/shopify/src/plugins/oauth.ts"
      via: "root build now covers the twin that previously failed to compile"
      pattern: "build"
    - from: "tests/sdk-verification/setup/global-setup.ts"
      to: "tests/sdk-verification/setup/execution-evidence-runtime.ts"
      via: "artifact lifecycle is coordinated through one runtime-evidence path"
      pattern: "symbol-execution.json|flushExecutionEvidence"
---

<objective>
Repair the release gates before behavior fixes continue.

Purpose: The current branch can still claim a green root build while a twin is broken, and a failing SDK run can destroy the evidence path that later verification depends on. This plan makes the build and artifact gates truthful and resilient before Shopify and Slack parity work proceeds.
Output: A root build that includes twins, a compiling Shopify twin, and an automated artifact-resilience proof.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-CONTEXT.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-RESEARCH.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-VALIDATION.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the root build truthful and fix the Shopify twin compile break</name>
  <files>
package.json
twins/shopify/src/plugins/oauth.ts
  </files>
  <read_first>
package.json
twins/shopify/package.json
twins/slack/package.json
twins/shopify/src/plugins/oauth.ts
.planning/phases/41-regression-closure-and-release-gate-recovery/41-RESEARCH.md
  </read_first>
  <action>
Update the root build gate and the Shopify twin compile surface together.

1. In `package.json`, replace the current root `build` script with a workspace-recursive build that includes both packages and twins. Use the exact pattern:
   - `pnpm -r --filter ./packages/* --filter ./twins/* build`
   Keep any existing prebuild/typecheck pieces only if they still run before this recursive build command.

2. In `twins/shopify/src/plugins/oauth.ts`, extend `OAuthTokenRequestBody` with:
   - `subject_token_type?: string;`
   Do not remove or weaken the current token-exchange validation branch. The goal is compile correctness, not broader auth acceptance.

3. If the root build or twin build fails for any other Phase 41-adjacent compile issue exposed by the new root script, fix that compile issue in the same touched file only if it is necessary to make the new root build truthful. Do not broaden into unrelated refactors.
  </action>
  <verify>
    <automated>pnpm build && pnpm --dir twins/shopify build && pnpm --dir twins/slack build</automated>
  </verify>
  <acceptance_criteria>
- `package.json` contains a root `build` script with both `--filter ./packages/*` and `--filter ./twins/*`.
- `twins/shopify/package.json` and `twins/slack/package.json` each still contain a `build` script, so the new root build path can actually execute both twins.
- `twins/shopify/src/plugins/oauth.ts` contains `subject_token_type?: string;` in `OAuthTokenRequestBody`.
- `pnpm build`, `pnpm --dir twins/shopify build`, and `pnpm --dir twins/slack build` all exit 0.
  </acceptance_criteria>
  <done>The root build is now a truthful release gate and the Shopify twin compile break is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make runtime evidence artifacts survive failing SDK runs</name>
  <files>
tests/sdk-verification/setup/global-setup.ts
tests/sdk-verification/setup/execution-evidence-runtime.ts
tests/sdk-verification/coverage/runtime-artifact-resilience.test.ts
tests/sdk-verification/coverage/fixtures/failing-evidence-fixture.test.ts
  </files>
  <read_first>
tests/sdk-verification/setup/global-setup.ts
tests/sdk-verification/setup/execution-evidence-runtime.ts
tests/sdk-verification/coverage/symbol-execution.json
tests/sdk-verification/coverage/vitest-evidence.json
tests/sdk-verification/coverage/generate-report-evidence.ts
.planning/phases/41-regression-closure-and-release-gate-recovery/41-RESEARCH.md
  </read_first>
  <action>
Repair the runtime-evidence lifecycle so a failing SDK run does not delete `tests/sdk-verification/coverage/symbol-execution.json`.

1. In `tests/sdk-verification/setup/global-setup.ts`, stop unlinking `tests/sdk-verification/coverage/symbol-execution.json` at startup. Replace that behavior with one of these exact safe states:
   - create/truncate the file to a valid empty artifact payload before tests run, or
   - write to a temporary runtime file and atomically replace `symbol-execution.json` on flush.
   Put the initialization in the setup path and the normal happy-path flush in the returned teardown path of `globalSetup()`. Do not hide this lifecycle in an unrelated helper.

2. In `tests/sdk-verification/setup/execution-evidence-runtime.ts`, register flush behavior that runs even when the test process exits non-zero. Use explicit failure-path hooks in that runtime file, with an idempotent guard, for at least:
   - `process.on('beforeExit', ...)`
   - `process.on('exit', ...)`
   - `process.on('SIGINT', ...)`
   - `process.on('SIGTERM', ...)`
   - `process.on('uncaughtException', ...)`
   - `process.on('unhandledRejection', ...)`
   The end state must guarantee that `symbol-execution.json` exists and parses as JSON after a failing run.

3. Create `tests/sdk-verification/coverage/fixtures/failing-evidence-fixture.test.ts` as a one-test file that intentionally fails after recording at least one symbol hit.

4. Create `tests/sdk-verification/coverage/runtime-artifact-resilience.test.ts` that spawns a child Vitest run of that failing fixture, asserts the child exits non-zero, then asserts:
   - it deletes any pre-existing `tests/sdk-verification/coverage/symbol-execution.json` before the child run, or records a `spawnStartedAt` timestamp and proves the resulting artifact is newer than that moment
   - `tests/sdk-verification/coverage/symbol-execution.json` exists
   - the file parses as JSON
   - the parsed payload contains `generatedAt`
   - the parsed payload contains `hits` as an array

Do not try to make the full SDK suite green in this task. The proof here is a deliberately red child run that leaves a usable artifact behind.
  </action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/coverage/runtime-artifact-resilience.test.ts && pnpm coverage:generate && pnpm drift:check</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/setup/global-setup.ts` no longer contains a destructive unlink of `symbol-execution.json`.
- `tests/sdk-verification/setup/execution-evidence-runtime.ts` contains explicit process-exit or equivalent failure-path flush hooks.
- `tests/sdk-verification/coverage/fixtures/failing-evidence-fixture.test.ts` exists and contains an intentional failing assertion after at least one `recordSymbolHit(...)` call.
- `tests/sdk-verification/coverage/runtime-artifact-resilience.test.ts` exists, spawns the failing fixture in a child process, and proves the artifact came from that child run by deleting the prior artifact first or by asserting `generatedAt >= spawnStartedAt`.
- `pnpm vitest run tests/sdk-verification/coverage/runtime-artifact-resilience.test.ts && pnpm coverage:generate && pnpm drift:check` exits 0, proving downstream gates remain usable immediately after the failing child run.
  </acceptance_criteria>
  <done>The SDK evidence artifact path is resilient to failing runs, so later proof gates are runnable instead of being stranded by the first red test.</done>
</task>

</tasks>

<verification>
Run the root and twin builds, then run the artifact-resilience test. Do not continue if any of those gates are still untrustworthy.
</verification>

<success_criteria>
Phase 41 now has truthful build gates and a resilient runtime-evidence artifact path, removing the release-path lies that would otherwise invalidate later green claims.
</success_criteria>

<output>
After completion, create `.planning/phases/41-regression-closure-and-release-gate-recovery/41-02-SUMMARY.md`
</output>
