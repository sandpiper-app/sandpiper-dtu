---
phase: 41-regression-closure-and-release-gate-recovery
plan: "06"
type: execute
wave: 4
depends_on:
  - "03"
  - "04"
  - "05"
files_modified:
  - packages/conformance/src/types.ts
  - packages/conformance/src/comparator.ts
  - packages/conformance/src/reporter.ts
  - twins/shopify/conformance/normalizer.ts
  - twins/slack/conformance/normalizer.ts
  - twins/slack/conformance/index.ts
  - twins/slack/conformance/suites/chat.conformance.ts
  - twins/slack/conformance/suites/oauth.conformance.ts
  - twins/slack/conformance/suites/users.conformance.ts
  - tests/sdk-verification/coverage/proof-integrity-regression.test.ts
  - tests/sdk-verification/coverage/coverage-report.json
  - .planning/phases/41-regression-closure-and-release-gate-recovery/41-06-signoff-receipt.json
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - INFRA-19
  - INFRA-24
  - INFRA-25
must_haves:
  truths:
    - "Conformance compares the deterministic headers and values the repo claims are identical, instead of collapsing proof to shape-only smoke"
    - "Reporter and conformance index wording label structural smoke as structural smoke and exact/value-checked suites as exact/value-checked proof"
    - "Coverage artifacts are regenerated only through the runtime-symbol-execution path after the proof fixes land"
    - "Active planning docs return to `complete` only after a post-restoration full signoff run is green"
  artifacts:
    - path: "packages/conformance/src/types.ts"
      provides: "Header-allowlist support in the conformance normalizer contract"
      contains: "compareHeaders"
    - path: "packages/conformance/src/comparator.ts"
      provides: "Deterministic header and value comparison in exact and structural modes"
      contains: "compareHeaders"
    - path: "tests/sdk-verification/coverage/coverage-report.json"
      provides: "Regenerated post-fix coverage artifact using runtime symbol execution"
      contains: "\"evidenceSource\": \"runtime-symbol-execution\""
    - path: ".planning/phases/41-regression-closure-and-release-gate-recovery/41-06-signoff-receipt.json"
      provides: "Machine-readable proof that the full signoff command passed once with docs pending and again after docs were restored"
      contains: "\"preRestore\""
  key_links:
    - from: "packages/conformance/src/comparator.ts"
      to: "twins/slack/conformance/normalizer.ts"
      via: "deterministic header/value comparison is driven by explicit suite opt-ins"
      pattern: "compareHeaders|compareValueFields"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "completion claims are restored only after the post-restoration signoff is green"
      pattern: "completed"
---

<objective>
Finish the phase with truthful conformance semantics, regenerated artifacts, and an honest release signoff.

Purpose: Even after the build, Shopify, Slack, and proof-surface fixes land, the repo can still overclaim parity if conformance under-compares deterministic headers/values or if the planning docs return to complete before the final post-restoration signoff is green. This plan closes those last gaps.
Output: A green conformance proof path, regenerated artifacts, and active docs restored to completed status only after the second full signoff pass succeeds.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-CONTEXT.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-RESEARCH.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-VALIDATION.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten conformance semantics and make the remaining proof-integrity assertion green</name>
  <files>
packages/conformance/src/types.ts
packages/conformance/src/comparator.ts
packages/conformance/src/reporter.ts
twins/shopify/conformance/normalizer.ts
twins/slack/conformance/normalizer.ts
twins/slack/conformance/index.ts
twins/slack/conformance/suites/chat.conformance.ts
twins/slack/conformance/suites/oauth.conformance.ts
twins/slack/conformance/suites/users.conformance.ts
tests/sdk-verification/coverage/proof-integrity-regression.test.ts
  </files>
  <read_first>
packages/conformance/src/types.ts
packages/conformance/src/comparator.ts
packages/conformance/src/reporter.ts
twins/shopify/conformance/normalizer.ts
twins/shopify/src/plugins/rest.ts
twins/shopify/src/plugins/graphql.ts
twins/slack/conformance/normalizer.ts
twins/slack/conformance/index.ts
twins/slack/conformance/suites/chat.conformance.ts
twins/slack/conformance/suites/oauth.conformance.ts
twins/slack/conformance/suites/users.conformance.ts
twins/slack/src/plugins/web-api/chat.ts
twins/slack/src/plugins/web-api/users.ts
twins/slack/src/plugins/web-api/files.ts
tests/sdk-verification/coverage/proof-integrity-regression.test.ts
  </read_first>
  <action>
Tighten conformance semantics only where the compared seams are deterministic, and make the remaining proof-integrity regression green.

1. In `packages/conformance/src/types.ts`, add header-allowlist support to the normalizer contract with the exact field name `compareHeaders: string[]`.

2. In `packages/conformance/src/comparator.ts`:
   - in exact mode, compare lower-cased header values for `content-type` plus any lower-cased header names listed in `compareHeaders`
   - in structural mode, compare declared `compareHeaders` and existing `compareValueFields` after normalization
   - do not compare volatile headers or volatile value fields that the suites have not opted into explicitly

3. In the Shopify and Slack conformance normalizers and suites, declare deterministic proof only where it is actually safe:
   - `x-shopify-api-version` on versioned Shopify REST/GraphQL endpoints
   - `x-oauth-scopes` and `x-accepted-oauth-scopes` on deterministic Slack auth/scope responses
   - deterministic Slack `ok` and `error` value seams in the OAuth, chat, and users suites that already produce stable error paths
   Do not label nondeterministic IDs, timestamps, or rate-limit counters as exact proof.

4. Update `packages/conformance/src/reporter.ts` and `twins/slack/conformance/index.ts` so structural-only suites are labeled as structural smoke and exact/value-checked suites are labeled accordingly. Do not use 1:1 language for suites that only prove structure.

5. Update `tests/sdk-verification/coverage/proof-integrity-regression.test.ts` so the remaining `conformance comparator preserves deterministic proof headers` assertion goes green only after the comparator and suite opt-ins are in place.
  </action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/coverage/proof-integrity-regression.test.ts -t "conformance comparator preserves deterministic proof headers" && pnpm --filter @dtu/twin-shopify conformance:twin && pnpm --filter @dtu/twin-slack conformance:twin</automated>
  </verify>
  <acceptance_criteria>
- `packages/conformance/src/types.ts` contains `compareHeaders`.
- `packages/conformance/src/comparator.ts` compares lower-cased header values for `content-type` plus any requested `compareHeaders`, and structural mode also compares opted-in `compareHeaders` plus `compareValueFields`.
- `twins/shopify/conformance/normalizer.ts` and `twins/slack/conformance/normalizer.ts` opt into `x-shopify-api-version`, `x-oauth-scopes`, and `x-accepted-oauth-scopes` only on deterministic paths.
- `packages/conformance/src/reporter.ts` and `twins/slack/conformance/index.ts` no longer overstate structural smoke as full parity.
- `pnpm vitest run tests/sdk-verification/coverage/proof-integrity-regression.test.ts -t "conformance comparator preserves deterministic proof headers" && pnpm --filter @dtu/twin-shopify conformance:twin && pnpm --filter @dtu/twin-slack conformance:twin` exits 0.
  </acceptance_criteria>
  <done>The remaining proof-integrity regression is green and conformance now proves exactly the deterministic seams it claims to prove.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate artifacts and restore active docs only after the post-restoration signoff is green</name>
  <files>
tests/sdk-verification/coverage/coverage-report.json
.planning/phases/41-regression-closure-and-release-gate-recovery/41-06-signoff-receipt.json
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/STATE.md
  </files>
  <read_first>
tests/sdk-verification/coverage/coverage-report.json
tests/sdk-verification/drift/check-drift.ts
.planning/phases/41-regression-closure-and-release-gate-recovery/41-06-signoff-receipt.json
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/STATE.md
.planning/phases/41-regression-closure-and-release-gate-recovery/41-VALIDATION.md
  </read_first>
  <action>
Regenerate artifacts and restore completion claims only after the product-side proof is already green, then prove those restored docs do not invalidate the release path.

1. With the active planning docs still in the reopened/pending state, run the full signoff command from `41-VALIDATION.md` once and confirm it is green before restoring any completion claims. Write `.planning/phases/41-regression-closure-and-release-gate-recovery/41-06-signoff-receipt.json` with a `preRestore` entry containing:
   - the exact full signoff command string
   - `docsState: "pending"`
   - `completedAt` as an ISO timestamp
   - `exitCode: 0`

2. Regenerate `tests/sdk-verification/coverage/coverage-report.json` only through the normal generator path invoked by `pnpm coverage:generate`. Do not hand-edit the JSON.

3. After that first green pass, restore the active planning docs truthfully:
   - in `.planning/REQUIREMENTS.md`, change the Phase 41 reopened requirement rows back to `Complete`
   - in `.planning/ROADMAP.md`, mark Phase 41 complete and restore the milestone summary lines
   - in `.planning/STATE.md`, restore `status: completed`, the milestone-complete wording, and final progress values

4. After the doc restoration, rerun the exact same full signoff command a second time. Update `41-06-signoff-receipt.json` with a `postRestore` entry containing the same `command` string, `docsState: "completed"`, a later `completedAt` timestamp, and `exitCode: 0`. This second run is the release-signoff result that authorizes the restored completion docs. If the second run fails, do not leave the docs in a completed state.
  </action>
  <verify>
    <automated>node -e "const r=require('./.planning/phases/41-regression-closure-and-release-gate-recovery/41-06-signoff-receipt.json'); const ok=r.preRestore&&r.postRestore&&r.preRestore.exitCode===0&&r.postRestore.exitCode===0&&r.preRestore.docsState==='pending'&&r.postRestore.docsState==='completed'&&r.preRestore.command===r.postRestore.command&&Date.parse(r.preRestore.completedAt)<Date.parse(r.postRestore.completedAt); if(!ok) process.exit(1)" && pnpm build && pnpm --dir twins/shopify build && pnpm --dir twins/slack build && pnpm test:sdk --reporter=verbose --reporter=json --outputFile.json=tests/sdk-verification/coverage/vitest-evidence.json && pnpm coverage:generate && pnpm drift:check && pnpm --filter @dtu/twin-shopify conformance:twin && pnpm --filter @dtu/twin-slack conformance:twin && pnpm vitest run tests/sdk-verification/coverage/proof-integrity-regression.test.ts tests/sdk-verification/sdk/slack-method-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `tests/sdk-verification/coverage/coverage-report.json` is regenerated by the normal runtime-symbol-execution path and still contains `evidenceSource: "runtime-symbol-execution"`.
- `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` remain in the reopened/pending state until after the first full signoff pass is green.
- `.planning/phases/41-regression-closure-and-release-gate-recovery/41-06-signoff-receipt.json` exists and contains `preRestore` plus `postRestore` entries with the same command string, ordered timestamps, `docsState` transitioning from `pending` to `completed`, and `exitCode: 0` for both runs.
- The active planning docs are restored to completed status only after the first full signoff pass succeeds, and the exact same full signoff command is then run a second time with the restored docs present.
- The second full signoff run exits 0; otherwise the docs are not left in a completed state.
  </acceptance_criteria>
  <done>The repo’s final green state is now both real and truthfully described: conformance proof is bounded correctly, artifacts are regenerated through the real pipeline, and active docs only claim completion after the post-restoration signoff is green.</done>
</task>

</tasks>

<verification>
Run the conformance-focused verify command first, then execute the full signoff command twice as directed in Task 2: once while docs are still pending, once after doc restoration. Do not mark the phase complete unless the second run is green.
</verification>

<success_criteria>
Phase 41 ends with one honest green release path: deterministic conformance proof is explicit, coverage artifacts are regenerated through the real evidence path, and the active docs only claim completion after the post-restoration verification run succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/41-regression-closure-and-release-gate-recovery/41-06-SUMMARY.md`
</output>
