---
phase: 41-regression-closure-and-release-gate-recovery
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - twins/shopify/src/services/api-version.ts
  - twins/shopify/src/plugins/oauth.ts
  - twins/shopify/src/plugins/rest.ts
  - twins/shopify/src/schema/resolvers.ts
  - packages/state/src/state-manager.ts
  - tests/sdk-verification/sdk/shopify-regression-closure.test.ts
  - tests/sdk-verification/sdk/shopify-api-auth.test.ts
  - tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts
autonomous: true
requirements:
  - SHOP-17
  - SHOP-18
  - SHOP-20
must_haves:
  truths:
    - "Shopify accepts only supported quarterly versions plus unstable; impossible versions like 2025-02 are rejected"
    - "Refresh and offline token-exchange flows return response shapes that populate vendored SDK session fields correctly"
    - "Product delete, orderUpdate, and inventory-level flows all persist through the shared state layer rather than transport-local stubs"
  artifacts:
    - path: "twins/shopify/src/services/api-version.ts"
      provides: "Supported-version set derived from the vendored quarterly enum plus `unstable`"
      contains: "SUPPORTED_API_VERSIONS"
    - path: "twins/shopify/src/plugins/oauth.ts"
      provides: "Grant-specific response branches for refresh and offline token exchange"
      contains: "refresh_token_expires_in"
    - path: "tests/sdk-verification/sdk/shopify-regression-closure.test.ts"
      provides: "Green Shopify regression closure coverage"
      contains: "Product.delete() removes the product"
  key_links:
    - from: "twins/shopify/src/services/api-version.ts"
      to: "third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/types.ts"
      via: "supported version set is derived from the vendored quarterly enum"
      pattern: "ApiVersion"
    - from: "twins/shopify/src/plugins/oauth.ts"
      to: "third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/create-session.ts"
      via: "offline response fields populate Session.refreshToken and Session.refreshTokenExpires"
      pattern: "refresh_token|refresh_token_expires_in"
    - from: "twins/shopify/src/schema/resolvers.ts"
      to: "packages/state/src/state-manager.ts"
      via: "orderUpdate stores line_items exactly once"
      pattern: "line_items"
---

<objective>
Close the remaining Shopify regressions that still break the pinned SDKs and release signoff.

Purpose: The repo still accepts impossible API versions, still under-shapes refresh/offline OAuth responses, and still drifts between REST, GraphQL, and stored state for delete/order/inventory flows. This plan makes those Shopify seams match the upstream contracts the repo already vendors.
Output: A green Shopify regression suite plus a green existing `shopify-behavioral-parity` path.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-CONTEXT.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-RESEARCH.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-VALIDATION.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-01-PLAN.md
@.planning/phases/41-regression-closure-and-release-gate-recovery/41-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce supported Shopify versions and return full offline OAuth shapes</name>
  <files>
twins/shopify/src/services/api-version.ts
twins/shopify/src/plugins/oauth.ts
tests/sdk-verification/sdk/shopify-regression-closure.test.ts
tests/sdk-verification/sdk/shopify-api-auth.test.ts
  </files>
  <read_first>
twins/shopify/src/services/api-version.ts
twins/shopify/src/plugins/oauth.ts
tests/sdk-verification/sdk/shopify-regression-closure.test.ts
tests/sdk-verification/sdk/shopify-api-auth.test.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/types.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/types.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/create-session.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/__tests__/refresh-token.test.ts
third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/__tests__/token-exchange.test.ts
  </read_first>
  <action>
Fix Shopify version validation and offline OAuth response shaping with exact target states.

1. In `twins/shopify/src/services/api-version.ts`, replace the current regex-plus-month-range acceptance with a supported-version set derived from the vendored Shopify `ApiVersion` enum in `third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/types.ts`, plus `unstable` if it is represented separately. Do not hand-maintain a disconnected quarterly array. If a direct import would be inappropriate in the twin runtime, add a small local helper generated from or guarded against that vendored enum in the same file and add/extend the regression test so future quarterly enum drift fails fast. Keep the existing `SUNSET_VERSIONS` behavior for older quarters. Any non-allowlisted value such as `2025-02` must throw as invalid.

2. In `twins/shopify/src/plugins/oauth.ts`, keep the existing grant validation rules, then change response shaping as follows:
   - `grant_type === 'refresh_token'` returns `{ access_token, scope, expires_in: 525600, refresh_token: <new uuid>, refresh_token_expires_in: 2592000 }`
   - token exchange for `requested_token_type === offline-access-token` and `expiring === '1'` returns the same offline-expiring shape
   - token exchange for `requested_token_type === offline-access-token` and `expiring === '0'` returns `{ access_token, scope }`
   - token exchange for `requested_token_type === online-access-token` keeps the current online fields
   The numeric values `525600` and `2592000` must come from the vendored upstream expectations in `refresh-token.test.ts` and `token-exchange.test.ts`; cite those tests in comments only if the code would otherwise be ambiguous. Continue storing the access token in state and preserve invalid-client / invalid-request branches.

3. Update `tests/sdk-verification/sdk/shopify-api-auth.test.ts` and the Wave 0 regression file so they assert the exact session fields that the vendored SDK derives from those response bodies:
   - `session.refreshToken`
   - `session.refreshTokenExpires`
   - `session.expires` for expiring offline and online flows
  </action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/sdk/shopify-regression-closure.test.ts tests/sdk-verification/sdk/shopify-api-auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
- `twins/shopify/src/services/api-version.ts` contains the quarterly values `2025-01`, `2025-04`, `2025-07`, and `2025-10` in the supported-version logic.
- `twins/shopify/src/plugins/oauth.ts` contains `refresh_token_expires_in` and an offline token-exchange branch gated by `requested_token_type` plus `expiring === '1'`.
- `tests/sdk-verification/sdk/shopify-api-auth.test.ts` asserts `refreshToken`, `refreshTokenExpires`, and `expires` on the resulting session objects for the grant shapes described in the action text.
- `pnpm vitest run tests/sdk-verification/sdk/shopify-regression-closure.test.ts tests/sdk-verification/sdk/shopify-api-auth.test.ts` exits 0.
  </acceptance_criteria>
  <done>Shopify version handling and offline OAuth flows now match the vendored SDK contracts instead of only looking valid on the wire.</done>
</task>

<task type="auto">
  <name>Task 2: Make delete, orderUpdate, and inventory flows state-backed and green</name>
  <files>
twins/shopify/src/plugins/rest.ts
twins/shopify/src/schema/resolvers.ts
packages/state/src/state-manager.ts
tests/sdk-verification/sdk/shopify-regression-closure.test.ts
tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts
  </files>
  <read_first>
twins/shopify/src/plugins/rest.ts
twins/shopify/src/schema/resolvers.ts
packages/state/src/state-manager.ts
tests/sdk-verification/sdk/shopify-regression-closure.test.ts
tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts
tests/sdk-verification/sdk/shopify-api-rest-client.test.ts
  </read_first>
  <action>
Repair the Shopify state seams using the shared state layer, not transport-local response hacks.

1. In `packages/state/src/state-manager.ts`, add a `deleteProduct(id: number): boolean` helper that deletes one product row by numeric ID and returns whether anything was removed.

2. In `twins/shopify/src/plugins/rest.ts`:
   - make `DELETE /admin/api/:version/products/:id.json` call `stateManager.deleteProduct(numericId)` and return `404` when nothing was deleted
   - keep the success response body `{}` for an existing delete, but ensure subsequent `GET /products/:id.json` no longer returns that product
   - make `InventoryLevel.adjust`, `InventoryLevel.connect`, `InventoryLevel.set`, `InventoryLevel.delete`, `GET /inventory_levels.json`, and `GET /locations/:id/inventory_levels.json` all use the shared `inventory_levels` table methods already present in `StateManager`
   - ensure the response shape for adjust/connect/set is `{ inventory_level: { inventory_item_id, location_id, available, ... } }`, matching the pinned REST resource expectations

3. In `twins/shopify/src/schema/resolvers.ts`, remove the extra `JSON.stringify(input.lineItems)` in `orderUpdate`. Pass the raw `input.lineItems` array/object into `StateManager.updateOrder()` so the resolver passes structured data and the state layer stringifies exactly once.

4. Update the regression file and `tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts` so the existing `InventoryLevel.adjust()` assertion and the new delete/orderUpdate assertions all go green.
  </action>
  <verify>
    <automated>pnpm vitest run tests/sdk-verification/sdk/shopify-regression-closure.test.ts tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts -t "Product.delete\\(\\) removes the product|orderUpdate preserves lineItems|InventoryLevel.adjust\\(\\)" && pnpm --dir twins/slack build</automated>
  </verify>
  <acceptance_criteria>
- `packages/state/src/state-manager.ts` contains a `deleteProduct(id: number): boolean` helper.
- `twins/shopify/src/schema/resolvers.ts` no longer contains `line_items: input.lineItems ? JSON.stringify(input.lineItems)` in `orderUpdate`.
- `twins/shopify/src/plugins/rest.ts` calls the shared inventory-level state helpers for `connect.json`, `adjust.json`, `set.json`, and `DELETE /inventory_levels.json`.
- `tests/sdk-verification/setup/seeders.ts` is unchanged by this plan, preserving safe parallel execution with Plan 41-04.
- Changes to `tests/sdk-verification/sdk/shopify-api-auth.test.ts` stay local to assertions and do not alter shared helpers or shared fixtures.
- `pnpm vitest run tests/sdk-verification/sdk/shopify-regression-closure.test.ts tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts -t "Product.delete\\(\\) removes the product|orderUpdate preserves lineItems|InventoryLevel.adjust\\(\\)" && pnpm --dir twins/slack build` exits 0.
  </acceptance_criteria>
  <done>Shopify destructive/update flows now persist through the shared state layer, so the SDK sees the same truth through REST and GraphQL.</done>
</task>

</tasks>

<verification>
Run the targeted Shopify regression and behavioral-parity tests. Do not advance until the new regression file and the previously failing `InventoryLevel.adjust()` path are both green.
</verification>

<success_criteria>
The open Shopify regressions are closed with explicit upstream-compatible behavior: invalid versions are rejected, offline sessions are shaped correctly, destructive/update paths persist, and the existing pinned SDK behavioral test goes green.
</success_criteria>

<output>
After completion, create `.planning/phases/41-regression-closure-and-release-gate-recovery/41-03-SUMMARY.md`
</output>
