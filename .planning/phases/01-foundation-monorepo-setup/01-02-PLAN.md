---
phase: 01-foundation-monorepo-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/state/package.json
  - packages/state/src/state-manager.ts
  - packages/state/src/index.ts
  - packages/core/package.json
  - packages/core/src/index.ts
  - twins/example/package.json
  - twins/example/tsconfig.json
  - twins/example/src/index.ts
  - twins/example/src/plugins/health.ts
  - twins/example/src/plugins/admin.ts
  - vitest.config.ts
  - vitest.shared.ts
  - packages/state/vitest.config.ts
  - twins/example/vitest.config.ts
  - twins/example/test/integration.test.ts
autonomous: true
requirements:
  - INFRA-02
  - INFRA-07
  - INFRA-08

must_haves:
  truths:
    - "Developer can create StateManager instance and reset state in under 100ms"
    - "Example twin returns 200 from /health endpoint"
    - "Example twin logs include correlation IDs in structured JSON format"
    - "Example twin can persist and retrieve entities via StateManager"
    - "Integration tests verify health check and state reset functionality"
  artifacts:
    - path: "packages/state/src/state-manager.ts"
      provides: "StateManager implementation with SQLite"
      exports: ["StateManager"]
      min_lines: 50
    - path: "twins/example/src/index.ts"
      provides: "Fastify app entry point"
      contains: "requestIdHeader"
      min_lines: 30
    - path: "twins/example/src/plugins/health.ts"
      provides: "Health check endpoint"
      contains: "/health"
    - path: "twins/example/src/plugins/admin.ts"
      provides: "Admin reset endpoint"
      contains: "/admin/reset"
    - path: "vitest.config.ts"
      provides: "Root test configuration"
      contains: "projects"
  key_links:
    - from: "packages/state/src/state-manager.ts"
      to: "better-sqlite3"
      via: "Database import and initialization"
      pattern: "import.*better-sqlite3"
    - from: "twins/example/src/index.ts"
      to: "StateManager"
      via: "import and init()"
      pattern: "StateManager.*init"
    - from: "twins/example/src/index.ts"
      to: "Fastify requestIdHeader"
      via: "Fastify constructor options"
      pattern: "requestIdHeader.*x-request-id"
    - from: "vitest.config.ts"
      to: "packages/*, twins/*"
      via: "projects configuration"
      pattern: "projects.*packages"

user_setup:
  - service: none
    why: "All dependencies installable via pnpm, no external services required"
---

<objective>
Implement state management layer with SQLite backend and create example twin application demonstrating health checks, structured logging, and stateful endpoints.

Purpose: Validate that the monorepo foundation supports real twin development by building a minimal but complete twin that exercises all shared infrastructure. Proves StateManager works, Fastify integration is solid, and tests can verify twin behavior.

Output: Working StateManager with sub-100ms reset capability, example twin app with Fastify + Pino logging + correlation IDs, health check endpoint, admin reset endpoint, stateful entity storage, and integration tests confirming everything works end-to-end.
</objective>

<execution_context>
@/Users/futur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/futur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-monorepo-setup/01-CONTEXT.md
@.planning/phases/01-foundation-monorepo-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement StateManager in @dtu/state with SQLite and sub-100ms reset</name>
  <files>
    packages/state/package.json
    packages/state/src/state-manager.ts
    packages/state/src/index.ts
  </files>
  <action>
Implement StateManager following research Pattern 4 (better-sqlite3 in-memory state reset):

1. **Add dependencies to package.json**: `"better-sqlite3": "^12.6.2"`, devDependencies: `"@types/better-sqlite3": "^7.6.12"`

2. **Create state-manager.ts** with StateManager class:
   - Constructor accepts optional `dbPath` (defaults to `:memory:`)
   - `init()`: Creates new Database instance, sets `pragma('journal_mode = WAL')` for performance, runs migrations to create entities table
   - `reset()`: Implements drop-and-recreate pattern per user decision - calls `this.db.close()` then `this.init()` (guaranteed clean slate)
   - `close()`: Closes database connection if open, sets `this.db = null`
   - `database` getter: Returns db instance or throws if not initialized
   - Private `runMigrations()`: Executes CREATE TABLE for entities with columns: `id TEXT PRIMARY KEY`, `type TEXT NOT NULL`, `data TEXT NOT NULL`, `created_at INTEGER NOT NULL DEFAULT (unixepoch())`, `updated_at INTEGER NOT NULL DEFAULT (unixepoch())`
   - Add basic CRUD methods: `createEntity(type, data)`, `getEntity(id)`, `listEntities(type?)`, `deleteEntity(id)` using prepared statements

3. **Update index.ts**: Export StateManager class and types: `export { StateManager } from './state-manager'; export type { StateManagerOptions } from './state-manager';`

Use exact code patterns from research section "State Manager with Reset Pattern". Support both in-memory (`:memory:`) and file-based paths per Claude's discretion decision. Reset MUST complete in <100ms per requirement INFRA-02.
  </action>
  <verify>
Run `pnpm --filter @dtu/state build` - compiles with no TypeScript errors. Create quick test: instantiate StateManager with `:memory:`, call init(), createEntity(), verify getEntity() returns it, call reset(), verify entity is gone. Measure reset time with console.time() - must be <100ms.
  </verify>
  <done>
StateManager class exists in packages/state/src/state-manager.ts with init/reset/close methods, better-sqlite3 dependency added, reset uses drop-and-recreate pattern completing in <100ms, exports updated in index.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create example twin with Fastify, health check, admin reset, and stateful endpoint</name>
  <files>
    twins/example/package.json
    twins/example/tsconfig.json
    twins/example/src/index.ts
    twins/example/src/plugins/health.ts
    twins/example/src/plugins/admin.ts
  </files>
  <action>
Create minimal example twin following research Pattern 3 (Fastify plugin encapsulation) and code examples:

1. **Create twins/example/package.json**:
   - `"name": "@dtu/twin-example"`, `"version": "0.1.0"`, `"private": true`
   - dependencies: `"@dtu/core": "workspace:*"`, `"@dtu/state": "workspace:*"`, `"@dtu/types": "workspace:*"`, `"fastify": "^5.7.0"`, `"pino": "^9.6.0"`, `"better-sqlite3": "^12.6.2"`
   - devDependencies: `"@types/better-sqlite3": "^7.6.12"`, `"@types/node": "^22.13.0"`, `"typescript": "workspace:*"`, `"tsx": "^4.19.2"`, `"vitest": "workspace:*"`, `"pino-pretty": "^13.0.0"`
   - scripts: `"dev": "tsx watch src/index.ts"`, `"build": "tsc"`, `"start": "node dist/index.js"`, `"test": "vitest"`

2. **Create tsconfig.json**: Extends `../../tsconfig.base.json`, set `outDir: "./dist"`, `rootDir: "./src"`, add references to all three packages

3. **Create src/index.ts** (Fastify app entry):
   - Import Fastify, randomUUID from crypto, StateManager
   - Initialize Fastify with logger config (Pino with `pino-pretty` transport in dev mode per research), `requestIdHeader: 'x-request-id'`, `genReqId: () => randomUUID()` for correlation IDs (INFRA-08)
   - Create StateManager instance, call init()
   - Decorate Fastify instance with `stateManager` using `fastify.decorate('stateManager', stateManager)`
   - Register health plugin, admin plugin
   - Add stateful endpoint: `GET /api/entities/:id` that uses `request.params.id`, logs with `request.log.info({ entityId: id })`, calls `stateManager.getEntity(id)`, returns entity or 404
   - Add `POST /api/entities` that accepts `{ type, data }` body, calls `stateManager.createEntity()`, returns created entity
   - Listen on port from `process.env.PORT || 3000`, host `0.0.0.0`
   - Add graceful shutdown: `process.on('SIGTERM', async () => { await fastify.close(); stateManager.close(); })`

4. **Create src/plugins/health.ts**:
   - Export FastifyPluginAsync that registers `GET /health` route
   - Return `{ status: 'ok', uptime: process.uptime() }` (INFRA-07)
   - Log health check with `request.log.info('health check')`

5. **Create src/plugins/admin.ts**:
   - Export FastifyPluginAsync that registers `POST /admin/reset` route
   - Access stateManager via `fastify.stateManager` (from decorator)
   - Call `stateManager.reset()`, log reset event, return `{ reset: true, timestamp: Date.now() }`

Use exact patterns from research "Fastify App with Correlation IDs and Health Check". Follow Fastify plugin encapsulation pattern per user decision. Do NOT use fastify-plugin wrapper (these plugins don't need global scope).
  </action>
  <verify>
Run `pnpm --filter @dtu/twin-example dev` - app starts on port 3000. Test endpoints: `curl http://localhost:3000/health` returns 200 with `{"status":"ok"}`, `curl -X POST http://localhost:3000/admin/reset` returns 200, `curl -X POST http://localhost:3000/api/entities -H "Content-Type: application/json" -d '{"type":"test","data":{"foo":"bar"}}'` creates entity and returns it. Check logs include `x-request-id` in JSON output.
  </verify>
  <done>
Example twin app exists in twins/example/ with Fastify server, health check endpoint returning 200, admin reset endpoint calling StateManager.reset(), stateful entity CRUD endpoints, structured JSON logging with correlation IDs via Pino and requestIdHeader configuration
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Vitest configuration and integration tests</name>
  <files>
    vitest.config.ts
    vitest.shared.ts
    packages/state/vitest.config.ts
    twins/example/vitest.config.ts
    twins/example/test/integration.test.ts
  </files>
  <action>
Set up Vitest for monorepo using research Pattern 5 (Vitest projects configuration):

1. **Create vitest.shared.ts** (base config):
   - Define base config with `test.globals: false`, `environment: 'node'`, `coverage` settings (provider: v8, reporters: text/json/html, exclude node_modules/dist)

2. **Create vitest.config.ts** (root):
   - Define projects: `['packages/*', 'twins/*']` (Vitest 4.x projects pattern per research, NOT deprecated workspace config)

3. **Create packages/state/vitest.config.ts**:
   - Merge with vitest.shared.ts using `mergeConfig` and `defineProject`
   - Set `test.name: '@dtu/state'`

4. **Create twins/example/vitest.config.ts**:
   - Same pattern, `test.name: '@dtu/twin-example'`

5. **Create twins/example/test/integration.test.ts**:
   - Import `describe`, `it`, `expect`, `beforeEach`, `afterEach` from vitest
   - Import Fastify and create app instance in beforeEach, close in afterEach
   - Test: "GET /health returns 200" - call `app.inject({ method: 'GET', url: '/health' })`, assert status 200 and body contains `status: 'ok'`
   - Test: "POST /admin/reset resets state" - create entity, call reset endpoint, verify entity is gone
   - Test: "POST /api/entities creates entity" - create entity, verify response has id and data
   - Test: "GET /api/entities/:id retrieves entity" - create entity, get by id, verify data matches
   - Test: "logs include correlation IDs" - make request with X-Request-Id header, verify logs contain that ID (may need to capture log output)

Use research patterns for Vitest monorepo. Tests verify INFRA-02 (state reset), INFRA-07 (health check), INFRA-08 (correlation IDs). Use Fastify's `inject()` method for HTTP testing (no need to start server).
  </action>
  <verify>
Run `pnpm test` from root - Vitest discovers both @dtu/state and @dtu/twin-example projects, runs all tests, all integration tests pass. Run `pnpm --filter @dtu/twin-example test` - runs only example twin tests.
  </verify>
  <done>
Vitest configuration exists at root with projects targeting packages and twins, shared config provides common test settings, integration tests verify health check returns 200, state reset works, entity CRUD functions correctly, all tests pass
  </done>
</task>

</tasks>

<verification>
After task completion:

1. **State management**: Instantiate StateManager, create 100 entities, call reset(), measure time - must complete in <100ms
2. **Health check**: Start example twin, curl `/health` - returns 200 with `{"status":"ok","uptime":N}`
3. **Admin reset**: Create entities via API, call `/admin/reset`, query entities - should be empty
4. **Correlation IDs**: Make request with `X-Request-Id: test-123` header, check logs - should include `"reqId":"test-123"` in JSON output
5. **Stateful endpoints**: POST entity, GET entity by ID, verify data round-trips correctly
6. **Integration tests**: Run `pnpm test` - all tests pass including health check, state reset, entity CRUD tests
7. **Build validation**: Run `pnpm build` from root - all packages including example twin compile successfully

All INFRA requirements (01, 02, 07, 08, 09) addressed. Foundation validated end-to-end.
</verification>

<success_criteria>
- StateManager implemented with better-sqlite3, supports :memory: and file-based backends
- StateManager.reset() completes in <100ms using drop-and-recreate pattern
- Example twin app starts via `pnpm --filter @dtu/twin-example dev`
- GET /health returns 200 with status and uptime (INFRA-07)
- POST /admin/reset calls StateManager.reset() and returns success
- Fastify logs are structured JSON with correlation IDs via Pino + requestIdHeader (INFRA-08)
- Example twin can create and retrieve entities via stateful endpoints
- Vitest projects configuration discovers all workspace packages
- Integration tests verify health check, state reset, entity CRUD, correlation IDs
- All tests pass via `pnpm test` from root
- Foundation demonstrates DTU methodology: behavioral clone pattern ready for replication in Phase 2 (INFRA-09)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-monorepo-setup/01-02-SUMMARY.md` documenting:
- StateManager implementation approach (better-sqlite3, reset pattern, performance measurements)
- Example twin architecture (Fastify plugins, route organization, logging configuration)
- Vitest setup (projects config, test patterns, coverage configuration)
- Decisions made in "Claude's Discretion" areas (state interaction pattern, route organization, SQLite backend strategy, internal twin structure)
- Performance validation (state reset timing, health check response time)
- Files created and their purposes
- Patterns established for Phase 2 to follow when building Shopify twin
</output>
