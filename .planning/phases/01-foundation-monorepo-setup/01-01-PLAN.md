---
phase: 01-foundation-monorepo-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - packages/types/package.json
  - packages/types/tsconfig.json
  - packages/types/src/index.ts
  - packages/state/package.json
  - packages/state/tsconfig.json
  - packages/state/src/index.ts
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - .gitignore
autonomous: true
requirements:
  - INFRA-01
  - INFRA-09

must_haves:
  truths:
    - "Developer can run pnpm install and all shared packages resolve dependencies"
    - "TypeScript project references enable incremental builds via tsc --build"
    - "Shared packages export stub types and interfaces"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace configuration"
      contains: "packages/*"
    - path: "package.json"
      provides: "Root package scripts"
      min_lines: 10
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript configuration"
      contains: "composite"
    - path: "packages/types/src/index.ts"
      provides: "Shared type definitions"
      exports: ["types"]
    - path: "packages/state/src/index.ts"
      provides: "State management exports"
      exports: ["StateManager"]
    - path: "packages/core/src/index.ts"
      provides: "Core utilities exports"
      exports: ["utilities"]
  key_links:
    - from: "packages/state/package.json"
      to: "@dtu/types"
      via: "workspace protocol dependency"
      pattern: "workspace:\\*"
    - from: "packages/core/package.json"
      to: "@dtu/types, @dtu/state"
      via: "workspace protocol dependencies"
      pattern: "workspace:\\*"
    - from: "packages/*/tsconfig.json"
      to: "../../tsconfig.base.json"
      via: "extends"
      pattern: "\"extends\".*tsconfig\\.base\\.json"
---

<objective>
Initialize monorepo structure with pnpm workspaces, TypeScript project references, and shared package foundations.

Purpose: Establish the architectural foundation for all twin development following pnpm workspace + TypeScript composite project best practices. This creates the skeleton that Phase 2+ will build upon.

Output: Working monorepo with three shared packages (@dtu/types, @dtu/state, @dtu/core) that build successfully via tsc --build, with proper workspace dependencies and incremental compilation support.
</objective>

<execution_context>
@/Users/futur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/futur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-monorepo-setup/01-CONTEXT.md
@.planning/phases/01-foundation-monorepo-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize monorepo structure with pnpm workspaces</name>
  <files>
    pnpm-workspace.yaml
    package.json
    tsconfig.base.json
    .gitignore
  </files>
  <action>
Create root pnpm workspace configuration:

1. **pnpm-workspace.yaml**: Define workspace packages targeting `packages/*` and `twins/*` directories

2. **package.json** (root):
   - Set `"private": true` (monorepo root not publishable)
   - Add scripts: `"build": "pnpm -r --filter='./packages/*' run build"`, `"test": "vitest run"`, `"lint": "pnpm -r run lint"`, `"clean": "pnpm -r run clean && rm -rf node_modules"`
   - Add devDependencies: `typescript@^5.7.3`, `vitest@^4.0.17`, `@types/node@^22.13.0`
   - Do NOT add Turborepo yet (defer until build times exceed 30s per research recommendations)

3. **tsconfig.base.json**: Shared TypeScript configuration with:
   - `target: "ES2022"`, `module: "ES2022"`, `moduleResolution: "bundler"`
   - `strict: true`, `esModuleInterop: true`, `skipLibCheck: true`
   - Path aliases: `"@dtu/types": ["./packages/types/src"]`, `"@dtu/core": ["./packages/core/src"]`, `"@dtu/state": ["./packages/state/src"]`

4. **.gitignore**: Add `node_modules/`, `dist/`, `*.tsbuildinfo`, `.env`, `*.log`, `coverage/`

Use pnpm v10.x patterns per research. Follow workspace protocol (`workspace:*`) for all inter-package dependencies established in research.
  </action>
  <verify>
Run `pnpm install` from project root - should succeed with no errors and create node_modules/.pnpm directory structure
  </verify>
  <done>
pnpm workspace configuration exists, root package.json has build/test/lint scripts, tsconfig.base.json provides shared compiler options, .gitignore excludes generated artifacts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared packages with stub exports and TypeScript project references</name>
  <files>
    packages/types/package.json
    packages/types/tsconfig.json
    packages/types/src/index.ts
    packages/state/package.json
    packages/state/tsconfig.json
    packages/state/src/index.ts
    packages/core/package.json
    packages/core/tsconfig.json
    packages/core/src/index.ts
  </files>
  <action>
Create three shared packages following TypeScript composite project pattern from research:

**@dtu/types** (no dependencies):
- package.json: `"name": "@dtu/types"`, `"version": "0.1.0"`, `"main": "./dist/index.js"`, `"types": "./dist/index.d.ts"`, scripts: `"build": "tsc --build"`, `"clean": "rm -rf dist *.tsbuildinfo"`, devDependencies: `"typescript": "workspace:*"`
- tsconfig.json: extends `../../tsconfig.base.json`, enable `composite: true`, `declaration: true`, `declarationMap: true`, `outDir: "./dist"`, `rootDir: "./src"`, include `["src/**/*"]`
- src/index.ts: Export stub types: `export interface TwinState { id: string; data: unknown; }`, `export type ResetMode = 'drop-and-recreate' | 'truncate';`

**@dtu/state** (depends on @dtu/types):
- package.json: Same pattern, add dependencies: `"@dtu/types": "workspace:*"`
- tsconfig.json: Same pattern, add `"references": [{ "path": "../types" }]`
- src/index.ts: Export stub: `export interface StateManager { init(): void; reset(): void; close(): void; }` (actual implementation in Plan 02)

**@dtu/core** (depends on @dtu/types, @dtu/state):
- package.json: Same pattern, add dependencies: `"@dtu/types": "workspace:*"`, `"@dtu/state": "workspace:*"`
- tsconfig.json: Same pattern, add `"references": [{ "path": "../types" }, { "path": "../state" }]`
- src/index.ts: Export stub: `export const CORE_VERSION = '0.1.0';` (Fastify utilities added in Plan 02)

Use exact patterns from research Pattern 1 (workspace protocol) and Pattern 2 (TypeScript project references with composite). All packages use `workspace:*` for internal dependencies per research anti-patterns (prevents phantom dependencies).
  </action>
  <verify>
Run `pnpm install` followed by `pnpm build` from root - all three packages should compile successfully with no TypeScript errors, producing dist/ directories with .js and .d.ts files
  </verify>
  <done>
Three shared packages exist with valid package.json files using workspace protocol, tsconfig.json files with composite mode and project references, stub exports that compile successfully via tsc --build
  </done>
</task>

</tasks>

<verification>
After task completion:

1. **Workspace structure**: Run `pnpm -r list` - should show @dtu/types, @dtu/state, @dtu/core packages
2. **Build orchestration**: Run `pnpm build` - should compile packages in dependency order (types → state/core)
3. **TypeScript validation**: Run `npx tsc --build --dry` - should show build order and no errors
4. **Dependency graph**: Check package.json files use `workspace:*` for all @dtu/* dependencies
5. **Incremental builds**: Touch `packages/types/src/index.ts`, run `pnpm build` - should only rebuild types and downstream packages

All checks must pass before proceeding to Plan 02.
</verification>

<success_criteria>
- pnpm workspace with packages/ directory structure exists
- Root package.json has build/test/lint scripts and required devDependencies
- tsconfig.base.json provides shared TypeScript configuration with path aliases
- Three shared packages (@dtu/types, @dtu/state, @dtu/core) build successfully
- TypeScript project references create correct dependency chain (types → state, types → core)
- Running `pnpm build` completes without errors and produces dist/ artifacts
- Workspace protocol dependencies prevent phantom dependency issues
- Foundation ready for Plan 02 to implement StateManager and example twin
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-monorepo-setup/01-01-SUMMARY.md` documenting:
- Monorepo structure created (workspace layout, package organization)
- TypeScript configuration (composite mode, project references, path aliases)
- Build system (pnpm scripts, incremental compilation)
- Any decisions made in areas marked "Claude's Discretion"
- Files created and their purposes
</output>
