---
phase: 31-slack-oauth-method-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/sdk-verification/sdk/slack-method-coverage.test.ts
  - tests/sdk-verification/coverage/generate-report-evidence.ts
autonomous: true
requirements:
  - SLCK-14

must_haves:
  truths:
    - "slack-method-coverage.test.ts calls one representative method from every registered family including slackLists, rtm, and entity"
    - "pnpm test:sdk -- tests/sdk-verification/sdk/slack-method-coverage.test.ts exits 0 with all tests green"
    - "EVIDENCE_MAP in generate-report-evidence.ts contains entries for Phase 25 admin/canvases/openid/stars/workflows/slackLists/rtm/entity symbol families attributed to slack-method-coverage.test.ts"
    - "pnpm coverage:generate reports an increased live count after the EVIDENCE_MAP additions"
  artifacts:
    - path: "tests/sdk-verification/sdk/slack-method-coverage.test.ts"
      provides: "Smoke tests for all 9 registered WebClient method families"
      contains: "slackLists.create"
    - path: "tests/sdk-verification/coverage/generate-report-evidence.ts"
      provides: "EVIDENCE_MAP with Phase 25/26 test file attributions for new symbol families"
      contains: "slack-method-coverage.test.ts"
  key_links:
    - from: "tests/sdk-verification/sdk/slack-method-coverage.test.ts"
      to: "twins/slack/src/plugins/web-api/new-families.ts"
      via: "WebClient.slackLists.create, rtm.connect, entity.presentDetails HTTP calls"
      pattern: "client\\.slackLists|client\\.rtm|client\\.entity"
    - from: "tests/sdk-verification/coverage/generate-report-evidence.ts"
      to: "tools/sdk-surface/manifests/slack-web-api@7.14.1.json"
      via: "EVIDENCE_MAP key format WebClient.{member} matching manifest symbols"
      pattern: "slack-method-coverage\\.test\\.ts"
---

<objective>
Expand slack-method-coverage.test.ts to cover the three remaining unrepresented WebClient method families (slackLists, rtm, entity) and add Phase 25/26 test file attributions to EVIDENCE_MAP in generate-report-evidence.ts.

Purpose: SLCK-14 requires all bound WebClient methods to be callable. The existing test file covers 6 of 9 families. This plan closes the gap and makes Phase 25/26 test execution visible to the coverage report.
Output: ~19-test slack-method-coverage.test.ts (was 16) + enriched EVIDENCE_MAP with 19+ new symbol entries for admin/canvases/openid/stars/workflows/slackLists/rtm/entity families.
</objective>

<execution_context>
@/Users/futur/.claude/get-shit-done/workflows/execute-plan.md
@/Users/futur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-slack-oauth-method-coverage/31-RESEARCH.md
</context>

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From tests/sdk-verification/sdk/slack-method-coverage.test.ts (existing test structure):
```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { createSlackClient } from '../helpers/slack-client.js';
import { resetSlack, seedSlackBotToken } from '../setup/seeders.js';

describe('SLCK-14: All bound WebClient methods return ok:true', () => {
  let token: string;
  beforeEach(async () => {
    await resetSlack();
    token = await seedSlackBotToken();
  });

  // Pattern: one representative method per family
  it('slackLists.create returns ok:true', async () => {
    const client = createSlackClient(token);
    await expect(client.slackLists.create({})).resolves.toMatchObject({ ok: true });
  });
});
```

From twins/slack/src/plugins/web-api/new-families.ts (registered routes):
- slackLists.create: POST /api/slackLists.create (returns { ok: true, list: { id: 'SL_STUB' } })
- slackLists.update: POST /api/slackLists.update
- rtm.connect: POST /api/rtm.connect (returns { ok: true, url: 'wss://twin.rtm.stub', self: { id: 'U_BOT_TWIN' } })
- rtm.start: POST /api/rtm.start
- entity.presentDetails: POST /api/entity.presentDetails (returns { ok: true, entity: {} })

Manifest members for these families (from slack-web-api@7.14.1.json):
- slackLists: 12 members — slackLists.access.delete, slackLists.access.set, slackLists.create, slackLists.download.get, slackLists.download.start, slackLists.items.create, slackLists.items.delete, slackLists.items.deleteMultiple, slackLists.items.info, slackLists.items.list, slackLists.items.update, slackLists.update
- rtm: 2 members — rtm.connect, rtm.start
- entity: 1 member — entity.presentDetails

EVIDENCE_MAP key format (from generate-report-evidence.ts):
- Top-level symbol: '@slack/web-api@7.14.1/WebClient'
- Member symbols: '@slack/web-api@7.14.1/WebClient.{member}'
  e.g. '@slack/web-api@7.14.1/WebClient.admin.users.list': 'sdk/slack-method-coverage.test.ts'

Existing EVIDENCE_MAP already covers: admin, workflows, canvases, openid, stars (from Phase 25 research), but these are NOT yet in the actual file. Only Phase 18 symbols are in the current EVIDENCE_MAP. All 19 new entries (admin 6 + workflows 3 + canvases 2 + openid 2 + stars 3 + slackLists 1 + rtm 1 + entity 1) must be added.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add slackLists, rtm, entity smoke tests to slack-method-coverage.test.ts</name>
  <files>tests/sdk-verification/sdk/slack-method-coverage.test.ts</files>
  <action>
Append three new test sections to the existing describe block in slack-method-coverage.test.ts. Add them after the stars.remove test (line 151). The file currently ends at line 152.

Add the following sections inside the existing `describe('SLCK-14: All bound WebClient methods return ok:true', ...)` block, before the closing `});`:

```typescript
  // -------------------------------------------------------------------------
  // slackLists.* family (12 methods) — representative: slackLists.create
  // -------------------------------------------------------------------------

  it('slackLists.create returns ok:true', async () => {
    const client = createSlackClient(token);
    await expect(client.slackLists.create({})).resolves.toMatchObject({ ok: true });
  });

  // -------------------------------------------------------------------------
  // rtm.* family (2 methods)
  // -------------------------------------------------------------------------

  it('rtm.connect returns ok:true', async () => {
    const client = createSlackClient(token);
    await expect(client.rtm.connect({})).resolves.toMatchObject({ ok: true });
  });

  // -------------------------------------------------------------------------
  // entity.* family (1 method)
  // -------------------------------------------------------------------------

  it('entity.presentDetails returns ok:true', async () => {
    const client = createSlackClient(token);
    await expect(client.entity.presentDetails({})).resolves.toMatchObject({ ok: true });
  });
```

Note: seedSlackBotToken() seeds a broad-scope token via allScopesString() which includes all methods in METHOD_SCOPES. The new-families stub() handler calls checkScope() and new-families routes are registered in METHOD_SCOPES. Verify that slackLists.create, rtm.connect, and entity.presentDetails appear in method-scopes.ts before writing tests — if they are missing from METHOD_SCOPES, the test will fail with missing_scope. Check with grep first.
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- tests/sdk-verification/sdk/slack-method-coverage.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>slack-method-coverage.test.ts has 19 tests (was 16), all green. slackLists.create, rtm.connect, and entity.presentDetails each resolve with { ok: true }.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 25/26 symbol attributions to EVIDENCE_MAP in generate-report-evidence.ts</name>
  <files>tests/sdk-verification/coverage/generate-report-evidence.ts</files>
  <action>
Add two new comment-delimited sections to EVIDENCE_MAP in generate-report-evidence.ts. Insert them after the last existing entry (line ~290, after the Phase 20 AwsLambdaReceiver entries, before the closing `};`).

Section 1 — Phase 25 SLCK-14 (slack-method-coverage.test.ts):
```typescript
  // Phase 25: SLCK-14 — slack-method-coverage.test.ts
  '@slack/web-api@7.14.1/WebClient.admin.users.list': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.admin.conversations.search': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.admin.teams.list': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.admin.apps.approved.list': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.admin.users.invite': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.admin.conversations.create': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.workflows.stepCompleted': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.workflows.stepFailed': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.workflows.updateStep': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.canvases.create': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.canvases.delete': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.openid.connect.token': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.openid.connect.userInfo': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.stars.list': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.stars.add': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.stars.remove': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.slackLists.create': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.rtm.connect': 'sdk/slack-method-coverage.test.ts',
  '@slack/web-api@7.14.1/WebClient.entity.presentDetails': 'sdk/slack-method-coverage.test.ts',
```

Section 2 — Phase 26 SLCK-18/19 (slack-scope-enforcement.test.ts):
The scope enforcement test exercises chat.postMessage, conversations.list, users.list — already attributed to primary test files. Only add a new entry if `oauth.v2.access` appears in the manifest as a WebClient member. Confirmed from manifest inspection: `WebClient` members include `oauth.v2.access` — add it:
```typescript
  // Phase 26: SLCK-18 — slack-scope-enforcement.test.ts
  '@slack/web-api@7.14.1/WebClient.oauth.v2.access': 'sdk/slack-scope-enforcement.test.ts',
```

IMPORTANT before adding oauth.v2.access entry: verify it appears in the manifest with:
```bash
python3 -c "import json; d=json.load(open('tools/sdk-surface/manifests/slack-web-api@7.14.1.json')); wc=d['symbols']['WebClient']; print([m for m in wc.get('members',[]) if 'oauth' in m])"
```
Only add the entry if `oauth.v2.access` is in the output. If absent, skip it.
  </action>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && node tests/sdk-verification/coverage/generate-report-evidence.ts 2>&1 | grep -E "live|deferred|Summary"</automated>
  </verify>
  <done>pnpm coverage:generate (or direct node invocation) shows increased live count compared to pre-Phase-31 baseline (was ≤202 from Phase 27). The 19+ new EVIDENCE_MAP entries are present in the file. No TypeScript duplicate-key errors.</done>
</task>

</tasks>

<verification>
Full test suite must remain green after these changes:
- pnpm test:sdk exits 0 with all Slack tests passing
- pnpm coverage:generate shows live count increased by at least 15 symbols (the Phase 25 families)
- No duplicate EVIDENCE_MAP keys (TypeScript would error at compile time)
</verification>

<success_criteria>
1. slack-method-coverage.test.ts has 19 tests covering all 9 registered WebClient method families (admin, workflows, canvases, openid, stars, slackLists, rtm, entity + existing families)
2. All 19 tests resolve with { ok: true } — no 404s, no missing_scope errors
3. generate-report-evidence.ts EVIDENCE_MAP contains ≥19 new entries attributed to slack-method-coverage.test.ts for Phase 25 symbols
4. pnpm coverage:generate shows live count increased from pre-Phase-31 baseline
5. pnpm test:sdk (full suite) exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-slack-oauth-method-coverage/31-01-SUMMARY.md` following the summary template.
</output>
