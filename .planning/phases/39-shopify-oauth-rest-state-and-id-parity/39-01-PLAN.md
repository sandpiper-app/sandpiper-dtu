---
phase: 39-shopify-oauth-rest-state-and-id-parity
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/sdk-verification/sdk/shopify-api-auth.test.ts
  - tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts
  - tests/sdk-verification/sdk/shopify-app-framework-auth-smoke.test.ts
  - twins/shopify/test/integration/inventory-collection-state.test.ts
autonomous: true
requirements:
  - SHOP-14
  - SHOP-15
  - SHOP-16
  - SHOP-17

must_haves:
  truths:
    - "Wave 0 proves grant-specific OAuth validation gaps with failing or newly explicit tests instead of relying on green-but-loose auth behavior"
    - "Wave 0 proves order/customer GraphQL-to-REST ID parity, REST write/filter persistence, inventory-level state, and collection filtering are all observable gaps"
    - "SHOP-16 stays smoke-level: auth/admin seam coverage exists without introducing real shopify-app-express, shopify-app-remix, or shopify-app-react-router packages"
    - "Phase 39 execution can proceed against concrete failing contracts instead of inferred defects"
  artifacts:
    - path: "tests/sdk-verification/sdk/shopify-api-auth.test.ts"
      provides: "Grant-specific OAuth RED cases for client_credentials, refresh_token, and token-exchange request bodies"
      contains: "Phase 39: grant-specific OAuth validation"
    - path: "tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts"
      provides: "SDK-facing parity tests for GID round-trip, REST write persistence, ids filters, inventory levels, and collection filters"
      contains: "Product.all({ collection_id })"
    - path: "tests/sdk-verification/sdk/shopify-app-framework-auth-smoke.test.ts"
      provides: "Smoke-only framework-readiness coverage for begin/callback/tokenExchange plus one admin call"
      contains: "auth.begin -> authorize -> auth.callback"
    - path: "twins/shopify/test/integration/inventory-collection-state.test.ts"
      provides: "Route-level inventory and collection state tests including reset coverage"
      contains: "POST /admin/reset clears inventory_levels, custom_collections, and collects"
  key_links:
    - from: "tests/sdk-verification/sdk/shopify-api-auth.test.ts"
      to: "twins/shopify/src/plugins/oauth.ts"
      via: "POST /admin/oauth/access_token grant-specific validation"
      pattern: "client_credentials|refresh_token|requested_token_type"
    - from: "tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts"
      to: "twins/shopify/src/plugins/rest.ts"
      via: "REST resource class parity checks against versioned Admin routes"
      pattern: "customers.json|orders.json|inventory_levels|collection_id"
    - from: "tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts"
      to: "twins/shopify/src/schema/resolvers.ts"
      via: "GraphQL create -> REST numeric lookup round-trip"
      pattern: "customerCreate|orderCreate"
    - from: "twins/shopify/test/integration/inventory-collection-state.test.ts"
      to: "packages/state/src/state-manager.ts"
      via: "stored inventory level and collection membership rows survive across requests and clear on reset"
      pattern: "inventory_levels|custom_collections|collects"
---

<objective>
Establish the Phase 39 execution contract before implementation: add Wave 0 tests that explicitly prove the remaining Shopify gaps around OAuth request validation, GraphQL-to-REST ID parity, REST persistence, inventory state, and collection membership.

Purpose: Phase 39 is a fidelity cleanup phase. The twin is already broadly green, so execution needs precise failing contracts to keep the work narrow and prevent a SHOP-16 framework rollout from expanding beyond smoke coverage.
Output: Updated `shopify-api-auth.test.ts` plus three new test files that downstream plans will turn green.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-RESEARCH.md
@.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-VALIDATION.md
@.planning/phases/23-shopify-oauth-storefront/23-01-SUMMARY.md
@.planning/phases/24-shopify-rest-persistence-billing-state-machine-rate-limiting/24-02-SUMMARY.md
@.planning/phases/36-shopify-behavioral-parity/36-01-SUMMARY.md
@.planning/phases/36-shopify-behavioral-parity/36-03-SUMMARY.md
@.planning/phases/36-shopify-behavioral-parity/36-04-SUMMARY.md
</context>

<interfaces>
From `tests/sdk-verification/helpers/shopify-api-client.ts`:
```typescript
export function createShopifyApiClient<Resources extends ShopifyRestResources = ShopifyRestResources>(options?: {
  billing?: BillingConfig;
  isEmbeddedApp?: boolean;
  scopes?: string[];
  restResources?: Resources;
});

export async function mintSessionToken(apiKey: string, apiSecretKey: string): Promise<string>;
```

From `tests/sdk-verification/setup/seeders.ts`:
```typescript
export async function resetShopify(): Promise<void>;
export async function seedShopifyAccessToken(): Promise<string>;
```

From vendored Shopify auth contracts:
```typescript
// token-exchange.ts
{
  client_id,
  client_secret,
  grant_type: 'urn:ietf:params:oauth:grant-type:token-exchange',
  subject_token,
  subject_token_type: 'urn:ietf:params:oauth:token-type:id_token',
  requested_token_type,
  expiring,
}

// client-credentials.ts
{ client_id, client_secret, grant_type: 'client_credentials' }

// refresh-token.ts
{ client_id, client_secret, refresh_token, grant_type: 'refresh_token' }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 39 Wave 0 OAuth, REST-state, and framework-readiness tests</name>
  <read_first>
    tests/sdk-verification/sdk/shopify-api-auth.test.ts
    tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts
    tests/sdk-verification/sdk/shopify-api-rest-client.test.ts
    tests/sdk-verification/helpers/shopify-api-client.ts
    tests/sdk-verification/setup/seeders.ts
    twins/shopify/src/plugins/oauth.ts
    twins/shopify/src/plugins/rest.ts
    twins/shopify/src/schema/resolvers.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/token-exchange.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/client-credentials.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/lib/auth/oauth/refresh-token.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/customer.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/order.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/product.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/inventory_level.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/collect.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/custom_collection.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2023-07/location.ts
  </read_first>
  <files>tests/sdk-verification/sdk/shopify-api-auth.test.ts, tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts, tests/sdk-verification/sdk/shopify-app-framework-auth-smoke.test.ts, twins/shopify/test/integration/inventory-collection-state.test.ts</files>
  <action>
    Extend and create the Phase 39 Wave 0 contract files without touching production code.

    1. In `tests/sdk-verification/sdk/shopify-api-auth.test.ts`, add a new `describe('Phase 39: grant-specific OAuth validation', ...)` block with these exact negative tests against `POST /admin/oauth/access_token`:
    - `returns 400 for client_credentials grant when client_secret is missing`
    - `returns 400 for refresh_token grant when refresh_token is missing`
    - `returns 400 for token exchange grant when subject_token is missing`
    - `returns 400 for token exchange grant when subject_token_type is missing`
    - `returns 400 for token exchange grant when requested_token_type is missing`
    - `returns 400 for token exchange grant when requested_token_type is unsupported`

    Use these exact request bodies:
    - `grant_type: 'client_credentials'`
    - `grant_type: 'refresh_token'`
    - `grant_type: 'urn:ietf:params:oauth:grant-type:token-exchange'`
    - `subject_token_type: 'urn:ietf:params:oauth:token-type:id_token'`
    - unsupported `requested_token_type: 'urn:shopify:params:oauth:token-type:bogus'`

    Keep the existing happy-path tests for `clientCredentials`, `refreshToken`, `tokenExchange`, `begin`, and `callback` intact.

    2. Create `tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts` using `createShopifyApiClient({ restResources })`, `resetShopify()`, `seedShopifyAccessToken()`, and raw `fetch()` only where the SDK resource path is awkward. Add Wave 0 tests for these exact seams:
    - `customerCreate via GraphQL is findable via GET /customers/:id.json`
    - `orderCreate via GraphQL is findable via GET /orders/:id.json`
    - `Product.save() persists title through PUT /admin/api/2024-01/products/:id.json`
    - `Customer.save() persists first_name and last_name through PUT /admin/api/2024-01/customers/:id.json`
    - `Order.save() persists name and total_price through PUT /admin/api/2024-01/orders/:id.json`
    - `Customer.all({ ids }) returns only the requested numeric ids`
    - `Order.all({ ids }) returns only the requested numeric ids`
    - `InventoryLevel.adjust/connect/set/delete round-trips through stored inventory_levels state`
    - `Location.inventory_levels(id=1) returns the connected inventory_levels row`
    - `Product.all({ collection_id }) returns only products linked by Collect rows`

    Import `restResources` from `@shopify/shopify-api/rest/admin/2024-01`; do not use individual class imports.

    3. Create `tests/sdk-verification/sdk/shopify-app-framework-auth-smoke.test.ts` as a smoke-only file. It must use only `createShopifyApiClient`, `shopify.auth.begin`, `shopify.auth.callback`, `shopify.auth.tokenExchange`, and one real admin client call. Add these exact tests:
    - `auth.begin -> authorize -> auth.callback produces a session that can GET /admin/api/2025-01/products.json`
    - `auth.tokenExchange -> admin Graphql client can request shop { name }`

    This file must not import `shopify-app-express`, `shopify-app-remix`, or `shopify-app-react-router`.

    4. Create `twins/shopify/test/integration/inventory-collection-state.test.ts` with route-level tests that prove the current stubs are wrong:
    - `POST /admin/api/2024-01/inventory_levels/connect.json`, `adjust.json`, and `set.json` change the same inventory row
    - `GET /admin/api/2024-01/locations/1/inventory_levels.json` and `GET /admin/api/2024-01/inventory_levels.json?inventory_item_ids=<id>&location_ids=1` both surface that row
    - `DELETE /admin/api/2024-01/inventory_levels.json?inventory_item_id=<id>&location_id=1` removes that row
    - `POST /admin/api/2024-01/custom_collections.json` plus `POST /admin/api/2024-01/collects.json` makes `GET /admin/api/2024-01/products.json?collection_id=<collectionId>` return only linked products
    - `POST /admin/reset` clears `inventory_levels`, `custom_collections`, and `collects`

    The Wave 0 goal is explicit coverage. It is acceptable for some new tests to be RED and for the smoke-only SHOP-16 guards to stay green.
    Do not change production code or mutate expected-RED assertions just to make this plan's verification pass; later plans are responsible for turning the RED contracts green.
  </action>
  <acceptance_criteria>
    - `rg -n "Phase 39: grant-specific OAuth validation|client_credentials grant when client_secret is missing|requested_token_type is unsupported" tests/sdk-verification/sdk/shopify-api-auth.test.ts` returns the new OAuth Wave 0 block and test names.
    - `rg -n "customerCreate via GraphQL is findable|Order.save\\(\\) persists|InventoryLevel.adjust/connect/set/delete|Product.all\\(\\{ collection_id \\}\\)" tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts` returns the REST-state parity tests for both the 39-03 and 39-04 seams.
    - `rg -n "auth.begin -> authorize -> auth.callback|auth.tokenExchange -> admin Graphql client can request shop \\{ name \\}" tests/sdk-verification/sdk/shopify-app-framework-auth-smoke.test.ts` returns the two smoke-only SHOP-16 tests.
    - `rg -n "shopify-app-express|shopify-app-remix|shopify-app-react-router" tests/sdk-verification/sdk/shopify-app-framework-auth-smoke.test.ts` returns no matches.
    - `rg -n "inventory_levels/connect.json|inventory_levels/adjust.json|inventory_levels/set.json|custom_collections.json|collects.json|POST /admin/reset clears inventory_levels, custom_collections, and collects" twins/shopify/test/integration/inventory-collection-state.test.ts` returns the exact route-level state tests.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && rg -n "Phase 39: grant-specific OAuth validation|client_credentials grant when client_secret is missing|requested_token_type is unsupported" tests/sdk-verification/sdk/shopify-api-auth.test.ts && rg -n "customerCreate via GraphQL is findable via GET /customers/:id.json|orderCreate via GraphQL is findable via GET /orders/:id.json|Product.save\\(\\) persists title through PUT /admin/api/2024-01/products/:id.json|Customer.save\\(\\) persists first_name and last_name through PUT /admin/api/2024-01/customers/:id.json|Order.save\\(\\) persists name and total_price through PUT /admin/api/2024-01/orders/:id.json|Customer.all\\(\\{ ids \\}\\) returns only the requested numeric ids|Order.all\\(\\{ ids \\}\\) returns only the requested numeric ids|InventoryLevel.adjust/connect/set/delete round-trips through stored inventory_levels state|Location.inventory_levels\\(id=1\\) returns the connected inventory_levels row|Product.all\\(\\{ collection_id \\}\\) returns only products linked by Collect rows" tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts && rg -n "auth.begin -> authorize -> auth.callback produces a session that can GET /admin/api/2025-01/products.json|auth.tokenExchange -> admin Graphql client can request shop \\{ name \\}" tests/sdk-verification/sdk/shopify-app-framework-auth-smoke.test.ts && rg -n "inventory_levels/connect.json|inventory_levels/adjust.json|inventory_levels/set.json|custom_collections.json|collects.json|POST /admin/reset clears inventory_levels, custom_collections, and collects" twins/shopify/test/integration/inventory-collection-state.test.ts</automated>
  </verify>
  <done>The four Wave 0 files exist, they name the concrete OAuth, REST-state, and framework-readiness seams Phase 39 must close, and the targeted run surfaces the remaining defects without broadening SHOP-16 into a real app-framework package phase.</done>
</task>

</tasks>

<verification>
Run the structural Wave 0 checks and confirm:
- the new OAuth negative cases explicitly fail or go red for missing grant-specific fields rather than silently minting tokens
- the new REST-state parity file contains both the 39-03 and 39-04 defect clusters
- the SHOP-16 file stays smoke-only and package-free
</verification>

<success_criteria>
Phase 39 has an executable Wave 0 contract: auth validation drift, ID parity drift, REST write/filter drift, and inventory/collection drift are all represented by explicit test files, and the SHOP-16 seam is framed as readiness smoke rather than framework rollout work.
</success_criteria>

<output>
After completion, create `.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-01-SUMMARY.md`
</output>
