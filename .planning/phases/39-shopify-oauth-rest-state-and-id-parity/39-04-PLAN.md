---
phase: 39-shopify-oauth-rest-state-and-id-parity
plan: 04
type: execute
wave: 2
depends_on:
  - 39-03
files_modified:
  - packages/state/src/state-manager.ts
  - twins/shopify/src/db/schema.sql
  - twins/shopify/src/plugins/rest.ts
  - tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts
  - twins/shopify/test/integration/inventory-collection-state.test.ts
  - twins/shopify/test/integration.test.ts
  - twins/shopify/test/integration/pagination.test.ts
autonomous: true
requirements:
  - SHOP-14
  - SHOP-15
  - SHOP-17

must_haves:
  truths:
    - "InventoryLevel operations persist the same row across connect, adjust, set, list, and delete requests"
    - "Location.inventory_levels and InventoryLevel.all read from real stored state rather than permanent empty arrays"
    - "Product.all({ collection_id }) only returns products linked to that collection through stored membership"
    - "Any new tables join reset coverage immediately and versioned Admin routes still echo the request version"
  artifacts:
    - path: "packages/state/src/state-manager.ts"
      provides: "SQLite-backed inventory_levels, custom_collections, and collects persistence plus reset-safe statement lifecycle"
      contains: "inventory_levels"
    - path: "twins/shopify/src/db/schema.sql"
      provides: "Documented schema for the new state tables"
      contains: "CREATE TABLE IF NOT EXISTS custom_collections"
    - path: "twins/shopify/src/plugins/rest.ts"
      provides: "Versioned inventory level, custom collection, collect, and collection-filter routes backed by StateManager"
      contains: "inventory_levels/connect.json"
    - path: "tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts"
      provides: "SDK-level parity coverage for InventoryLevel, Location.inventory_levels, CustomCollection, Collect, and Product.all({ collection_id })"
      contains: "InventoryLevel.adjust/connect/set/delete round-trips through stored inventory_levels state"
    - path: "twins/shopify/test/integration/inventory-collection-state.test.ts"
      provides: "Route-level persistence and reset proof for inventory and collection state"
      contains: "POST /admin/reset clears inventory_levels, custom_collections, and collects"
    - path: "twins/shopify/test/integration/pagination.test.ts"
      provides: "Version-routing regression for collection-filter responses and header echo semantics"
      contains: "/admin/api/2025-01/products.json?collection_id="
  key_links:
    - from: "twins/shopify/src/plugins/rest.ts"
      to: "packages/state/src/state-manager.ts"
      via: "inventory_levels, custom_collections, and collects routes backed by SQLite state"
      pattern: "connectInventoryLevel|createCustomCollection|createCollect"
    - from: "tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts"
      to: "twins/shopify/src/plugins/rest.ts"
      via: "pinned REST resources exercise the new collection and inventory routes"
      pattern: "InventoryLevel|CustomCollection|Collect|collection_id"
    - from: "twins/shopify/test/integration.test.ts"
      to: "packages/state/src/state-manager.ts"
      via: "reset coverage for new tables"
      pattern: "inventory_levels|custom_collections|collects"
    - from: "twins/shopify/test/integration/pagination.test.ts"
      to: "twins/shopify/src/plugins/rest.ts"
      via: "versioned collection-filter regression exercises x-shopify-api-version echo and shared product filtering"
      pattern: "products.json|collection_id|x-shopify-api-version"
---

<objective>
Replace the last Shopify REST inventory and collection stubs with real schema-backed state, then prove the new behavior still honors reset guarantees and versioned Admin routing.

Purpose: The final Phase 39 gap is structural. Inventory levels and collection membership cannot be made truthful without actual persistence, so this plan introduces only the minimum schema/state needed to close that seam.
Output: New SQLite tables and state-manager methods plus versioned REST routes and tests for InventoryLevel, Location.inventory_levels, CustomCollection, Collect, and product collection filtering.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-RESEARCH.md
@.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-VALIDATION.md
@.planning/phases/28-shopify-rest-pagination-version-policy/28-03-SUMMARY.md
@.planning/phases/33-cross-cutting-reset-coverage/33-01-SUMMARY.md
@.planning/phases/36-shopify-behavioral-parity/36-04-SUMMARY.md
@tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts
@twins/shopify/test/integration/inventory-collection-state.test.ts
</context>

<interfaces>
From vendored REST resource contracts:
```typescript
InventoryLevel.all({ session, inventory_item_ids, location_ids, limit, updated_at_min })
InventoryLevel.adjust({ inventory_item_id, location_id, available_adjustment })
InventoryLevel.connect({ inventory_item_id, location_id, relocate_if_necessary })
InventoryLevel.set({ inventory_item_id, location_id, available, disconnect_if_necessary })
InventoryLevel.delete({ inventory_item_id, location_id })

Location.inventory_levels({ session, id })

CustomCollection.post({ session, data: { custom_collection: ... } })
CustomCollection.find({ session, id })
Collect.post({ session, data: { collect: { collection_id, product_id } } })

Product.all({ session, collection_id })
```

From `packages/state/src/state-manager.ts` reset pattern:
```typescript
reset(): void {
  this.db.close();
  this.db = null;
  // null prepared statements
  this.init();
}

close(): void {
  this.db.close();
  this.db = null;
  // null prepared statements
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add reset-safe SQLite persistence for inventory levels and collection membership</name>
  <read_first>
    packages/state/src/state-manager.ts
    twins/shopify/src/db/schema.sql
    twins/shopify/test/integration/inventory-collection-state.test.ts
    .planning/phases/33-cross-cutting-reset-coverage/33-01-SUMMARY.md
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/inventory_level.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/collect.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/custom_collection.ts
  </read_first>
  <files>packages/state/src/state-manager.ts, twins/shopify/src/db/schema.sql, twins/shopify/test/integration/inventory-collection-state.test.ts</files>
  <action>
    Add the minimum new schema and state-manager methods needed for truthful inventory and collection behavior.

    In `packages/state/src/state-manager.ts` and the documented `twins/shopify/src/db/schema.sql`, add these exact tables:
    - `inventory_levels`
      - `id INTEGER PRIMARY KEY AUTOINCREMENT`
      - `inventory_item_id INTEGER NOT NULL`
      - `location_id INTEGER NOT NULL`
      - `available INTEGER NOT NULL DEFAULT 0`
      - `created_at INTEGER NOT NULL`
      - `updated_at INTEGER NOT NULL`
      - `UNIQUE(inventory_item_id, location_id)`
    - `custom_collections`
      - `id INTEGER PRIMARY KEY AUTOINCREMENT`
      - `gid TEXT UNIQUE NOT NULL`
      - `title TEXT`
      - `handle TEXT`
      - `created_at INTEGER NOT NULL`
      - `updated_at INTEGER NOT NULL`
    - `collects`
      - `id INTEGER PRIMARY KEY AUTOINCREMENT`
      - `collection_id INTEGER NOT NULL`
      - `product_id INTEGER NOT NULL`
      - `position INTEGER DEFAULT 1`
      - `created_at INTEGER NOT NULL`
      - `updated_at INTEGER NOT NULL`
      - `UNIQUE(collection_id, product_id)`

    Add prepared statements and public methods for:
    - `connectInventoryLevel(inventoryItemId, locationId)`
    - `adjustInventoryLevel(inventoryItemId, locationId, availableAdjustment)`
    - `setInventoryLevel(inventoryItemId, locationId, available)`
    - `deleteInventoryLevel(inventoryItemId, locationId)`
    - `listInventoryLevels({ inventoryItemIds?: number[], locationIds?: number[] })`
    - `createCustomCollection({ gid, title, handle })`
    - `getCustomCollection(id)`
    - `listCustomCollections()`
    - `createCollect({ collection_id, product_id, position })`
    - `getCollect(id)`
    - `listCollects({ collectionId? })`
    - `listProductsByCollectionId(collectionId)`

    Follow the StateManager lifecycle pattern exactly:
    - declare statement fields
    - prepare them in `prepareStatements()`
    - null them in both `reset()` and `close()`

    In `twins/shopify/test/integration/inventory-collection-state.test.ts`, keep the route-level scenarios from Wave 0 and make them assert the new persisted behavior plus reset clearing.
  </action>
  <acceptance_criteria>
    - `rg -n "CREATE TABLE IF NOT EXISTS inventory_levels|CREATE TABLE IF NOT EXISTS custom_collections|CREATE TABLE IF NOT EXISTS collects" packages/state/src/state-manager.ts twins/shopify/src/db/schema.sql` returns all three new tables.
    - `rg -n "connectInventoryLevel|adjustInventoryLevel|setInventoryLevel|deleteInventoryLevel|createCustomCollection|createCollect|listProductsByCollectionId" packages/state/src/state-manager.ts` returns the new public persistence methods.
    - `rg -n "inventoryLevels|customCollections|collects|inventory_levels|custom_collections|collects" packages/state/src/state-manager.ts` shows the new statements are nulled in both `reset\\(\\)` and `close\\(\\)`.
    - `rg -n "POST /admin/reset clears inventory_levels, custom_collections, and collects|inventory_levels/connect.json|custom_collections.json|collects.json" twins/shopify/test/integration/inventory-collection-state.test.ts` returns the route-level persistence and reset tests.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm vitest run --project @dtu/twin-shopify twins/shopify/test/integration/inventory-collection-state.test.ts</automated>
  </verify>
  <done>The twin has real SQLite tables and state-manager methods for inventory levels and collection membership, and the route-level integration file proves those rows persist across requests and clear on reset.</done>
</task>

<task type="auto">
  <name>Task 2: Replace versioned inventory and collection stubs with StateManager-backed REST routes</name>
  <read_first>
    twins/shopify/src/plugins/rest.ts
    packages/state/src/state-manager.ts
    tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/inventory_level.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/collect.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/custom_collection.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2023-07/location.ts
    .planning/phases/36-shopify-behavioral-parity/36-04-SUMMARY.md
    .planning/phases/28-shopify-rest-pagination-version-policy/28-03-SUMMARY.md
  </read_first>
  <files>twins/shopify/src/plugins/rest.ts, tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts</files>
  <action>
    Replace the remaining inventory and collection stubs in `twins/shopify/src/plugins/rest.ts` with versioned handlers backed by the new state methods.

    Implement these exact route behaviors:
    - `GET /admin/api/:version/inventory_levels.json`
      - parse `inventory_item_ids` and `location_ids`
      - return `{ inventory_levels: [...] }` from `stateManager.listInventoryLevels(...)`
    - `GET /admin/api/:version/locations/:id/inventory_levels.json`
      - return only rows for that `location_id`
    - `POST /admin/api/:version/inventory_levels/connect.json`
      - upsert or create the row with `available: 0`
    - `POST /admin/api/:version/inventory_levels/adjust.json`
      - return `404 { errors: 'Not Found' }` when the `(inventory_item_id, location_id)` row does not exist
      - otherwise add `available_adjustment` to the stored row and return the updated `inventory_level`
    - `POST /admin/api/:version/inventory_levels/set.json`
      - upsert the row when missing and set the stored `available` exactly
    - `DELETE /admin/api/:version/inventory_levels.json`
      - return `404 { errors: 'Not Found' }` when no row exists for that pair
      - otherwise delete by `inventory_item_id` + `location_id`
    - `POST /admin/api/:version/custom_collections.json`
      - create a canonical `gid://shopify/Collection/{rowId}` row and return `{ custom_collection: ... }`
    - `GET /admin/api/:version/custom_collections.json`
    - `GET /admin/api/:version/custom_collections/:id.json`
    - `POST /admin/api/:version/collects.json`
    - `GET /admin/api/:version/collects.json`
    - `GET /admin/api/:version/collects/:id.json`
    - `GET /admin/api/:version/products.json`
      - after existing `since_id` and `ids` filters, if `collection_id` is present, filter to `stateManager.listProductsByCollectionId(Number(collection_id))`

    Register the new routes in this exact relative order inside `rest.ts`:
    - keep `locations/count.json` before `locations/:id.json`
    - register `GET /custom_collections.json` and `POST /custom_collections.json` before `GET /custom_collections/:id.json`
    - register `GET /collects.json` and `POST /collects.json` before `GET /collects/:id.json`
    - register `POST /inventory_levels/connect.json`, `POST /inventory_levels/adjust.json`, and `POST /inventory_levels/set.json` before `DELETE /inventory_levels.json`
    - do not add an `inventory_levels/:id.json` route in this phase

    Extend `tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts` so the pinned REST resource classes prove:
    - `InventoryLevel.adjust/connect/set/delete` all see the same stored row
    - `Location.inventory_levels({ id: 1 })` returns that row
    - `CustomCollection.post/find/all` work against the twin
    - `Collect.post/find/all` work against the twin
    - `Product.all({ collection_id })` returns only linked products
    - Preserve the exact Wave 0 test names already present in `shopify-rest-state-parity.test.ts`; add inventory/collection cases without renaming the existing order/customer/product cases.

    Do not fabricate collection results from request params. Every response must derive from stored `inventory_levels`, `custom_collections`, and `collects` rows.
  </action>
  <acceptance_criteria>
    - `rg -n "fastify.get\\(adminPath\\('/inventory_levels.json'\\)|fastify.post\\(adminPath\\('/inventory_levels/connect.json'\\)|fastify.post\\(adminPath\\('/custom_collections.json'\\)|fastify.post\\(adminPath\\('/collects.json'\\)" twins/shopify/src/plugins/rest.ts` returns the new versioned route registrations.
    - `rg -n "inventory_item_ids|location_ids|collection_id|errors: 'Not Found'" twins/shopify/src/plugins/rest.ts` shows the exact query parameters plus explicit inventory edge-case handling.
    - `rg -n "InventoryLevel.adjust/connect/set/delete round-trips through stored inventory_levels state|Location.inventory_levels\\(id=1\\) returns the connected inventory_levels row|Product.all\\(\\{ collection_id \\}\\) returns only products linked by Collect rows" tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts` returns the SDK parity tests for this plan.
    - `rg -n "restResources" tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts` confirms the file uses the bundled resource export, not individual class imports.
    - `pnpm vitest run tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts tests/sdk-verification/sdk/shopify-api-rest-client.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm vitest run tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts tests/sdk-verification/sdk/shopify-api-rest-client.test.ts</automated>
  </verify>
  <done>The versioned Admin REST routes for inventory levels, custom collections, and collects are backed by stored state, and the SDK parity file proves the pinned REST resource classes now observe truthful inventory and collection behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add version-routing and reset regressions for the new state tables</name>
  <read_first>
    twins/shopify/test/integration.test.ts
    twins/shopify/test/integration/pagination.test.ts
    packages/state/src/state-manager.ts
    .planning/phases/28-shopify-rest-pagination-version-policy/28-03-SUMMARY.md
    .planning/phases/33-cross-cutting-reset-coverage/33-01-SUMMARY.md
  </read_first>
  <files>twins/shopify/test/integration.test.ts, twins/shopify/test/integration/pagination.test.ts</files>
  <action>
    Extend existing integration coverage so the new Phase 39 state does not regress SHOP-17 or XCUT-01-style reset guarantees.

    In `twins/shopify/test/integration.test.ts`:
    - Add reset coverage using the existing reset-coverage style already present in `integration.test.ts` for these exact tables:
      - `inventory_levels`
      - `custom_collections`
      - `collects`
    - Insert one row into each table through `app.stateManager.database`, call `POST /admin/reset`, re-read `app.stateManager.database`, and assert `SELECT COUNT(*) as count FROM <table>` is `0`.

    In `twins/shopify/test/integration/pagination.test.ts`:
    - Add a version-routing regression that proves the new collection filter respects request versions:
      - create one custom collection and one collect
      - assert `GET /admin/api/2025-01/products.json?collection_id=<id>` and `GET /admin/api/2024-01/products.json?collection_id=<id>` return the same product IDs
      - assert the `2025-01` response still echoes `x-shopify-api-version: 2025-01`

    If you add a timing assertion, keep the warm-up-reset pattern from Phase 33 and do not change the existing sub-100ms threshold.
  </action>
  <acceptance_criteria>
    - `rg -n "inventory_levels|custom_collections|collects|POST /admin/reset" twins/shopify/test/integration.test.ts` returns the new reset-coverage block for the three tables.
    - `rg -n "/admin/api/2025-01/products.json\\?collection_id=|x-shopify-api-version" twins/shopify/test/integration/pagination.test.ts` returns the versioned collection-filter regression.
    - `rg -n "SELECT COUNT\\(\\*\\) as count FROM inventory_levels|SELECT COUNT\\(\\*\\) as count FROM custom_collections|SELECT COUNT\\(\\*\\) as count FROM collects" twins/shopify/test/integration.test.ts` returns the reset assertions for the three new tables.
    - `pnpm vitest run tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts tests/sdk-verification/sdk/shopify-api-rest-client.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm vitest run --project @dtu/twin-shopify twins/shopify/test/integration.test.ts twins/shopify/test/integration/pagination.test.ts && pnpm vitest run tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts tests/sdk-verification/sdk/shopify-api-rest-client.test.ts</automated>
  </verify>
  <done>The new inventory and collection tables join reset coverage immediately, and versioned Admin routing remains correct for the new collection-filter behavior.</done>
</task>

</tasks>

<verification>
Run the route-level inventory/collection file, then the SDK parity file, then the broader integration regressions. Confirm the new state is truthful, reset-safe, and still served through the same versioned route policy established in earlier phases.
</verification>

<success_criteria>
Inventory levels, locations inventory views, custom collections, collects, and product collection filters are all backed by SQLite state; the new tables clear on reset; and the new behavior still honors Shopify version routing and header echo semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-04-SUMMARY.md`
</output>
