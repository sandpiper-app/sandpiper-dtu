---
phase: 39-shopify-oauth-rest-state-and-id-parity
plan: 03
type: execute
wave: 1
depends_on:
  - 39-01
files_modified:
  - twins/shopify/src/schema/resolvers.ts
  - twins/shopify/src/plugins/admin.ts
  - twins/shopify/src/plugins/rest.ts
  - packages/state/src/state-manager.ts
  - tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts
  - twins/shopify/test/integration/rest-persistence.test.ts
autonomous: true
requirements:
  - SHOP-14
  - SHOP-15
  - SHOP-17

must_haves:
  truths:
    - "Orders and customers created through GraphQL or fixtures round-trip through REST numeric IDs instead of timestamp-derived GIDs"
    - "Product, customer, and order writes persist in SQLite and are returned back by subsequent GET/list calls"
    - "Product, customer, and order REST responses expose Shopify-style fields instead of raw database columns such as `gid` and serialized `line_items`"
    - "Customer and order `ids` filters behave like the pinned REST resource classes expect"
    - "All new REST behavior continues to live under `/admin/api/:version/...` without regressing SHOP-17 version routing"
  artifacts:
    - path: "twins/shopify/src/schema/resolvers.ts"
      provides: "Canonical two-step order/customer GID creation in GraphQL mutations"
      contains: "orderCreate"
    - path: "twins/shopify/src/plugins/admin.ts"
      provides: "Canonical order/customer/inventory item fixture GIDs"
      contains: "UPDATE customers SET gid = ? WHERE id = ?"
    - path: "twins/shopify/src/plugins/rest.ts"
      provides: "Normalized state-backed POST/PUT/GET/list handlers for products, customers, and orders"
      contains: "fastify.post(adminPath('/customers.json')"
    - path: "tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts"
      provides: "SDK-facing parity coverage for order/customer ID round-trip and REST persistence"
      contains: "Customer.save() persists first_name and last_name"
    - path: "twins/shopify/test/integration/rest-persistence.test.ts"
      provides: "Route-level persistence checks for products, customers, and orders"
      contains: "PUT /admin/api/2024-01/orders/"
  key_links:
    - from: "twins/shopify/src/schema/resolvers.ts"
      to: "twins/shopify/src/plugins/rest.ts"
      via: "GraphQL create returns a GID whose numeric suffix is accepted by REST `:id.json` routes"
      pattern: "createGID|/customers/:id.json|/orders/:id.json"
    - from: "twins/shopify/src/plugins/admin.ts"
      to: "tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts"
      via: "fixtures produce canonical GIDs for parity tests instead of synthetic timestamps"
      pattern: "fixtures/load|gid://shopify/Customer|gid://shopify/Order"
    - from: "twins/shopify/src/plugins/rest.ts"
      to: "packages/state/src/state-manager.ts"
      via: "all product/customer/order writes and reads pass through state-manager CRUD"
      pattern: "updateProduct|updateCustomer|updateOrder"
---

<objective>
Fix the order/customer ID mismatch and the remaining product/customer/order REST persistence drift so the pinned Shopify REST resources behave against stored state instead of permissive stubs.

Purpose: SHOP-14 and SHOP-15 are already mapped to Shopify client surfaces. Phase 39 closes the remaining seams where those surfaces appear to work but only because the twin returns hardcoded or mis-keyed responses.
Output: Canonical order/customer fixture and GraphQL IDs plus state-backed create/update/find/filter handlers for products, customers, and orders.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-RESEARCH.md
@.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-VALIDATION.md
@.planning/phases/24-shopify-rest-persistence-billing-state-machine-rate-limiting/24-02-SUMMARY.md
@.planning/phases/28-shopify-rest-pagination-version-policy/28-03-SUMMARY.md
@.planning/phases/36-shopify-behavioral-parity/36-03-SUMMARY.md
@.planning/phases/36-shopify-behavioral-parity/36-04-SUMMARY.md
@tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts
</context>

<interfaces>
From `packages/state/src/state-manager.ts`:
```typescript
createOrder(data: { gid: string; name?: string; total_price?: string; currency_code?: string; customer_gid?: string; line_items?: any; financial_status?: string }): number;
updateOrder(id: number, data: { name?: string; total_price?: string; currency_code?: string; customer_gid?: string; line_items?: any }): void;
getOrderById(id: number): any | undefined;

createProduct(data: { gid: string; title?: string; description?: string; vendor?: string; product_type?: string; price?: string }): number;
getProduct(rowId: number): any | undefined;
updateProduct(id: number, data: { title?: string; description?: string; vendor?: string; product_type?: string; price?: string }): void;

createCustomer(data: { gid: string; email?: string; first_name?: string; last_name?: string }): number;
getCustomer(id: number): any | undefined;
updateCustomer(id: number, data: { email?: string; first_name?: string; last_name?: string }): void;
```

From vendored REST resource classes:
```typescript
Customer.all({ session, ids, since_id })
Customer.find({ session, id })
Customer.post({ session, data: { customer: ... } })
Customer.save()

Order.all({ session, ids, since_id })
Order.find({ session, id })
Order.post({ session, data: { order: ... } })
Order.save()

Product.all({ session, ids, since_id })
Product.save()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize order and customer GIDs across GraphQL mutations and fixtures</name>
  <read_first>
    twins/shopify/src/schema/resolvers.ts
    twins/shopify/src/plugins/admin.ts
    packages/state/src/state-manager.ts
    tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts
    .planning/phases/24-shopify-rest-persistence-billing-state-machine-rate-limiting/24-02-SUMMARY.md
    .planning/phases/36-shopify-behavioral-parity/36-03-SUMMARY.md
  </read_first>
  <files>twins/shopify/src/schema/resolvers.ts, twins/shopify/src/plugins/admin.ts, tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts</files>
  <action>
    Apply the product-style two-step GID pattern to orders and customers, and use the same canonical-row-ID approach in fixture loading.

    In `twins/shopify/src/schema/resolvers.ts`:
    - Update `orderCreate` so it inserts with a temp GID such as `gid://shopify/Order/temp-${Date.now()}`, captures the numeric row ID from `stateManager.createOrder(...)`, runs `UPDATE orders SET gid = ? WHERE id = ?`, and returns the row after its final canonical GID is stored.
    - Update `customerCreate` the same way with `UPDATE customers SET gid = ? WHERE id = ?`.
    - Keep `createGID('Order', rowId)` and `createGID('Customer', rowId)` as the final GID source; do not keep timestamp GIDs.

    In `twins/shopify/src/plugins/admin.ts`:
    - Change fixture seeding for `orders`, `customers`, and `inventoryItems` from timestamp-based final GIDs to the same temp-insert-then-update pattern.
    - Use these exact SQL table targets:
      - `UPDATE orders SET gid = ? WHERE id = ?`
      - `UPDATE customers SET gid = ? WHERE id = ?`
      - `UPDATE inventory_items SET gid = ? WHERE id = ?`
    - Keep the existing product two-step insert unchanged.

    In `tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts`, add or keep assertions that prove:
    - `customerCreate via GraphQL is findable via GET /customers/:id.json`
    - `orderCreate via GraphQL is findable via GET /orders/:id.json`
    - fixture-loaded customer and order GIDs now use the REST numeric row ID suffix rather than a timestamp-like value
  </action>
  <acceptance_criteria>
    - `rg -n "UPDATE orders SET gid = \\? WHERE id = \\?|UPDATE customers SET gid = \\? WHERE id = \\?" twins/shopify/src/schema/resolvers.ts twins/shopify/src/plugins/admin.ts` returns the canonical two-step SQL for orders and customers.
    - `rg -n "UPDATE inventory_items SET gid = \\? WHERE id = \\?" twins/shopify/src/plugins/admin.ts` returns the fixture-side inventory item canonicalization.
    - `rg -n "customerCreate via GraphQL is findable via GET /customers/:id.json|orderCreate via GraphQL is findable via GET /orders/:id.json" tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts` returns the exact parity tests.
    - `rg -n "createGID\\('Order'|createGID\\('Customer'" twins/shopify/src/schema/resolvers.ts twins/shopify/src/plugins/admin.ts` shows the final GIDs are row-ID-based rather than timestamp literals.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm vitest run tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts</automated>
  </verify>
  <done>Orders and customers created through GraphQL or fixtures now receive canonical row-ID GIDs, and the parity suite proves those GIDs round-trip through REST numeric lookup routes.</done>
</task>

<task type="auto">
  <name>Task 2: Make product, customer, and order REST writes and ids filters stateful</name>
  <read_first>
    twins/shopify/src/plugins/rest.ts
    packages/state/src/state-manager.ts
    tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts
    twins/shopify/test/integration/rest-persistence.test.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/customer.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/order.ts
    third_party/upstream/shopify-app-js/packages/apps/shopify-api/rest/admin/2024-10/product.ts
    .planning/phases/24-shopify-rest-persistence-billing-state-machine-rate-limiting/24-02-SUMMARY.md
    .planning/phases/36-shopify-behavioral-parity/36-04-SUMMARY.md
    .planning/phases/28-shopify-rest-pagination-version-policy/28-03-SUMMARY.md
  </read_first>
  <files>twins/shopify/src/plugins/rest.ts, packages/state/src/state-manager.ts, tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts, twins/shopify/test/integration/rest-persistence.test.ts</files>
  <action>
    Replace the remaining product/customer/order stubs with state-backed versioned handlers.

    In `twins/shopify/src/plugins/rest.ts`:
    - Replace `PUT /admin/api/:version/products/:id.json` so it:
      - parses the numeric `:id`
      - calls `stateManager.updateProduct(id, { title, description, vendor, product_type, price })`
      - re-reads the row with `getProduct(id)`
      - returns `404 { errors: 'Not Found' }` for missing IDs
      - returns `{ product: { id, admin_graphql_api_id, title, created_at, updated_at } }` from stored state
    - Change `GET /admin/api/:version/customers/:id.json` to numeric lookup via `stateManager.getCustomer(numericId)` with the same 404 shape.
    - Add `POST /admin/api/:version/customers.json` and `PUT /admin/api/:version/customers/:id.json` using:
      - two-step create with temp GID then `gid://shopify/Customer/{rowId}`
      - `updateCustomer(id, { email, first_name, last_name })`
      - `POST` and `PUT` return `422 { errors: 'customer is required' }` when `body.customer` is missing
    - Add `POST /admin/api/:version/orders.json` and `PUT /admin/api/:version/orders/:id.json` using:
      - two-step create with temp GID then `gid://shopify/Order/{rowId}`
      - `updateOrder(id, { name, total_price, currency_code, customer_gid, line_items })`
      - `POST` and `PUT` return `422 { errors: 'order is required' }` when `body.order` is missing
    - In both `GET /customers.json` and `GET /orders.json`, add `ids` filtering before `paginateList` using the same comma-separated numeric parsing pattern already used by `products.json`.
    - Normalize every product/customer/order REST response in this file instead of returning raw DB rows:
      - products: `{ id, admin_graphql_api_id, title, created_at, updated_at }`
      - customers: `{ id, admin_graphql_api_id, email, first_name, last_name, created_at, updated_at }`
      - orders: `{ id, admin_graphql_api_id, name, total_price, currency_code, display_fulfillment_status, display_financial_status, line_items, created_at, updated_at }`
    - For orders, parse stored `line_items` JSON back to arrays before returning it in REST responses.
    - Keep every route under `adminPath(...)`; do not add unversioned Admin API routes.
    - Preserve the exact Wave 0 test names already created in `shopify-rest-state-parity.test.ts`; extend that file without renaming the existing cases.

    In `packages/state/src/state-manager.ts`:
    - Only add new public helpers if required by the route code. If you add one, follow the established prepared-statement lifecycle: declare field, prepare in `prepareStatements()`, null it in both `reset()` and `close()`.

    In the tests:
    - Extend `shopify-rest-state-parity.test.ts` to cover:
      - `Product.save() persists title through PUT /products/:id.json`
      - `Customer.save() persists first_name and last_name through PUT /customers/:id.json`
      - `Order.save() persists name and total_price through PUT /orders/:id.json`
      - `Customer.all({ ids }) returns only the requested numeric ids`
      - `Order.all({ ids }) returns only the requested numeric ids`
    - Extend `twins/shopify/test/integration/rest-persistence.test.ts` with route-level POST/PUT cases for customers and orders plus a route-level product PUT persistence proof.

    Do not add `collection_id` behavior here. That depends on real membership state and belongs in `39-04`.
  </action>
  <acceptance_criteria>
    - `rg -n "fastify.post\\(adminPath\\('/customers.json'\\)|fastify.put\\(adminPath\\('/customers/:id.json'\\)|fastify.post\\(adminPath\\('/orders.json'\\)|fastify.put\\(adminPath\\('/orders/:id.json'\\)" twins/shopify/src/plugins/rest.ts` returns the new versioned customer and order write routes.
    - `rg -n "stateManager.updateProduct|stateManager.updateCustomer|stateManager.updateOrder" twins/shopify/src/plugins/rest.ts` shows all three write paths go through stored state.
    - `rg -n "customer is required|order is required|errors: 'Not Found'|admin_graphql_api_id|JSON.parse\\(order\\.line_items" twins/shopify/src/plugins/rest.ts` shows explicit validation, not-found handling, and normalized response serialization for the new product/customer/order routes.
    - `rg -n "Product.save\\(\\) persists title|Customer.save\\(\\) persists first_name and last_name|Order.save\\(\\) persists name and total_price|Customer.all\\(\\{ ids \\}\\)|Order.all\\(\\{ ids \\}\\)" tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts` returns the SDK-facing persistence and filter tests.
    - `rg -n "PUT /admin/api/2024-01/products/|POST /admin/api/2024-01/customers.json|PUT /admin/api/2024-01/orders/" twins/shopify/test/integration/rest-persistence.test.ts` returns the route-level persistence coverage.
    - `pnpm vitest run tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts tests/sdk-verification/sdk/shopify-api-rest-client.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm vitest run tests/sdk-verification/sdk/shopify-rest-state-parity.test.ts twins/shopify/test/integration/rest-persistence.test.ts && pnpm vitest run tests/sdk-verification/sdk/shopify-behavioral-parity.test.ts tests/sdk-verification/sdk/shopify-api-rest-client.test.ts</automated>
  </verify>
  <done>Product/customer/order writes persist through SQLite-backed handlers, customer and order ids filters behave like the pinned REST resources expect, the new routes fail with explicit 422/404 shapes instead of guessed behavior, and the broader Shopify SDK regression files stay green.</done>
</task>

</tasks>

<verification>
Run the SDK parity file and the route-level persistence file together. Confirm order/customer GraphQL IDs, fixture IDs, product PUT, customer/order write routes, and ids filters all behave against stored state.
</verification>

<success_criteria>
Orders and customers now follow the same canonical-ID strategy as products, REST create/update/find/filter behavior is backed by persisted rows instead of permissive stubs, and SHOP-17 versioned routing remains intact across the new handlers.
</success_criteria>

<output>
After completion, create `.planning/phases/39-shopify-oauth-rest-state-and-id-parity/39-03-SUMMARY.md`
</output>
