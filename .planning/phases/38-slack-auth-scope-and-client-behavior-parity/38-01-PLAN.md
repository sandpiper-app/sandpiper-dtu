---
phase: 38-slack-auth-scope-and-client-behavior-parity
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md
  - tests/sdk-verification/sdk/slack-auth-parity.test.ts
  - tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts
  - tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts
autonomous: true
requirements:
  - SLCK-20
  - SLCK-21
  - SLCK-22
  - SLCK-23

must_haves:
  truths:
    - "Phase 38 requirement IDs SLCK-20 through SLCK-23 are defined in REQUIREMENTS.md with meanings that match the roadmap and validation docs"
    - "The old deferred Enterprise Grid requirement no longer occupies SLCK-20"
    - "Wave 0 auth parity tests fail because OIDC tokens are not persisted, oauth.v2.access ignores client_secret or granted scopes, apps.connections.open accepts the wrong token class, and auth.test collapses user identity into bot identity"
    - "Wave 0 conversation parity tests fail because conversations.list/info/history still require universal family scopes instead of request-aware or channel-aware scopes"
    - "Wave 0 client-behavior parity tests fail because filesUploadV2 returns reduced file objects and response_url still appends instead of replacing or deleting"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Resolved Phase 38 requirement definitions and traceability rows"
      contains: "SLCK-20"
    - path: ".planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md"
      provides: "Nyquist validation map aligned to the same SLCK-20..23 meanings used by REQUIREMENTS.md"
      contains: "38-02-02"
    - path: "tests/sdk-verification/sdk/slack-auth-parity.test.ts"
      provides: "Wave 0 auth/token parity tests for OIDC, oauth.v2.access, apps.connections.open, and auth.test"
      contains: "openid.connect.token round-trips"
    - path: "tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts"
      provides: "Wave 0 dynamic scope tests for conversations.list/info/history"
      contains: "conversations.list types=public_channel"
    - path: "tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts"
      provides: "Wave 0 client-visible behavior tests for filesUploadV2 and response_url"
      contains: "replace_original updates the original message"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 38 requirement ID realignment"
      pattern: "SLCK-20|SLCK-21|SLCK-22|SLCK-23"
    - from: "tests/sdk-verification/sdk/slack-auth-parity.test.ts"
      to: "twins/slack/src/plugins/oauth.ts"
      via: "authorize -> oauth.v2.access -> auth.test route flow"
      pattern: "oauth.v2.access|auth.test"
    - from: "tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts"
      to: "twins/slack/src/plugins/web-api/conversations.ts"
      via: "conversations.list/info/history parity assertions"
      pattern: "conversations.list|conversations.info|conversations.history"
    - from: "tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts"
      to: "twins/slack/src/plugins/web-api/files.ts"
      via: "filesUploadV2 completion assertions"
      pattern: "filesUploadV2|files.completeUploadExternal"
---

<objective>
Establish the Phase 38 execution contract before implementation: fix the SLCK-20..23 traceability mismatch in project docs and add RED parity tests that prove each confirmed Slack defect still exists.

Purpose: Phase 38 was reopened because the existing suite stayed green while Slack behavior drift remained. This Wave 0 plan converts the six confirmed defects into explicit failing tests and removes the requirement-ID ambiguity that would otherwise break execution and verification.
Output: Updated `.planning/REQUIREMENTS.md` plus three new RED parity test files covering auth/token semantics, conversation scope resolution, and client-visible behavior.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-RESEARCH.md
@.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md
@.planning/phases/35-slack-behavioral-parity/35-01-SUMMARY.md
@.planning/phases/31-slack-oauth-method-coverage/31-02-SUMMARY.md
</context>

<interfaces>
From `tests/sdk-verification/setup/seeders.ts`:
```typescript
export async function resetSlack(): Promise<void>;
export async function seedSlackBotToken(token = 'xoxb-test-token'): Promise<string>;
export async function seedSlackChannel(name: string): Promise<string>;
```

From `tests/sdk-verification/helpers/slack-client.ts`:
```typescript
export function createSlackClient(token?: string): WebClient;
// slackApiUrl is process.env.SLACK_API_URL + '/api/'
```

From `twins/slack/src/plugins/admin.ts`:
```typescript
POST /admin/tokens
Body: { token, tokenType, teamId, userId, scope, appId }

POST /admin/set-wss-url
Body: { url: string }

POST /admin/interactions/trigger
Body: { message_ts, channel, action_id, user_id, block_id? }
```

From `twins/slack/src/plugins/interactions.ts`:
```typescript
POST /response-url/:id
// forwards request.body to interactionHandler.handleResponseUrl(id, body)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Realign Phase 38 requirement IDs in REQUIREMENTS.md</name>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-RESEARCH.md
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Edit `.planning/REQUIREMENTS.md` so Phase 38 traceability is explicit and internally consistent.

    1. Under `## v1.2 Requirements` in the `### Slack Fidelity` section, add four new requirement bullets immediately after `SLCK-19` with these exact meanings:
    - `SLCK-20`: `openid.connect.token` persists OIDC access tokens that work with `openid.connect.userInfo`; `oauth.v2.access` validates `client_secret`, echoes the authorize-time granted scope string, and `apps.connections.open` requires an app token with `connections:write`
    - `SLCK-21`: `conversations.list`, `conversations.info`, and `conversations.history` resolve required `channels/groups/im/mpim` read or history scopes from request `types` or the resolved channel class instead of requiring every family scope at once
    - `SLCK-22`: `filesUploadV2` returns Slack-shaped completed file metadata, and `response_url` honors `replace_original` and `delete_original`
    - `SLCK-23`: `auth.test` returns identity fields that match the token class instead of always returning the bot identity

    2. In `## v2 Requirements` -> `### Extended Fidelity`, rename the existing deferred Enterprise Grid bullet from `SLCK-20` to `SLCK-24` without changing its text. This preserves the deferred idea while freeing `SLCK-20..23` for the reopened v1.2 phase.

    3. In the `### v1.2 (Pending)` traceability table, add rows:
    - `SLCK-20 | Phase 38 | Pending`
    - `SLCK-21 | Phase 38 | Pending`
    - `SLCK-22 | Phase 38 | Pending`
    - `SLCK-23 | Phase 38 | Pending`

    4. In `.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md`, realign the per-task verification map to the same requirement meanings:
    - keep `38-01-01` and `38-02-01` on `SLCK-20`
    - keep `38-01-02` and `38-03-01` on `SLCK-21`
    - keep `38-01-03` and `38-04-01` on `SLCK-22`
    - add `38-01-04` and `38-02-02` rows for `SLCK-23` using `slack-auth-parity.test.ts` / `slack-auth-gateway.test.ts`
    - remove the old `38-04-02` row so no client-behavior task still claims `SLCK-23`

    5. Update the summary counts at the bottom so they stay accurate after the new Slack rows:
    - `v1.2 requirements: 23 total (9 complete, 14 pending gap closure)`
    - `Mapped to phases: 23`
    - `Unmapped: 0`

    Do not add Phase 39 or Phase 40 requirement definitions here. This task resolves the concrete Phase 38 mismatch only.
  </action>
  <acceptance_criteria>
    - `rg -n "\\*\\*SLCK-20\\*\\*: .*openid\\.connect\\.token.*oauth\\.v2\\.access.*apps\\.connections\\.open" .planning/REQUIREMENTS.md` returns exactly one new v1.2 Slack requirement line.
    - `rg -n "\\*\\*SLCK-24\\*\\*: Slack \`admin\\.\\*\\` methods implement full Enterprise Grid simulation beyond basic stubs" .planning/REQUIREMENTS.md` returns the deferred v2 requirement under the new ID.
    - `rg -n "\\| SLCK-20 \\| Phase 38 \\| Pending \\||\\| SLCK-21 \\| Phase 38 \\| Pending \\||\\| SLCK-22 \\| Phase 38 \\| Pending \\||\\| SLCK-23 \\| Phase 38 \\| Pending \\|" .planning/REQUIREMENTS.md` returns four traceability rows.
    - `rg -n "38-01-04 .* SLCK-23|38-02-02 .* SLCK-23" .planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md` returns the two validation rows that now map SLCK-23 to auth-specific checks.
    - `rg -n "38-04-02" .planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md` returns no matches.
    - `rg -n "v1\\.2 requirements: 23 total \\(9 complete, 14 pending gap closure\\)" .planning/REQUIREMENTS.md` returns the updated summary line.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && rg -n "\\*\\*SLCK-(20|21|22|23|24)\\*\\*|\\| SLCK-(20|21|22|23) \\| Phase 38 \\| Pending \\||v1\\.2 requirements: 23 total" .planning/REQUIREMENTS.md && rg -n "38-01-04 .* SLCK-23|38-02-02 .* SLCK-23" .planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md</automated>
  </verify>
  <done>REQUIREMENTS.md and 38-VALIDATION.md both define SLCK-20 through SLCK-23 with the same Phase 38 meanings, the old deferred Enterprise Grid idea moves to SLCK-24, and the Nyquist verification map no longer assigns SLCK-23 to a client-behavior-only plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 auth/token parity tests</name>
  <read_first>
    tests/sdk-verification/sdk/slack-auth-gateway.test.ts
    tests/sdk-verification/sdk/slack-oauth-install-provider.test.ts
    tests/sdk-verification/setup/seeders.ts
    tests/sdk-verification/helpers/slack-client.ts
    twins/slack/src/plugins/oauth.ts
    twins/slack/src/plugins/web-api/new-families.ts
    twins/slack/src/plugins/web-api/auth.ts
    twins/slack/src/plugins/web-api/stubs.ts
  </read_first>
  <files>tests/sdk-verification/sdk/slack-auth-parity.test.ts</files>
  <action>
    Create `tests/sdk-verification/sdk/slack-auth-parity.test.ts` with one `beforeEach(async () => { await resetSlack(); })` and six RED tests that prove the current auth/token defects.

    Use `createSlackClient()`, direct `fetch(process.env.SLACK_API_URL! + ...)`, and admin token seeding. Add helpers inside the file for:
    - `authorizeCode({ clientId, scope, redirectUri })` using `GET /oauth/v2/authorize`
    - `exchangeCode({ code, clientId, clientSecret, redirectUri })` using `POST /api/oauth.v2.access`

    Add these exact tests and expected values:
    1. `openid.connect.token round-trips into openid.connect.userInfo using the returned access_token`
      - Call `client.openid.connect.token({ code: 'oidc-wave0', client_id: 'test-client', client_secret: 'test-client-secret' })`
      - Create `new WebClient(oidc.access_token, { slackApiUrl: process.env.SLACK_API_URL!.replace(/\\/$/, '') + '/api/' })`
      - Call `.openid.connect.userInfo()`
      - Assert `userInfo.sub === 'U_AUTHED'` and `userInfo.email === 'authed-user@twin.dev'`
    2. `oauth.v2.access rejects a mismatched client_secret with invalid_client`
      - Authorize with `client_id=test-client`, `scope=openid,channels:read`, `redirect_uri=http://localhost/slack/oauth_redirect`
      - Exchange with `client_secret=wrong-client-secret`
      - Assert body `{ ok: false, error: 'invalid_client' }`
    3. `oauth.v2.access echoes the authorize-time granted scope string in scope and authed_user.scope`
      - Authorize with `scope=channels:read,openid`
      - Exchange with `client_secret=test-client-secret`
      - Assert `body.scope === 'channels:read,openid'`
      - Assert `body.authed_user.scope === 'channels:read,openid'`
    4. `apps.connections.open rejects a bot token with connections:write when tokenType is bot`
      - `POST /admin/set-wss-url` with `ws://127.0.0.1:3099/socket-mode`
      - Seed `xoxb-connections-bot` via `/admin/tokens` with `tokenType: 'bot'`, `scope: 'connections:write'`
      - Call `POST /api/apps.connections.open`
      - Assert `{ ok: false, error: 'invalid_auth' }`
    5. `apps.connections.open returns the seeded WebSocket URL for an xapp token with connections:write`
      - Seed `xapp-1-A_TWIN-wave0` via `/admin/tokens` with `tokenType: 'app'`, `scope: 'connections:write'`
      - Call `POST /api/apps.connections.open`
      - Assert `{ ok: true, url: 'ws://127.0.0.1:3099/socket-mode' }`
    6. `auth.test returns bot identity for xoxb token and user identity for oauth.v2.access authed_user token`
      - Use `seedSlackBotToken('xoxb-bot-auth-parity')`
      - Use a successful authorize/exchange to get `authed_user.access_token`
      - Assert bot result has `user === 'bot'`, `user_id === 'U_BOT_TWIN'`, `bot_id === 'B_BOT_TWIN'`
      - Assert user result has `user === 'authed-user'`, `user_id === 'U_AUTHED'`, and `bot_id === undefined`

    Keep the file Wave-0-only: do not fix any production code here, and do not weaken assertions to “resolves” or “ok: true”.
  </action>
  <acceptance_criteria>
    - `rg -n "openid\\.connect\\.token round-trips|mismatched client_secret|authorize-time granted scope string|xapp token with connections:write|auth\\.test returns bot identity" tests/sdk-verification/sdk/slack-auth-parity.test.ts` returns all six named tests.
    - `rg -n "admin/set-wss-url|admin/tokens|oauth\\.v2.access|openid\\.connect\\.userInfo" tests/sdk-verification/sdk/slack-auth-parity.test.ts` shows the file exercises the exact routes involved in the confirmed defects.
    - `rg -n "authed-user@twin\\.dev|U_AUTHED|invalid_client|ws://127\\.0\\.0\\.1:3099/socket-mode" tests/sdk-verification/sdk/slack-auth-parity.test.ts` returns the expected parity values.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-auth-parity.test.ts</automated>
  </verify>
  <done>`slack-auth-parity.test.ts` exists, contains the six named Wave 0 parity tests, and the targeted run fails for the current OIDC, oauth.v2.access, app-token, and auth.test drifts rather than passing green by accident.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Wave 0 conversation-scope and client-behavior parity tests</name>
  <read_first>
    tests/sdk-verification/sdk/slack-conversations.test.ts
    tests/sdk-verification/sdk/slack-scope-enforcement.test.ts
    tests/sdk-verification/sdk/slack-webclient-base.test.ts
    twins/slack/test/integration.test.ts
    tests/sdk-verification/setup/seeders.ts
    twins/slack/src/plugins/web-api/conversations.ts
    twins/slack/src/plugins/web-api/files.ts
    twins/slack/src/services/interaction-handler.ts
    twins/slack/src/plugins/interactions.ts
  </read_first>
  <files>tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts, tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts</files>
  <action>
    Create two new Wave 0 test files.

    First, create `tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts` with `beforeEach(async () => { await resetSlack(); })`. Seed channels through `POST /admin/fixtures/load` with these exact shapes:
    - public: `{ id: 'C_SCOPE_PUBLIC', name: 'scope-public', is_channel: true, is_private: false }`
    - private: `{ id: 'G_SCOPE_PRIVATE', name: 'scope-private', is_channel: true, is_private: true }`
    - dm: `{ id: 'D_SCOPE_DM', name: 'scope-dm', is_channel: false, is_private: true }`
    - mpim: `{ id: 'G_SCOPE_MPIM', name: 'scope-mpim', is_channel: false, is_private: true }`

    Add these RED tests:
    1. `conversations.list types=public_channel succeeds with channels:read and x-accepted-oauth-scopes=channels:read`
    2. `conversations.list types=private_channel fails with needed=groups:read when the token only has channels:read`
    3. `conversations.info uses groups:read for G_SCOPE_PRIVATE and channels:read for C_SCOPE_PUBLIC`
    4. `conversations.history uses im:history for D_SCOPE_DM and channels:history for C_SCOPE_PUBLIC`

    Use direct `fetch()` for these assertions so you can inspect both body fields and `X-Accepted-OAuth-Scopes` headers.

    Second, create `tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts`. Add `beforeEach(async () => { await resetSlack(); })` and these RED tests:
    1. `filesUploadV2 returns nested completed metadata with id, name, and permalink`
      - Use `createSlackClient(await seedSlackBotToken('xoxb-files-wave0'))`
      - Call `client.filesUploadV2({ content: 'wave0 text', filename: 'wave0.txt', channel_id: 'C_GENERAL' })`
      - Assert `response.files[0].files[0].id` matches `/^F_/`, `name === 'wave0.txt'`, and `permalink` contains `/files/`
    2. `files.completeUploadExternal accepts files as a JSON string payload`
      - POST directly to `/api/files.completeUploadExternal` with body `{ files: '[{"id":"F_WAVE0","title":"wave0.txt"}]' }`
      - Assert returned `files[0].name === 'wave0.txt'`
    3. `response_url replace_original updates the original message ts instead of appending a new message`
      - Seed a message with `chat.postMessage`
      - Trigger `/admin/interactions/trigger`
      - POST `{ text: 'replacement text', replace_original: true }` to the returned `response_url`
      - Assert `conversations.history` still contains the original `ts` with text `replacement text` and does not contain a second message with that text
    4. `response_url delete_original removes the original message from conversations.history`
      - Trigger a second interaction against a fresh message
      - POST `{ delete_original: true }` to the returned `response_url`
      - Assert `conversations.history` no longer includes the original `ts`

    Keep both files focused on the six confirmed defects only. Do not add broad coverage or new helper abstractions.
  </action>
  <acceptance_criteria>
    - `rg -n "types=public_channel|needed=groups:read|im:history|channels:history" tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts` returns the four named dynamic-scope assertions.
    - `rg -n "filesUploadV2 returns nested completed metadata|files\\.completeUploadExternal accepts files as a JSON string payload|replace_original updates the original message|delete_original removes the original message" tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts` returns the four named client-visible behavior assertions.
    - `rg -n "admin/fixtures/load|admin/interactions/trigger|response_url|conversations.history" tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts` shows both new files hit the exact twin routes implicated by the defects.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts</automated>
  </verify>
  <done>The two new test files exist, name every confirmed conversation-scope and client-visible behavior defect explicitly, and fail red against the current implementation instead of silently passing.</done>
</task>

</tasks>

<verification>
Run the three Wave 0 files and confirm they fail for the expected reasons:
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-auth-parity.test.ts`
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-conversation-scope-parity.test.ts`
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts`

The failures should come from parity assertions, not from missing setup, missing environment variables, or transport-level 404s.
</verification>

<success_criteria>
- REQUIREMENTS.md defines SLCK-20, SLCK-21, SLCK-22, and SLCK-23 for Phase 38 and moves the old deferred Enterprise Grid item to SLCK-24
- `slack-auth-parity.test.ts` covers OIDC round-trip, oauth.v2.access client_secret and scope propagation, apps.connections.open token class, and auth.test identity drift
- `slack-conversation-scope-parity.test.ts` covers dynamic scope resolution for conversations.list/info/history
- `slack-client-behavior-parity.test.ts` covers filesUploadV2 metadata and response_url replace/delete behavior
- All three targeted commands fail red before Plans 02-04 land
</success_criteria>

<output>
After completion, create `.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-01-SUMMARY.md`
</output>
