---
phase: 38-slack-auth-scope-and-client-behavior-parity
plan: 04
type: execute
wave: 1
depends_on:
  - 38-01
files_modified:
  - twins/slack/src/plugins/web-api/files.ts
  - twins/slack/src/services/interaction-handler.ts
autonomous: true
requirements:
  - SLCK-22

must_haves:
  truths:
    - "filesUploadV2 returns Slack-shaped completed file metadata with id, name, and permalink"
    - "files.completeUploadExternal accepts the files field when it arrives as a JSON string"
    - "response_url with replace_original updates the original message instead of appending a second one"
    - "response_url with delete_original removes the original message instead of appending a tombstone message"
    - "Existing Slack WebClient base behavior tests stay green after these client-visible semantics become stricter"
  artifacts:
    - path: "twins/slack/src/plugins/web-api/files.ts"
      provides: "files.completeUploadExternal normalization and rich completed file metadata"
      contains: "permalink"
    - path: "twins/slack/src/services/interaction-handler.ts"
      provides: "replace_original and delete_original response_url behavior"
      contains: "replace_original"
  key_links:
    - from: "twins/slack/src/plugins/web-api/files.ts"
      to: "tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts"
      via: "filesUploadV2 nested completion response"
      pattern: "files.completeUploadExternal|permalink"
    - from: "twins/slack/src/services/interaction-handler.ts"
      to: "tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts"
      via: "response_url replace/delete semantics"
      pattern: "replace_original|delete_original"
---

<objective>
Fix the remaining client-visible Slack behavior drift: `filesUploadV2` must complete with Slack-shaped file metadata, and `response_url` must honor `replace_original` and `delete_original` instead of always appending a new message.

Purpose: These flows are visible to real Slack clients and Bolt apps. Returning `ok: true` is not enough if the payload shape or follow-up message semantics are wrong.
Output: Normalized `files.completeUploadExternal` behavior in `files.ts` and correct `response_url` mutation behavior in `interaction-handler.ts`.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-RESEARCH.md
@.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md
@tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts
@tests/sdk-verification/sdk/slack-webclient-base.test.ts
@twins/slack/test/integration.test.ts
</context>

<interfaces>
From upstream `third_party/upstream/node-slack-sdk/packages/web-api/src/WebClient.spec.ts`:
```typescript
.post('/api/files.completeUploadExternal', { files: '[{"id":"F0123456789","title":"test-txt.txt"}]' })
.reply(200, {
  ok: true,
  files: [{ id: 'F0123456789', name: 'test-txt.txt', permalink: 'https://my-workspace.slack.com/files/U0123456789/F0123456789/test-txt.txt' }],
});
```

From `twins/slack/src/plugins/web-api/files.ts`:
```typescript
const { files } = (request.body as any) ?? {};
const completed = (files ?? []).map((f: any) => ({ id: f.id, title: f.title ?? 'Uploaded file' }));
return { ok: true, files: completed };
```

From `twins/slack/src/services/interaction-handler.ts`:
```typescript
entry.usesRemaining--;
const ts = generateMessageTs();
this.slackStateManager.createMessage({ channel_id: entry.channelId, user_id: 'U_BOT_TWIN', text: body.text ?? '', ... });
return { ok: true };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Normalize files.completeUploadExternal input and return Slack-shaped file metadata</name>
  <read_first>
    twins/slack/src/plugins/web-api/files.ts
    tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts
    tests/sdk-verification/sdk/slack-webclient-base.test.ts
    third_party/upstream/node-slack-sdk/packages/web-api/src/WebClient.spec.ts
  </read_first>
  <files>twins/slack/src/plugins/web-api/files.ts</files>
  <action>
    Update `POST /api/files.completeUploadExternal` so it matches the upstream client contract.

    1. Add a local helper in `files.ts` that normalizes `request.body.files`:
    - if it is a string, parse it as JSON
    - if it is already an array, use it directly
    - if parsing fails or the normalized value is not an array, return `{ ok: false, error: 'invalid_arguments' }`
    2. Inside the completion map, compute `const name = f.title ?? f.name ?? 'uploaded-file.txt';` once and then build each completed file object with these exact fields:
    - `id: f.id`
    - `name`
    - `title: name`
    - `mimetype: 'text/plain'`
    - `filetype: 'text'`
    - `user: tokenRecord.user_id ?? 'U_BOT_TWIN'`
    - `url_private: \`https://twin-workspace.slack.com/files/${tokenRecord.user_id ?? 'U_BOT_TWIN'}/${f.id}/${encodeURIComponent(name)}\``
    - `permalink: \`https://twin-workspace.slack.com/files/${tokenRecord.user_id ?? 'U_BOT_TWIN'}/${f.id}/${encodeURIComponent(name)}\``
    3. Return `{ ok: true, files: completed, response_metadata: {} }`

    Leave `files.getUploadURLExternal` and the binary upload endpoint unchanged. This task is only about step 3 normalization and metadata shape.
  </action>
  <acceptance_criteria>
    - `rg -n "typeof files === 'string'|JSON\\.parse\\(files\\)|invalid_arguments" twins/slack/src/plugins/web-api/files.ts` shows the JSON-string normalization path and failure mode.
    - `rg -n "name: .*uploaded-file\\.txt|url_private:|permalink:|response_metadata: \\{\\}" twins/slack/src/plugins/web-api/files.ts` shows the Slack-shaped completed file fields.
    - `rg -n "tokenRecord\\.user_id \\?\\? 'U_BOT_TWIN'" twins/slack/src/plugins/web-api/files.ts` shows the response metadata is tied to the authenticated token class.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts tests/sdk-verification/sdk/slack-webclient-base.test.ts -t "filesUploadV2|files.completeUploadExternal"</automated>
  </verify>
  <done>The filesUploadV2 parity assertions pass with nested file metadata, and the existing WebClient base upload-chain test remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Honor replace_original and delete_original in response_url handling</name>
  <read_first>
    twins/slack/src/services/interaction-handler.ts
    twins/slack/src/plugins/interactions.ts
    tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts
    twins/slack/test/integration.test.ts
  </read_first>
  <files>twins/slack/src/services/interaction-handler.ts</files>
  <action>
    Change `InteractionHandler.handleResponseUrl()` so it mutates the original message when requested instead of always appending.

    1. Keep the existing expiry and `usesRemaining` handling unchanged.
    2. After decrementing `entry.usesRemaining`, branch in this order:
    - If `body.delete_original === true`, delete the original message row by running `this.slackStateManager.database.prepare('DELETE FROM slack_messages WHERE ts = ? AND channel_id = ?').run(entry.messageTs, entry.channelId)` and return `{ ok: true }`
    - Else if `body.replace_original === true`, call `this.slackStateManager.updateMessage(entry.messageTs, { text: body.text ?? '', blocks: body.blocks ? JSON.stringify(body.blocks) : '' })` and return `{ ok: true }`
    - Else, preserve the current append behavior by creating a new message with `generateMessageTs()`
    3. Do not create a new message in the replace or delete branches.

    Use the existing `database` getter on `SlackStateManager`; do not add a new state-manager method in this plan. File ownership for client-visible behavior stays limited to `interaction-handler.ts`.
  </action>
  <acceptance_criteria>
    - `rg -n "delete_original === true|DELETE FROM slack_messages WHERE ts = \\? AND channel_id = \\?" twins/slack/src/services/interaction-handler.ts` shows the delete-original path.
    - `rg -n "replace_original === true|updateMessage\\(entry\\.messageTs" twins/slack/src/services/interaction-handler.ts` shows the replace-original path.
    - `rg -n "createMessage\\(" twins/slack/src/services/interaction-handler.ts` still appears exactly once in the append-only fallback branch.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts -t "replace_original|delete_original"</automated>
  </verify>
  <done>`response_url` replace/delete parity tests pass, and the handler still respects the original 5-use / 30-minute lifecycle contract.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-client-behavior-parity.test.ts`
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-webclient-base.test.ts`

Then sample the existing Slack integration interaction flow if needed:
- `pnpm vitest run twins/slack/test/integration.test.ts -t "response URL works"`
</verification>

<success_criteria>
- `filesUploadV2` returns nested completion payloads with file metadata that includes `id`, `name`, and `permalink`
- `files.completeUploadExternal` accepts `files` as a JSON string or array
- `response_url` with `replace_original` mutates the original message instead of creating a duplicate
- `response_url` with `delete_original` removes the original message from conversation history
</success_criteria>

<output>
After completion, create `.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-04-SUMMARY.md`
</output>
