---
phase: 38-slack-auth-scope-and-client-behavior-parity
plan: 02
type: execute
wave: 1
depends_on:
  - 38-01
files_modified:
  - twins/slack/src/plugins/oauth.ts
  - twins/slack/src/plugins/web-api/new-families.ts
  - twins/slack/src/plugins/web-api/auth.ts
  - twins/slack/src/plugins/web-api/stubs.ts
  - tests/sdk-verification/setup/seeders.ts
  - tests/sdk-verification/sdk/slack-bolt-socket-mode-receiver.test.ts
  - twins/slack/test/smoke.test.ts
  - twins/slack/test/auth.test.ts
  - twins/slack/test/web-api.test.ts
  - twins/slack/test/ui.test.ts
  - twins/slack/test/integration.test.ts
autonomous: true
requirements:
  - SLCK-20
  - SLCK-23

must_haves:
  truths:
    - "openid.connect.token returns an access token that openid.connect.userInfo accepts in a second request"
    - "oauth.v2.access rejects a mismatched client_secret and echoes the authorize-time granted scope string on success"
    - "apps.connections.open rejects bot tokens with connections:write and accepts only xapp app tokens"
    - "auth.test returns bot identity for bot tokens and user identity for OAuth-issued user tokens"
    - "Socket Mode tests seed a real app token instead of seeding an xapp string as a bot token"
    - "Existing in-repo Slack twin tests exchange OAuth codes with explicit client_id and client_secret after oauth.v2.access is tightened"
    - "Existing InstallProvider and auth gateway tests remain green after tightening these behaviors"
  artifacts:
    - path: "twins/slack/src/plugins/oauth.ts"
      provides: "client_secret validation and granted-scope propagation for oauth.v2.access"
      contains: "client_secret"
    - path: "twins/slack/src/plugins/web-api/new-families.ts"
      provides: "stateful OIDC token issuance and userInfo route parity"
      contains: "openid.connect.userInfo"
    - path: "twins/slack/src/plugins/web-api/stubs.ts"
      provides: "app-token enforcement for apps.connections.open"
      contains: "xapp-"
    - path: "twins/slack/src/plugins/web-api/auth.ts"
      provides: "token-class-aware auth.test identity response"
      contains: "tokenRecord.token_type"
    - path: "tests/sdk-verification/setup/seeders.ts"
      provides: "dedicated Slack app-token seeder for Socket Mode and app-token parity tests"
      contains: "seedSlackAppToken"
    - path: "twins/slack/test/auth.test.ts"
      provides: "updated in-repo OAuth happy-path tests that pass explicit client credentials"
      contains: "client_secret: 'test'"
  key_links:
    - from: "twins/slack/src/plugins/oauth.ts"
      to: "twins/slack/src/plugins/web-api/auth.ts"
      via: "oauth.v2.access user token -> auth.test identity branch"
      pattern: "authed_user|token_type"
    - from: "twins/slack/src/plugins/web-api/new-families.ts"
      to: "tests/sdk-verification/sdk/slack-auth-parity.test.ts"
      via: "openid.connect.token -> openid.connect.userInfo round-trip"
      pattern: "openid.connect.token|openid.connect.userInfo"
    - from: "twins/slack/src/plugins/web-api/stubs.ts"
      to: "tests/sdk-verification/sdk/slack-auth-parity.test.ts"
      via: "apps.connections.open rejects bot token but accepts xapp token"
      pattern: "apps.connections.open"
    - from: "tests/sdk-verification/setup/seeders.ts"
      to: "tests/sdk-verification/sdk/slack-bolt-socket-mode-receiver.test.ts"
      via: "Socket Mode app token must be stored with tokenType 'app'"
      pattern: "seedSlackAppToken|xapp-"
    - from: "twins/slack/src/plugins/oauth.ts"
      to: "twins/slack/test/integration.test.ts"
      via: "tightened oauth.v2.access contract requires explicit client credentials in local twin tests"
      pattern: "client_id: 'test'|client_secret: 'test'"
---

<objective>
Fix the Slack auth and token-semantics defects proven by Wave 0: OIDC tokens must be stateful, oauth.v2.access must validate credentials and preserve granted scopes, apps.connections.open must require an app token, and auth.test must report the correct identity shape for the token class.

Purpose: These defects are all part of the same issuance-and-consumption seam. If token exchange, token storage, and token-class-specific auth behavior drift apart, the Slack SDK still initializes but real parity breaks.
Output: Tightened `oauth.ts`, stateful OIDC handlers in `new-families.ts`, app-token enforcement in `stubs.ts`, and token-class-aware `auth.test`.
</objective>

<execution_context>
@/Users/futur/.codex/get-shit-done/workflows/execute-plan.md
@/Users/futur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-RESEARCH.md
@.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-VALIDATION.md
@.planning/phases/35-slack-behavioral-parity/35-01-SUMMARY.md
@.planning/phases/31-slack-oauth-method-coverage/31-02-SUMMARY.md
@.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-01-PLAN.md
@tests/sdk-verification/sdk/slack-auth-parity.test.ts
</context>

<interfaces>
From `twins/slack/src/plugins/oauth.ts`:
```typescript
interface CodeBinding {
  redirectUri: string;
  scope: string;
  clientId: string;
}
const issuedCodes = new Map<string, CodeBinding>();
```

From `twins/slack/src/plugins/web-api/new-families.ts`:
```typescript
fastify.post('/api/openid.connect.token', async (request, reply) => { ... });
fastify.post('/api/openid.connect.userInfo', stub('openid.connect.userInfo', ...));
```

From `twins/slack/src/plugins/web-api/stubs.ts`:
```typescript
fastify.post('/api/apps.connections.open', async (request, reply) => {
  const tokenRecord = fastify.slackStateManager.getToken(token);
  const scopeCheck = checkScope('apps.connections.open', tokenRecord.scope);
  const wssUrl = fastify.slackStateManager.getWssUrl();
  return reply.send({ ok: true, url: wssUrl });
});
```

From `twins/slack/src/plugins/web-api/auth.ts`:
```typescript
return reply.status(200).send({
  ok: true,
  url: 'https://twin-workspace.slack.com/',
  team: 'Twin Workspace',
  user: 'bot',
  team_id: TWIN_TEAM_ID,
  user_id: TWIN_USER_ID,
  bot_id: TWIN_BOT_ID,
  is_enterprise_install: false,
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Make OIDC and oauth.v2.access stateful and credential-validating</name>
  <read_first>
    twins/slack/src/plugins/oauth.ts
    twins/slack/src/plugins/web-api/new-families.ts
    tests/sdk-verification/sdk/slack-auth-parity.test.ts
    tests/sdk-verification/sdk/slack-oauth-install-provider.test.ts
    .planning/phases/35-slack-behavioral-parity/35-01-SUMMARY.md
    .planning/phases/31-slack-oauth-method-coverage/31-02-SUMMARY.md
  </read_first>
  <files>twins/slack/src/plugins/oauth.ts, twins/slack/src/plugins/web-api/new-families.ts</files>
  <action>
    Implement the token-exchange fixes without introducing a new storage system.

    In `twins/slack/src/plugins/oauth.ts`:
    1. Add a local `CLIENT_SECRETS` mapping above `oauthPlugin` with these exact entries:
    - `'test': 'test'`
    - `'test-client': 'test-client-secret'`
    - `'test-client-id-19': 'test-client-secret-19'`
    Use `const expectedSecret = CLIENT_SECRETS[client_id];`.
    2. In `POST /api/oauth.v2.access`, destructure `client_secret` from the request body and reject the exchange with `{ ok: false, error: 'invalid_client' }` when `client_secret` is missing or does not equal `expectedSecret`.
    Also reject unknown `client_id` values the same way: if `expectedSecret` is falsy, return `{ ok: false, error: 'invalid_client' }`.
    3. Replace the hardcoded `botScopes` and `userScopes` strings with `binding.scope`. Persist both the bot token and the user token using `binding.scope`, and return `scope: binding.scope` plus `authed_user.scope: binding.scope` exactly as granted at authorize time.
    4. Before creating the user token, ensure `U_AUTHED` exists via `fastify.slackStateManager.createUser({ id: 'U_AUTHED', team_id: 'T_TWIN', name: 'authed-user', real_name: 'Authed User', email: 'authed-user@twin.dev' })`.

    In `twins/slack/src/plugins/web-api/new-families.ts`:
    1. Keep `openid.connect.token` as a no-auth handler, but validate and persist the issued token. Reuse the same `CLIENT_SECRETS` map shape as `oauth.ts`; reject unknown `client_id` or mismatched `client_secret` with `{ ok: false, error: 'invalid_client' }`. Build `const oidcToken = \`xoxp-oidc-${suffix}\`;`, ensure `U_AUTHED` exists with the same `authed-user` seed values, and call `fastify.slackStateManager.createToken(oidcToken, 'user', 'T_TWIN', 'U_AUTHED', 'openid', 'A_TWIN')` before replying.
    2. Replace the generic `stub('openid.connect.userInfo', ...)` registration with a dedicated handler that:
    - extracts the bearer token with `extractToken(request)`
    - looks up the token record via `fastify.slackStateManager.getToken(token)`
    - runs `checkScope('openid.connect.userInfo', tokenRecord.scope)`
    - sets `X-OAuth-Scopes` and `X-Accepted-OAuth-Scopes`
    - loads the user record with `fastify.slackStateManager.getUser(tokenRecord.user_id)`
    - returns `{ ok: true, sub: tokenRecord.user_id, 'https://slack.com/user_id': tokenRecord.user_id, email: user?.email ?? 'authed-user@twin.dev', email_verified: true, name: user?.real_name ?? user?.name ?? 'Authed User' }`

    Do not add new tables, new env vars, or a second token store. Reuse the existing token and user rows only.
  </action>
  <acceptance_criteria>
    - `rg -n "CLIENT_SECRETS|expectedSecret = CLIENT_SECRETS\\[client_id\\]|invalid_client|binding\\.scope|authed-user@twin\\.dev" twins/slack/src/plugins/oauth.ts` shows the explicit credential-validation and granted-scope propagation logic without a fallback secret pattern.
    - `rg -n "createToken\\(oidcToken, 'user', 'T_TWIN', 'U_AUTHED', 'openid', 'A_TWIN'\\)|openid\\.connect\\.userInfo|email_verified: true" twins/slack/src/plugins/web-api/new-families.ts` shows the OIDC token persistence and stateful userInfo response.
    - `rg -n "authed-user|Authed User|U_AUTHED" twins/slack/src/plugins/oauth.ts twins/slack/src/plugins/web-api/new-families.ts` returns the seeded user identity values used by Wave 0 tests.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-auth-parity.test.ts -t "openid.connect.token|mismatched client_secret|authorize-time granted scope string"</automated>
  </verify>
  <done>The OIDC round-trip, oauth.v2.access client_secret failure, and granted-scope echo tests pass green without regressing the existing InstallProvider happy path.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce app-token semantics and token-class-aware auth.test identity</name>
  <read_first>
    twins/slack/src/plugins/web-api/stubs.ts
    twins/slack/src/plugins/web-api/auth.ts
    tests/sdk-verification/setup/seeders.ts
    tests/sdk-verification/sdk/slack-auth-parity.test.ts
    tests/sdk-verification/sdk/slack-auth-gateway.test.ts
    tests/sdk-verification/sdk/slack-bolt-socket-mode-receiver.test.ts
  </read_first>
  <files>twins/slack/src/plugins/web-api/stubs.ts, twins/slack/src/plugins/web-api/auth.ts, tests/sdk-verification/setup/seeders.ts, tests/sdk-verification/sdk/slack-bolt-socket-mode-receiver.test.ts</files>
  <action>
    Tighten the token consumer side to match the new issuance semantics.

    In `twins/slack/src/plugins/web-api/stubs.ts`:
    1. In `POST /api/apps.connections.open`, after token lookup and before returning the WSS URL, reject any token that is not both:
    - stored with `tokenRecord.token_type === 'app'`
    - prefixed with `xapp-`
    Return `{ ok: false, error: 'invalid_auth' }` for bot or user tokens even if they have `connections:write`.
    2. Keep the existing `connections:write` scope check and `X-OAuth-Scopes` / `X-Accepted-OAuth-Scopes` headers for valid app tokens.

    In `twins/slack/src/plugins/web-api/auth.ts`:
    1. Replace the single hardcoded bot-shaped response with a token-type-aware branch.
    2. For `tokenRecord.token_type === 'bot'`, preserve the current response exactly.
    3. For `tokenRecord.token_type === 'user'`, load the user record and return:
    - `user: user?.name ?? 'authed-user'`
    - `team_id: tokenRecord.team_id`
    - `user_id: tokenRecord.user_id`
    - `is_enterprise_install: false`
    Omit `bot_id` from this branch.
    4. For `tokenRecord.token_type === 'app'`, return:
    - `user: 'app'`
    - `team_id: tokenRecord.team_id`
    - `user_id: tokenRecord.user_id`
    - `app_id: tokenRecord.app_id`
    - `is_enterprise_install: false`
    Omit `bot_id` from this branch as well.

    Do not change the existing auth preamble, rate-limit handling, or scope headers. This task is only about token-class semantics after the token is already valid.

    In `tests/sdk-verification/setup/seeders.ts`:
    1. Add `export async function seedSlackAppToken(token = 'xapp-test-token'): Promise<string>` that POSTs to `/admin/tokens` with:
    - `token`
    - `tokenType: 'app'`
    - `teamId: 'T_TWIN'`
    - `userId: 'U_APP_TWIN'`
    - `scope: 'connections:write'`
    - `appId: 'A_TWIN'`
    2. Leave `seedSlackBotToken()` unchanged. The dedicated app-token helper exists so tests stop encoding app-token semantics through a bot token record.

    In `tests/sdk-verification/sdk/slack-bolt-socket-mode-receiver.test.ts`:
    1. Change the import to `resetSlack, seedSlackAppToken, seedSlackBotToken`.
    2. Replace `await seedSlackBotToken(APP_TOKEN);` with `await seedSlackAppToken(APP_TOKEN);`.
    3. Do not change `TOKEN = await seedSlackBotToken('xoxb-slck12-sm-bot-token');` — the bot token still backs the Bolt `App` instance, while the app token is only for `apps.connections.open`.
  </action>
  <acceptance_criteria>
    - `rg -n "tokenRecord\\.token_type !== 'app'|!token\\.startsWith\\('xapp-'" twins/slack/src/plugins/web-api/stubs.ts` shows the app-token gate in apps.connections.open.
    - `rg -n "tokenRecord\\.token_type === 'user'|tokenRecord\\.token_type === 'app'|user\\?\\.name \\?\\? 'authed-user'|app_id: tokenRecord\\.app_id" twins/slack/src/plugins/web-api/auth.ts` shows the token-class-aware auth.test branches.
    - `rg -n "bot_id" twins/slack/src/plugins/web-api/auth.ts` only appears in the bot branch and not in the user or app branches.
    - `rg -n "seedSlackAppToken|tokenType: 'app'|scope: 'connections:write'" tests/sdk-verification/setup/seeders.ts` shows the dedicated app-token seeder.
    - `rg -n "seedSlackAppToken\\(APP_TOKEN\\)" tests/sdk-verification/sdk/slack-bolt-socket-mode-receiver.test.ts` shows the Socket Mode test now seeds a real app token.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-auth-parity.test.ts tests/sdk-verification/sdk/slack-auth-gateway.test.ts tests/sdk-verification/sdk/slack-bolt-socket-mode-receiver.test.ts tests/sdk-verification/sdk/slack-oauth-install-provider.test.ts</automated>
  </verify>
  <done>All auth-parity, auth-gateway, Socket Mode, and InstallProvider assertions pass with bot, user, and app tokens behaving according to their token class.</done>
</task>

<task type="auto">
  <name>Task 3: Update in-repo Slack twin tests to the tightened oauth.v2.access contract</name>
  <read_first>
    twins/slack/test/smoke.test.ts
    twins/slack/test/auth.test.ts
    twins/slack/test/web-api.test.ts
    twins/slack/test/ui.test.ts
    twins/slack/test/integration.test.ts
    twins/slack/src/plugins/oauth.ts
  </read_first>
  <files>twins/slack/test/smoke.test.ts, twins/slack/test/auth.test.ts, twins/slack/test/web-api.test.ts, twins/slack/test/ui.test.ts, twins/slack/test/integration.test.ts</files>
  <action>
    Bring the local Slack twin test suite up to the same OAuth contract the SDK tests already expect.

    In each of these files:
    - `twins/slack/test/smoke.test.ts`
    - `twins/slack/test/auth.test.ts`
    - `twins/slack/test/web-api.test.ts`
    - `twins/slack/test/ui.test.ts`
    - `twins/slack/test/integration.test.ts`

    update every `POST /api/oauth.v2.access` happy-path request that currently sends only `{ code }` so it instead sends:
    - `code`
    - `client_id: 'test'`
    - `client_secret: 'test'`

    Keep the existing authorize step:
    - `GET /oauth/v2/authorize?client_id=test&scope=chat:write&redirect_uri=https://localhost/callback&state=test`

    For the form-urlencoded coverage in `twins/slack/test/web-api.test.ts`, update the encoded body to include:
    - `client_id=test`
    - `client_secret=test`
    - `code=<real auth code>`

    Do not weaken any assertions or replace the OAuth path with `/admin/tokens`. These tests should continue proving that the tightened OAuth endpoint still supports the documented happy path.
  </action>
  <acceptance_criteria>
    - `rg -n "client_id: 'test'|client_secret: 'test'" twins/slack/test/smoke.test.ts twins/slack/test/auth.test.ts twins/slack/test/web-api.test.ts twins/slack/test/ui.test.ts twins/slack/test/integration.test.ts` returns the explicit credential fields in every updated happy-path exchange.
    - `rg -n "client_id=test&client_secret=test" twins/slack/test/web-api.test.ts` returns the updated form-urlencoded coverage.
    - `rg -n "payload: \\{ code \\}" twins/slack/test/smoke.test.ts twins/slack/test/auth.test.ts twins/slack/test/web-api.test.ts twins/slack/test/ui.test.ts twins/slack/test/integration.test.ts` returns no remaining happy-path exchanges that omit client credentials.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/futur/projects/sandpiper-dtu && pnpm vitest run twins/slack/test/smoke.test.ts twins/slack/test/auth.test.ts twins/slack/test/web-api.test.ts twins/slack/test/ui.test.ts twins/slack/test/integration.test.ts</automated>
  </verify>
  <done>The local Slack twin smoke, auth, web-api, UI, and integration suites still pass while exercising the tightened oauth.v2.access contract with explicit client credentials.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-auth-parity.test.ts`
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-auth-gateway.test.ts`
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-bolt-socket-mode-receiver.test.ts`
- `pnpm test:sdk -- --run tests/sdk-verification/sdk/slack-oauth-install-provider.test.ts`
- `pnpm vitest run twins/slack/test/smoke.test.ts twins/slack/test/auth.test.ts twins/slack/test/web-api.test.ts twins/slack/test/ui.test.ts twins/slack/test/integration.test.ts`

All five commands must pass without reintroducing broad hardcoded scopes or non-stateful OIDC tokens.
</verification>

<success_criteria>
- OIDC access tokens created by `openid.connect.token` are stored and accepted by `openid.connect.userInfo`
- `oauth.v2.access` rejects the wrong `client_secret` and returns the exact authorize-time scope string
- `apps.connections.open` returns `invalid_auth` for `xoxb-` bot tokens and succeeds for `xapp-` app tokens with `connections:write`
- `auth.test` returns bot identity for bot tokens and user identity for OAuth-issued user tokens
</success_criteria>

<output>
After completion, create `.planning/phases/38-slack-auth-scope-and-client-behavior-parity/38-02-SUMMARY.md`
</output>
